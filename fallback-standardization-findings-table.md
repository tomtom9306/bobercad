# Human-Readable Findings Table

Generated from `fallback-standardization-audit.txt`. This is a compact review table; the source audit remains the detailed evidence log.

## Coverage

| Metric | Value |
| --- | ---: |
| Findings extracted | 216 |
| App text/reviewable files covered in source audit | 276 / 276 |
| App binary/assets inventoried in source audit | 7 / 7 |
| Source nonblank physical lines recorded | 72,178 |
| Support files covered in source audit | 62 / 62 |
| Support nonblank physical lines recorded | 36,737 / 36,737 |
| Data MJS files covered in remainder pass | 27 / 27 |
| Data MJS nonblank lines covered | 4,360 / 4,360 |
| Data JSON/MD files covered in remainder pass | 64 / 64 |
| Data JSON/MD nonblank lines covered | 843,710 / 843,710 |
| Other root/tmp/audit files covered | 10 / 10 |
| Current data file | Data pass complete |

## Counts By Area

| Area | Findings |
| --- | ---: |
| Rendering interaction | 40 |
| Viewer UI | 25 |
| UI commands / metadata | 20 |
| Smart Components | 31 |
| UI design / CSS | 13 |
| UI shell / workspace | 12 |
| Rendering scene | 6 |
| Rendering WebGL | 6 |
| Connection primitives | 5 |
| Engine store | 5 |
| Engine geometry | 4 |
| Viewer dimensions | 4 |
| Engine project API | 2 |
| Tooling / scripts | 29 |
| Docs / audit records | 13 |
| Engine model / schema | 1 |

## Counts By Type

| Type | Findings |
| --- | ---: |
| Fallback / hidden default | 67 |
| Compatibility layer | 36 |
| Duplicated logic | 27 |
| Contract drift | 39 |
| API bypass | 16 |
| Standardization | 23 |
| CSS / selector issue | 8 |

## Counts By Review Priority

| Priority | Findings |
| --- | ---: |
| High | 23 |
| Medium | 126 |
| Low | 67 |

## Fast Review Queue

This table keeps the details compact. Use the ID to jump back to the detailed evidence in `fallback-standardization-audit.txt`.

| ID | Priority | Area | Type | Finding | Main evidence | Why it matters | Suggested action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F01 | Medium | UI shell / workspace | Compatibility layer | Workspace storage keeps a legacy toolbar compatibility layer | `bobercad/app/ui/shell/workspace-storage.mjs:25-52` | Every read/write/import passes through migration, so old UI storage shape remains a live branch in normal runtime code. | If migration from the old localStorage/export format is no longer required, remove `hasLegacyModelingToolbar`/`legacyModelingToolbar`; otherwise move it to a named one-time importer and keep runtime storage current-shape-only. |
| F02 | Medium | Tooling / scripts | Compatibility layer | Dev server accepts both canonical and legacy encoded branch URL prefixes | `scripts/serve_viewer.js:98-108` | The preview URL contract says `/codex/agentN/...` is required, but the server still supports `/codex%2FagentN/...`. | Remove `legacyEncodedBranchPathPrefix` once old links are not needed, or document the deprecation window. |
| F03 | Medium | Engine geometry | Compatibility layer | Object trim region keys still carry legacy aliases | `bobercad/app/engine/geometry/trim-region-geometry.mjs:116-125` | Region removal accepts two identities for the same derived part, which makes future data migrations and UI display of region keys harder to reason about. | Migrate existing sample/project data to canonical region keys, then remove the alias path; until then, document it as an explicit compatibility layer. |
| F04 | High | UI commands / metadata | Compatibility layer | Planned commands are visible product surface with no real implementation path | `bobercad/app/ui/commands/command-registry.mjs:197-228`<br>`bobercad/app/ui/viewer/viewer-command-registration.mjs:346-348` | The command registry, navbar, palette, state resolver, and handler map all carry placeholder workflow branches. | Either hide unimplemented commands from runtime surfaces or centralize planned-command state/handler behavior from command metadata instead of registering bespoke no-op handlers. |
| F05 | Medium | Smart Components | Duplicated logic | Validation helpers are copied across engine API modules instead of inherited from one validator contract | `bobercad/app/engine/api/model/semantic-builders.mjs:48-113`<br>`bobercad/app/engine/api/project/axis-relations.mjs:10-29`<br>`bobercad/app/engine/api/project/member-factory.mjs:8-84`<br>`bobercad/app/engine/api/model/transport-sectioning.mjs:9-60` | `bobercad/app/engine/core/math.mjs:20-49` and `bobercad/app/engine/core/model.mjs:3-20` already provide some primitives, but not a complete error-context validator API. | Add one shared validation module with domain error prefixes/context adapters, then migrate module-local required/optional helpers onto it. |
| F06 | Medium | Rendering interaction | Duplicated logic | 2D/3D geometry primitives are partly duplicated | `bobercad/app/engine/api/model/geometry.mjs:17-48`<br>`bobercad/app/engine/geometry/polygon.mjs:68-79` | Boundary behavior and tolerance rules can drift between model API geometry and rendering/CSG geometry. | Promote a shared 2D polygon predicate and vec/point validator API, then call it from model geometry, snap, and polygon modules. |
| F07 | Low | UI commands / metadata | Duplicated logic | Inspector metadata duplicates formatting and object-reference action helpers | `bobercad/app/ui/commands/inspector-editable-object-property-metadata.mjs:482-523`<br>`bobercad/app/ui/commands/inspector-property-metadata.mjs:857-865`<br>`bobercad/app/ui/commands/inspector-support-object-property-metadata.mjs:125-147` | Inspector panels can diverge in numeric formatting, object-reference actions, and missing-value behavior for the same kind of field. | Extract shared `inspector-formatting` and `object-ref-actions` helpers under `bobercad/app/ui/commands/`, then make editable/support/main metadata use the same surface. |
| F08 | Low | UI commands / metadata | Duplicated logic | Snap strength is defined in two places | `bobercad/app/ui/commands/snap-metadata.mjs:1-58`<br>`bobercad/app/rendering/interaction/snap-profiles.mjs:3` | Adding or renaming a snap strength requires synchronized edits in UI metadata and runtime profile resolution. | Put strength ids/specs in one runtime-safe shared module and let UI add labels/descriptions around the shared ids. |
| F09 | Medium | Viewer UI | Fallback / hidden default | Shortcut setting fallback logic is duplicated | `bobercad/app/rendering/interaction/keyboard-shortcuts.mjs:77-79`<br>`bobercad/app/ui/viewer/panels/panel-elements.mjs:596-598` | Shortcut default handling can drift between runtime behavior and panel rendering, especially for intentionally false or missing settings. | Import the existing keyboard shortcut helper in `panel-elements.mjs`. |
| F10 | Medium | Smart Components | Duplicated logic | Project model/object access helpers are duplicated across API, store, and Smart Component runtime layers | `bobercad/app/engine/api/project/objects.mjs:47-59`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:58-68`<br>`bobercad/app/engine/modules/smart-components/smart-component-model-helpers.mjs:15-27` | Collection names, missing-object behavior, whether a missing collection is created, and error prefixes can drift between normal project API commands, store mutation helpers, and Smart Component generation. | Promote one public project object-access module that owns collection registration, objectIndex validation, collection lookup, and typed `byId` behavior; let store and Smart Component helpers wrap that only for mutation-specific behavior. |
| F11 | Medium | Rendering interaction | Compatibility layer | Datum/grid geometry has hidden fallback axes despite the schema requiring explicit grid axes | `bobercad/app/schemas/project.schema.json:598-609`<br>`bobercad/app/engine/api/project/datums.mjs:58-76`<br>`bobercad/app/rendering/scene/scene-datum-reference-assembly.mjs:40-52` | Invalid or old grid data can still render/snap with invented axes instead of failing validation, which conflicts with the explicit-datum source-of-truth rule. | Split strict datum evaluation from any legacy importer. Runtime rendering/snapping should require schema-valid axes; a migration/import path can convert old `rotation`-style grid data once. |
| F12 | Low | Connection primitives | API bypass | Fastener/hole diameter derivation is implemented in one primitive but bypassed in other connection primitives | `docs/architecture/data-model.md:206`<br>`bobercad/app/engine/api/model/connection-primitives/fasteners/secondary-web-bolting.mjs:12-27`<br>`bobercad/app/engine/api/model/smart-component-parameter-values.mjs:10-25`<br>`bobercad/app/engine/api/model/connection-primitives/plates/member-end-plate.mjs:84-96` | Different connection generators can disagree on hole diameter and tolerance behavior; adding a catalog tolerance requires checking multiple independent implementations. | Extract a shared fastener/hole derivation primitive used by all through-bolting generators, then let configs expose tolerance/custom diameter instead of raw diameter where appropriate. |
| F13 | Low | UI design / CSS | Compatibility layer | Left-dock data panels share DOM primitives but still duplicate panel lifecycle/search/focus scaffolding | `bobercad/app/ui/viewer/project-data-panel.mjs:57-113`<br>`bobercad/app/ui/viewer/project-files-panel.mjs:63-121`<br>`bobercad/app/ui/viewer/project-properties-panel.mjs:63-92`<br>`bobercad/app/ui/viewer/model-browser.mjs:84-127` | Search focus behavior, dataset naming, and empty-state behavior can diverge between panels even though the user-facing interaction is the same. | Introduce a small data-panel controller/helper for query state, search rendering, focus restoration, and row lookup. Keep each panel responsible only for its domain rows/sections. |
| F14 | Medium | Rendering interaction | Compatibility layer | Snap solver contains an active compatibility layer for candidate shape | `bobercad/app/engine/api/interaction/snap-solver.mjs:106-142` | Snap providers can keep emitting mixed old/new candidate shapes indefinitely, so candidate contract drift is hidden in the solver instead of failing at provider boundaries. | Define one canonical snap-candidate DTO and either migrate all providers to emit it or move this normalizer to an explicitly named one-time/provider-adapter layer with diagnostics for legacy flat fields. |
| F15 | Low | Connection primitives | Compatibility layer | Fin-plate bolting diagnostics still reference the old/direct hole diameter parameter path | `bobercad/app/engine/api/model/connection-primitives/fasteners/secondary-web-bolting.mjs:156-161` | UI diagnostics and auto-resolve hints can point users to a parameter that this generator no longer reads, while other primitives still use direct `holes.diameter`. This blurs the intended standard from F12. | Replace `holes.diameter` diagnostic paths with `holes.tolerance` and `holes.customDiameter` where relevant, and finish standardizing all connection primitives on the same hole-diameter derivation contract. |
| F16 | Low | Rendering interaction | Standardization | Secondary-web connection primitives can use either injected cached context or recomputed context | `bobercad/app/engine/api/model/connection-primitives/shared/secondary-web-context.mjs:19-24`<br>`bobercad/app/engine/api/model/connection-primitives/plates/secondary-web-plate.mjs:101` | Later primitives can depend on a context snapshot produced by an earlier primitive, while the same helper also supports recomputation. That makes operation ordering and stale context harder to reason about. | Make connection context creation an explicit pipeline step or shared runtime input, then remove the implicit "use cached if present, else recompute" branch from primitive helpers. |
| F17 | Low | Connection primitives | Fallback / hidden default | Connection primitive generators use both raw `ctx.param` and default/diagnostic-aware `ctx.parameterValue` for required parameters | `bobercad/app/engine/api/model/connection-primitives/plates/base-plate.mjs:123-124`<br>`bobercad/app/engine/api/model/connection-primitives/plates/member-end-plate.mjs:146-149`<br>`bobercad/app/engine/api/model/connection-primitives/stiffeners/support-web-stiffeners.mjs:73-75` | Some generator parameters can bypass definition defaults and missing-parameter diagnostics while others do not, depending on which primitive authored them. | Standardize generator parameter access: reserve `ctx.param` for explicitly raw reads, otherwise migrate required primitive parameters to `ctx.parameterValue`/`ctx.params` with clear required/default behavior. |
| F18 | Medium | Smart Components | Duplicated logic | Smart Component semantic builder has public methods and normalization rules outside the API register/store contract | `bobercad/app/engine/api/model/semantic-builders.mjs:185-578`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:106-112`<br>`bobercad/data/libraries/smart-components/components/frames/portal-frame/build.mjs:17-50`<br>`bobercad/data/libraries/smart-components/components/stairs/landings/shared-landing.mjs:99-120` | The actual public Smart Component API is wider than the register says, and generated trim objects can evolve under semantic-builder rules that are parallel to, rather than inherited from, the store trim-normalization... | Make `api-register.json` enumerate the real semantic builder surface, split read access from creation namespaces where possible, and route trim builder operations through the same normalization/validation helper used by project-store trim... |
| F19 | Medium | Smart Components | Fallback / hidden default | Smart Component parameter values are resolved differently in UI and runtime, so derived values are reimplemented instead of inherited | `bobercad/app/engine/api/model/smart-component-parameter-values.mjs:54-65`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:148-159`<br>`bobercad/data/libraries/smart-components/components/connections/fin-plate/config.json:365-381`<br>`bobercad/app/engine/api/model/connection-primitives/fasteners/secondary-web-bolting.mjs:156-161` | The UI can display/edit a derived parameter value that runtime builders cannot read through the same `ctx.parameterValue` contract, forcing each generator to reimplement derivations. Invalid derived-parameter config... | Promote one Smart Component parameter resolver/derive schema used by UI, runtime build context, validation, and generator APIs. Derived display-only parameters should be explicitly marked as UI-only; generator-consumed derived values... |
| F20 | Medium | Viewer UI | Compatibility layer | Smart Component diagnostics still emit and display a legacy `parameters` alias while new APIs reject `parameters` | `bobercad/app/engine/api/model/solver-result.mjs:57`<br>`bobercad/app/engine/api/model/checks.mjs:58-62`<br>`bobercad/app/engine/api/model/compliance.mjs:86-98`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:121-137` | Callers see a mixed contract: authoring APIs tell them `parameters` is unsupported, but generated diagnostics still contain and display it. That keeps the old field live and makes future diagnostic schema cleanup risky. | Migrate UI display to `parameterPaths`, add a temporary read adapter only if old project files need it, and stop emitting `parameters` from new runtime diagnostics. |
| F21 | Low | Smart Components | Contract drift | Transport sectioning exposes undocumented `ctx.*` methods and downgrades named strategies to count-based object batching | `bobercad/app/engine/api/model/transport-sectioning.mjs:151-233`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:221-240`<br>`bobercad/data/libraries/smart-components/components/stairs/sections/transport-sections/build.mjs:108-113` | A user-facing strategy name such as `manual-stations` implies geometric/station-based splitting, but the runtime currently partitions object ids by count. That can produce believable transport section records while... | Register the `ctx.transport*` APIs explicitly, then either implement strategy-specific sectioning using split frames/stations or rename the current behavior as count-based grouping and keep station metadata out of the strategy contract. |
| F22 | Medium | Rendering interaction | Compatibility layer | Axis relation project API directly parses rendering snap DTOs and carries grid-line defaults/aliases | `bobercad/app/engine/api/project/axis-relations.mjs:82-130`<br>`bobercad/app/rendering/interaction/member-transform-edit-controller.mjs:650-689`<br>`bobercad/app/engine/store/project-store-smart-component-helpers.mjs:108-113` | A project/model API now depends on the current rendering snap candidate shape, including legacy/alternate source id naming and implicit grid defaults. Snap DTO migrations or multi-level grids can change persisted... | Move snap-to-axis-source conversion into the interaction layer or a named adapter, then make project relations accept a canonical `source` object with explicit grid system, level, axis group, and axis id requirements. |
| F23 | High | Rendering interaction | API bypass | Snap selection bypasses the central Smart Component ownership/dependency API | `bobercad/app/engine/api/project/dependencies.mjs:170-198`<br>`bobercad/app/rendering/scene/scene-object-visibility.mjs:1-8`<br>`bobercad/app/rendering/interaction/snap-selection-manager.mjs:55-89` | Snap filtering can disagree with scene visibility, inspector highlighting, store dependency APIs, and detached/zone-managed Smart Component objects. Users may be unable to snap to objects that the rest of the app... | Replace the local snap-selection traversal with `smartComponentObjectIds(project, instance)` from `dependencies.mjs`, plus a small wrapper that resolves `smartComponentId` to the instance. |
| F24 | Low | Rendering interaction | Fallback / hidden default | Member creation has a parallel `modelDefaults.collections.members` resolver instead of using the canonical effective-object merge rule | `bobercad/app/engine/core/model.mjs:97-111`<br>`bobercad/app/engine/api/project/member-factory.mjs:25-55` | Newly created members and existing members read through `objectById`/`collectionObjects` can apply different default semantics. Defaults under `members["*"]` other than `material`, nested defaults, and central... | Extract a shared member-default/effective-new-object helper or make `createMemberObject` compose the same default merge contract before applying creation-only fields such as id, mark, endpoints, and authoring metadata. |
| F25 | Low | Rendering interaction | Fallback / hidden default | Member read APIs mix raw stored members and effective/default-merged members behind similar names | `bobercad/app/engine/core/model.mjs:103-118`<br>`bobercad/app/engine/api/project/members.mjs:51-53`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:90-94`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:87-94` | Two call sites can ask for a "member" and receive different shapes depending on whether they imported `api/project/members.memberById`, `project-store-model-helpers.memberById`, or public `store.member`. This can hide... | Name the distinction explicitly, for example `storedMemberById` versus `effectiveMemberById`, and route public reads through one effective-object API. Mutation helpers can still request stored objects, but their names should make that... |
| F26 | Medium | Smart Components | Duplicated logic | Object/index collection helpers are duplicated with different missing-collection and cloning behavior | `bobercad/app/engine/api/project/objects.mjs:1-19`<br>`bobercad/app/engine/modules/smart-components/smart-component-model-helpers.mjs:10-13`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:58-67` | Project API, store mutations, and Smart Component patch/runtime code can disagree about whether a missing collection is invalid or should be synthesized, whether inserted objects are cloned, and which collections are... | Export the collection registry and strict/patch-mode collection helpers from one project object module. Smart Component patch code can opt into a clearly named "create missing patch collection" mode, but it should inherit the same... |
| F27 | High | Engine project API | Fallback / hidden default | Plate-sketch normalization bypasses effective `modelDefaults` and reintroduces required/defaulted plate fields | `docs/quality/code-removal-log.md:4063-4074`<br>`bobercad/app/engine/api/project/plate-sketch/model-and-placement.mjs:17-25` | A schema-valid plate that relies on plate defaults can fail plate-sketch edits/normalization, and new plates/profiles can ignore a project's chosen material default. This mirrors the member default split from F24/F25... | Make plate-sketch operations normalize against an effective plate view or a shared default resolver, then write only intentional object overrides. Replace hardcoded `S355` fallbacks with the project/material default path or require... |
| F28 | Low | Engine project API | Standardization | Plate-sketch relation behavior is repeated across assert, diagnostic, rank-analysis, solver, and mutation code instead of coming from one relation operation registry | `bobercad/app/engine/api/project/plate-sketch/relation-analysis.mjs:82-178`<br>`bobercad/app/engine/api/project/plate-sketch/solver-core.mjs:126-140`<br>`bobercad/app/engine/api/project/plate-sketch/relation-mutations.mjs:72-110` | Adding or changing a relation type requires editing several switch/if chains across analysis, diagnostics, rank math, solver behavior, and mutation targeting. It is easy for satisfaction checks, UI health, solver... | Introduce one relation-operation registry/spec per relation type that owns target ids, satisfaction residual, diagnostic message, rank equations, solver target choice, and optional solve projection. Then make analysis, solver, preview,... |
| F29 | Low | Viewer UI | API bypass | Plate-sketch dimension APIs can silently downgrade requested driving dimensions to driven dimensions | `bobercad/app/engine/api/project/plate-sketch/relation-mutations.mjs:345-367`<br>`bobercad/app/engine/store/project-store-plate-sketch-methods.mjs:39-60`<br>`bobercad/app/ui/viewer/panels/contributions/plate-sketch-inspector.mjs:131-152` | A caller can ask the public API to make a dimension driving, receive a successful project update, and later find that the stored relation is driven/reference. This hides a constraint-policy decision inside mutation... | Make redundancy downgrades explicit in the API result or move the policy into the relation-operation registry proposed in F28. If automatic downgrade is intended, return metadata/diagnostics saying the requested driving dimension was... |
| F30 | Low | UI commands / metadata | Fallback / hidden default | Plate-sketch relation inference is both implicit and explicit, with no single policy/provenance layer | `bobercad/app/engine/api/project/plate-sketch/sketch-geometry-and-relations.mjs:125-140`<br>`bobercad/app/engine/api/project/plate-sketch/model-and-placement.mjs:81-90`<br>`bobercad/app/engine/api/model/semantic-builders.mjs:126-135`<br>`bobercad/app/engine/api/project/plate-sketch/relation-mutations.mjs:204-227` | The project can persist relations that were inferred automatically, inferred by topology repair, or explicitly requested by the user, but relation records do not expose that provenance. Later... | Centralize sketch-relation inference policy. Creation/topology APIs should take a documented `inferRelations` policy, and inferred relation records should either carry provenance metadata or be produced only through a named inference... |
| F31 | High | Engine store | Fallback / hidden default | Direct plate-sketch vertex moves silently remove touching equal-length relations | `bobercad/app/engine/api/project/plate-sketch/solver-core.mjs:563-569`<br>`bobercad/app/engine/api/project/plate-sketch/relation-mutations.mjs:313-342`<br>`bobercad/app/engine/store/project-store-plate-sketch-methods.mjs:27-32` | A user or API caller can move a point and lose equal-length constraints as a side effect, without a result payload that identifies which relations were removed. Other relation types are solved or asserted instead, so... | Move relation relaxation into the relation-operation policy layer from F28 and make mutation APIs return or emit metadata for removed/relaxed constraints. If silent removal is intentional for drag editing, it should be an explicit... |
| F32 | Low | Engine store | Contract drift | Plate-sketch topology edits silently prune dependent bends and can change plate type | `bobercad/app/engine/api/project/plate-sketch/topology.mjs:111-124`<br>`bobercad/app/engine/store/project-store-plate-sketch-methods.mjs:63-84` | Editing plate topology can delete bend chains and change the object's semantic type as a side effect without caller-visible dependency metadata. UI/automation can report only a successful sketch operation while losing... | Return mutation metadata for pruned bend ids and type changes, or route topology edits through a shared dependency/mutation-result contract. If automatic bend pruning is required, make it explicit in store/API result payloads and UI... |
| F33 | Low | Rendering interaction | Compatibility layer | Profile catalog APIs keep two live catalog shapes: raw profile map and wrapped `{ profiles }` library pack | `bobercad/app/engine/api/project/profiles.mjs:12-15`<br>`bobercad/data/libraries/profiles/profile-libraries/starter-profiles/config.json:28`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:288-290`<br>`bobercad/app/rendering/interaction/member-create-controller.mjs:112-120` | Callers cannot tell from the function names whether they should pass a profile library pack, a raw map, or a project-merged map. This preserves a compatibility layer in the core profile API and makes it easier for... | Split the shapes explicitly, for example `profileMapFromLibraryPack(pack)` and `projectProfileCatalog(project, libraryProfileMap)`, then make public lookup functions accept only the merged raw map. Keep any wrapped-pack adapter at... |
| F34 | Medium | UI commands / metadata | Duplicated logic | Trim operation type metadata is duplicated across engine, UI commands, and rendering icons | `bobercad/app/engine/api/project/trim-operations.mjs:1`<br>`bobercad/app/ui/commands/trim-operation-metadata.mjs:1-8`<br>`bobercad/app/rendering/trim-operation-icons.mjs:3-10` | Adding/renaming a trim operation requires synchronized edits in three places. Engine validation can accept a type that UI cannot label/render, or UI/rendering can expose a type the engine rejects. | Create one trim operation spec registry with ids and engine semantics, then layer UI labels/icons/rendering-only metadata on top of those ids instead of redefining the list. |
| F35 | Medium | Viewer UI | Compatibility layer | Trim operation member references keep scalar and array forms live at the same time | `bobercad/app/engine/api/project/trim-operations.mjs:41-50`<br>`bobercad/app/schemas/project.schema.json:2162-2188`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:569-578`<br>`bobercad/app/ui/viewer/panels/trim-joint-editor-panel.mjs:933-939` | `memberAId` can drift from `memberAIds[0]`, and callers must know which operations use scalar-only versus array+scalar. This makes profile-cope a compatibility branch inside every trim operation traversal, validation,... | Choose one canonical shape per trim operation kind. For profile-cope, make arrays canonical and derive scalar first ids only in adapters that still need them, or migrate schema/store/UI to a tagged operation spec where scalar and... |
| F36 | Medium | Rendering interaction | Fallback / hidden default | Work-plane API uses an implicit global-XY fallback as active geometry state | `bobercad/app/engine/api/project/work-plane.mjs:9-20` | Geometry creation/snapping can proceed on an invented plane that is not stored in `model.referencePlanes`, while explicit reference planes are validated strictly. This is useful UX defaulting, but it keeps a hidden... | Treat global XY as a named default work-plane policy in viewer/editor state or project settings, and make callers pass an explicit default-plane request. Keep strict project reference-plane resolution separate from the UI fallback. |
| F37 | Medium | UI commands / metadata | API bypass | Number formatting is reimplemented across core, rendering, and UI instead of using one formatting contract | `bobercad/app/engine/core/format.mjs:3-16`<br>`bobercad/app/rendering/annotations/drafting-dimensions.mjs:10-11`<br>`bobercad/app/ui/viewer/panels/member-transform-panel.mjs:12-23`<br>`bobercad/app/ui/viewer/panels/panel-elements.mjs:24-30` | Numeric rounding, invalid display, and trailing-zero rules can differ between overlays, forms, inspector rows, Smart Component parameters, and core labels. Users can see different text for the same value depending on... | Promote `formatNumber` as the shared primitive and add small named presets for control values, drafting dimensions, inspector values, and parameter labels. Local formatters should import those presets instead of reimplementing rounding. |
| F38 | Low | Rendering scene | Standardization | Feature cutter routing is split between the generic cut-body API and scene-only hole/slot cutters | `bobercad/app/engine/geometry/cut-features.mjs:8`<br>`bobercad/app/rendering/scene/scene-feature-cutters.mjs:61-127` | A caller using `cutBodiesForFeature` as the generic feature-to-cutter API gets no bodies for holes or slots, while rendering knows a separate route. Adding a new feature type requires knowing whether it belongs in the... | Centralize feature-to-cutter routing in one registry/API, or make `cutBodiesForFeature` return an explicit "handled by scene feature cutter" result instead of an indistinguishable empty list for active cut features. |
| F39 | High | Smart Components | Compatibility layer | Fastener catalog access has both strict wrapped and loose raw-map shapes | `bobercad/app/schemas/fastener-library.schema.json:7-14`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:70-72`<br>`bobercad/app/engine/api/model/connection-primitives/fasteners/secondary-web-bolting.mjs:5-9`<br>`bobercad/app/engine/geometry/evaluators/fastener-evaluator.mjs:39-44` | Fastener catalog consumers do not share one contract. A raw-map shape can work in rendering while failing store/Smart Component validation, making catalog bugs environment-dependent. | Add one shared `fastenerCatalogEntries` helper in a runtime-safe catalog API module and make renderer/store/Smart Component code all use the same strict or explicitly versioned compatibility behavior. |
| F40 | Medium | Rendering scene | Fallback / hidden default | Feature reference basis resolution is duplicated between fastener rendering and feature cutters | `bobercad/app/engine/geometry/evaluators/fastener-evaluator.mjs:58-98`<br>`bobercad/app/rendering/scene/scene-feature-cutters.mjs:22-58` | Fastener axes and hole/slot cutter axes can drift if one resolver adds support for a reference shape, fallback, or validation rule and the other does not. This is a core geometry contract currently owned by two... | Extract a shared feature-reference basis resolver under `engine/geometry` and have fastener evaluation plus scene feature cutters call it with their own error prefix/metadata needs. |
| F41 | High | Rendering interaction | Compatibility layer | Trim operation evaluation still falls back from missing/unknown operation types instead of requiring the schema/API contract | `bobercad/app/schemas/project.schema.json:2141-2147`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:569-572`<br>`bobercad/app/engine/geometry/evaluators/trim-evaluator.mjs:430-434` | Invalid or legacy trim operation data can still render/evaluate as an end-butt trim or show a marker plane while schema/store would reject it. That masks data-contract bugs and makes viewer behavior differ from... | Remove the runtime default or move it into a named legacy importer/migration. Evaluators should call the canonical trim operation type validator and throw on missing/unsupported `operation.type`. |
| F42 | Medium | Connection primitives | Contract drift | Weld reference dispatch is a catch-all instead of a canonical reference-kind contract | `bobercad/app/schemas/project.schema.json:2643-2648`<br>`bobercad/app/engine/api/model/semantic-builders.mjs:494-509`<br>`bobercad/app/engine/api/model/connection-primitives/welds/support-edge-fillet.mjs:32-52`<br>`bobercad/app/engine/geometry/evaluators/weld-evaluator.mjs:274-276` | A typo or new weld reference kind with `memberId` can render as a member-profile weld instead of failing, while unsupported reference kinds without `memberId` fail with a generic member-profile error. The actual weld... | Define a shared weld reference-kind registry/validator. `evaluateWeld`, semantic builders, schema, and store updates should all reject unknown kinds or route them through explicit adapters. |
| F43 | Medium | Connection primitives | Fallback / hidden default | Local frame/up-axis fallback policy is implemented independently in member, path, and connection geometry | `bobercad/app/engine/geometry/member-evaluator.mjs:123-134`<br>`bobercad/app/engine/api/geometry/paths.mjs:61-68` | Member frames, generic path frames, fin-plate layout references, and gusset plate axes can choose different local axes around vertical/parallel cases. Near-degenerate geometry may flip or rotate depending on which... | Promote a shared frame/basis resolver with named policies (`global-up`, `preferred-up`, `interface-z`, etc.) and make member/path/connection code call it instead of embedding local fallback chains. |
| F44 | Low | Smart Components | Contract drift | Reference-plane API documentation still advertises `size` although schema/runtime require `extents` | `bobercad/app/engine/api/api-register.json:303-310`<br>`bobercad/app/engine/api/model/semantic-builders.mjs:144-147`<br>`bobercad/app/engine/geometry/reference-plane.mjs:49-55`<br>`bobercad/app/schemas/project.schema.json:1028-1037` | Smart Component authors following the API register can provide `size` and hit a runtime builder error, while generated API docs suggest both shapes are valid. | Update `api-register.json` to say `extents` only, and keep any historical `size` migration in a named importer rather than public API docs. |
| F45 | Low | Engine geometry | Fallback / hidden default | Trim region geometry silently substitutes a unit profile span when profile data is missing | `bobercad/app/engine/geometry/trim-region-geometry.mjs:9-12`<br>`bobercad/app/engine/geometry/evaluators/trim-evaluator.mjs:30-40` | Missing profile catalog data can produce tiny or arbitrary trim helper geometry instead of failing at the point where geometry needs the profile. That can hide catalog/profile wiring errors behind plausible but wrong... | Require a profile for trim geometry that depends on section size. If a UI marker really needs a fallback span, pass it explicitly as a named rendering fallback, not as the default behavior of `sectionMaxSpan`. |
| F46 | Low | Smart Components | Fallback / hidden default | Smart Component interface resolution applies a hardcoded geometry nudge outside the shared member-interface resolver | `bobercad/app/engine/modules/smart-components/smart-component-build.mjs:389-414`<br>`bobercad/app/engine/store/project-store-smart-component-helpers.mjs:182-188` | The same stored interface can resolve differently depending on whether the caller goes through Smart Component runtime or directly through `resolveInterfaceWithConnectionReference`. The arbitrary `10` unit offset also... | Move this connection-facing disambiguation into the shared member/interface geometry resolver as a named option or semantic side-selection rule, and replace the magic distance with an explicit tolerance/profile-scaled policy. |
| F47 | High | Smart Components | Duplicated logic | Smart Component source resolution is duplicated and ignores the stored `library`/`version` contract | `bobercad/app/engine/modules/smart-components/smart-component-catalog.mjs:16-42`<br>`bobercad/app/engine/modules/smart-components/smart-component-registry.mjs:38-43`<br>`bobercad/app/schemas/project.schema.json:2688-2724`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:259` | A stored instance that references a different library or stale component version will still resolve by current preset id. Catalog and registry error handling can also drift because they maintain separate validators and... | Add one shared Smart Component source resolver in `smart-component-catalog.mjs` that validates `library`, `id`, and an explicit version policy. Make registry/store/scripts call that helper, and use a separate preset-id API for transient... |
| F48 | Medium | Smart Components | Compatibility layer | Smart Component creation splits preset and definition resolution across the caller boundary | `bobercad/app/engine/modules/smart-components/smart-component-creation.mjs:321-323`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:256-260` | A caller can pass a definition that does not match the preset, and the API keeps the id-only `sourceComponent` compatibility path alive. Creation behavior is split between store and Smart Component runtime instead of... | Make `createProjectSmartComponentFromPreset` resolve the definition through the canonical catalog/source helper. Keep any test-only or preview-only definition override as an explicit, validated override that asserts `definition.type ===... |
| F49 | Medium | Rendering interaction | Compatibility layer | Smart Component connection object creation is implemented twice with separate defaults | `bobercad/app/engine/modules/smart-components/smart-component-creation.mjs:198-275`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:289-355` | Top-level and nested connection components can drift in generated object shape, naming, authoring metadata, and supported interface types. Fixes to zone/interface/assembly creation must be remembered in two places. | Extract a shared connection-object factory for Smart Component runtime that accepts a top-level or nested mode/options object, and make both creation paths use it. |
| F50 | Medium | Viewer UI | Fallback / hidden default | Smart Component instance input accessors exist, but many callers still read raw `inputs` shapes directly | `bobercad/app/engine/modules/smart-components/smart-component-instance-helpers.mjs:7-27`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:71-77`<br>`bobercad/app/engine/store/project-store-smart-component-helpers.mjs:130-134`<br>`bobercad/app/engine/modules/smart-components/smart-component-creation.mjs:414` | Required vs optional behavior, empty-string handling, and fallback member-id logic can diverge between runtime build, store mutations, preview filtering, and inspector/display code. | Promote this helper module into the canonical Smart Component input accessor surface, add any missing permissive/read-only helpers needed by UI preview, and migrate store/runtime callers off raw `inputs.*` reads where validation or null... |
| F51 | Medium | Smart Components | Duplicated logic | `setSmartComponentRoleActive` has a runtime implementation but the store reimplements it with different validation | `bobercad/app/engine/modules/smart-components/smart-component-options.mjs:103-109`<br>`bobercad/app/engine/store/project-command-store.mjs:50-57`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:394-400` | Runtime and store role toggles can disagree when a role exists in `definition.components` but not in `objectRoles`, or vice versa. Future changes to role suppression semantics must be made twice. | Make the store commit through the runtime `setSmartComponentRoleActive` path, or move the shared role validation/toggle function behind one helper used by both runtime and store wrappers. |
| F52 | Medium | Smart Components | Contract drift | Smart Component outputs use suffix-based validation instead of a declared output contract | `bobercad/app/engine/modules/smart-components/smart-component-build.mjs:182-190`<br>`bobercad/app/engine/modules/smart-components/smart-component-overrides.mjs:145-157`<br>`bobercad/app/schemas/project.schema.json:2771-2774` | Component outputs become an implicit naming convention rather than a typed API. Misspelled output names, non-id payloads, or changed shapes can be consumed by child components/UI without schema-level or... | Add a declared output contract to Smart Component definitions and validate `ctx.output` against it. Keep the `Id`/`Ids` suffix rule only as a temporary lint/backstop, not the primary contract. |
| F53 | Medium | Smart Components | Contract drift | Runtime Smart Component definition validation is weaker than the JSON schema contract | `bobercad/app/schemas/smart-component.schema.json:7-18`<br>`bobercad/app/engine/modules/smart-components/smart-component-parameters-and-definition.mjs:270-292` | Smart Component configs loaded as JS/runtime objects can contain typo fields or unsupported top-level contract drift that would fail schema validation but pass runtime loading. Schema and runtime can stop describing... | Make runtime definition loading validate against the same schema or mirror the schema's top-level known-key set, including `$schema` policy. If JS-only fields such as `build` are allowed, isolate them as runtime-only extensions with an... |
| F54 | Medium | Tooling / scripts | Fallback / hidden default | Smart Component preview context ids are only strictly validated in a contract script, while runtime silently ignores unknown ids | `bobercad/app/engine/modules/smart-components/smart-component-preview-contexts.mjs:20-24`<br>`bobercad/app/engine/modules/smart-components/smart-component-parameters-and-definition.mjs:245-259`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:140-153` | A typo in `preview.contexts` can silently fall back to unrelated default preview contexts at runtime, especially outside the contract-covered connection preset path. Preview availability can look valid while not... | Move known-context validation into runtime definition loading or share the preview-context registry with `defineSmartComponent`, and make unknown context ids fail at catalog load time. |
| F55 | Low | Smart Components | Duplicated logic | Smart Component catalog loading is not concurrency-safe | `bobercad/app/engine/modules/smart-components/smart-component-registry.mjs:5-7` | Two concurrent callers before the first load completes can both load the register and attempt to register the same definitions/presets, causing a duplicate-definition or duplicate-preset failure. The API looks... | Replace the bare boolean with a cached `loadPromise` that is assigned before async work starts; subsequent callers should await that promise. Keep `loaded` only as derived state if still useful. |
| F56 | Low | Smart Components | Standardization | Id sanitization is split between Smart Component `safeId` and the project object API `cleanId` | `bobercad/app/engine/modules/smart-components/smart-component-runtime-validation.mjs:78-82`<br>`bobercad/app/engine/api/project/objects.mjs:61-67` | Generated ids differ depending on whether an object is created through Smart Component runtime or the public project object API. Hyphenated component types such as `stair-system` become `stair_system` through one path... | Put id sanitization in one project id helper and make Smart Component generation call it with an explicit mode if it needs stricter ids. |
| F57 | Medium | Smart Components | Contract drift | Smart Component diagnostic display is implemented both in runtime overrides and the project store | `bobercad/app/engine/modules/smart-components/smart-component-overrides.mjs:16-20`<br>`bobercad/app/engine/store/project-command-store.mjs:148-152` | Error display styling and eligible object collections can drift between normal Smart Component build diagnostics and store-side reconciliation diagnostics. A future display contract change would need to be remembered... | Export one diagnostic display/apply helper from the Smart Component runtime layer and make the store call that helper when it has to mark reconciliation errors. |
| F58 | Medium | Smart Components | Fallback / hidden default | Smart Component store reconciliation performs hidden geometry repair after generation | `bobercad/app/engine/store/project-command-store.mjs:367-407` | Smart Component build output is not fully authoritative; endpoint geometry can be silently changed by a store-only post-process that is not part of the Smart Component definition, primitive API, or member/trim API.... | Move the beam-to-trim fitting into the relevant Smart Component/connection primitive or expose it as an explicit model operation/solver result. The store should commit the result, not own geometry repair logic. |
| F59 | Medium | UI commands / metadata | Fallback / hidden default | Regenerated Smart Component commands can under-report changed objects | `bobercad/app/engine/store/project-command-registry.mjs:28-40`<br>`bobercad/app/engine/store/project-command-store.mjs:422-428`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:299-310` | Subscribers or render/update paths that trust command result ids can miss objects actually changed by regeneration. The fallback diff code exists but is bypassed by the explicit, too-small metadata list. | Either let the command registry derive ids for regeneration commands, or have Smart Component patch/reconcile return the full touched-id set and pass that complete set through command metadata. |
| F60 | Medium | Engine store | Duplicated logic | Store interface validation is weaker than `project.schema.json` | `bobercad/app/engine/store/project-store-model-helpers.mjs:441-455`<br>`bobercad/app/schemas/project.schema.json:1207-1378` | Store update paths can accept interface objects that later fail schema validation or behave differently in geometry resolution. The store validator becomes a parallel, less strict interface contract. | Replace manual interface validation with schema-backed validation or move the schema's conditional interface contract into one shared model validator used by both store and JSON checks. |
| F61 | Low | Engine store | Fallback / hidden default | Store default-generation policies are hardcoded instead of centralized | `bobercad/app/engine/store/project-store-model-helpers.mjs:299-315`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:341-357` | UI/store-created model objects can get defaults that are not visible in schemas, model defaults, command metadata, or API registers. Scripts or future APIs that create the same object family may choose different... | Extract grid-axis and trim-operation default policies into named project/model API helpers with documented inputs. If these are UI convenience defaults, keep them out of generic store helpers and call them explicitly from UI-facing... |
| F62 | Medium | Smart Components | Fallback / hidden default | Smart Component deletion keeps a fallback removal algorithm that is weaker than the canonical helper | `bobercad/app/engine/store/project-store-smart-component-methods.mjs:367-383`<br>`bobercad/app/engine/store/project-command-store.mjs:262-270` | Alternate/test construction of `createSmartComponentStoreMethods` without the injected helper can leave connection helper objects behind. The fallback makes deletion semantics depend on dependency injection shape... | Remove the fallback and require `smartComponentRemovalObjectIds`, or export one canonical removal-id helper that both command store and Smart Component store methods must call. |
| F63 | Low | Rendering interaction | Fallback / hidden default | Datum/grid creation methods are misplaced inside the Smart Component store-method module | `bobercad/app/engine/store/project-store-smart-component-methods.mjs` | Project datum/grid authoring is hidden behind a Smart Component method bundle, making module ownership misleading and increasing the chance that future datum/grid defaults, validation, or UI commands get coupled to... | Move level/grid creation into a dedicated project datum/grid store module or into the central project command store next to other generic project object creation methods. |
| F64 | Medium | Engine store | Duplicated logic | Trim joint store methods validate operations but not the top-level trim-joint schema contract | `bobercad/app/engine/store/project-command-store.mjs:628-635`<br>`bobercad/app/engine/store/project-store-trim-methods.mjs:66-71`<br>`bobercad/app/schemas/project.schema.json:2465-2522` | Store commands can create or update trim joints that pass local operation-level checks but fail project schema validation. This is another parallel, weaker model contract. | Add a top-level `validateTrimJoint` helper that enforces schema-level required fields, operation count, participant count, and known keys, and call it from `replaceTrimJoint` and create/update/remove trim paths. |
| F65 | Low | Viewer dimensions | Fallback / hidden default | Dimension annotations have their own Smart Component role/interface/parameter resolution layer | `bobercad/app/rendering/annotations/dimension-geometry-and-label-context.mjs:36-53` | Dimension overlays can resolve a different role, interface, or parameter value than Smart Component runtime/build context if role values become arrays, interface order changes, connection zone ids move, or... | Expose a read-only Smart Component annotation context/helper from runtime that resolves role objects, role interfaces, and parameter values once, and make dimension annotations consume that instead of manually walking project/model shapes. |
| F66 | Low | Engine geometry | Standardization | Hole diameter annotation text contains an encoding artifact | `bobercad/app/rendering/annotations/dimension-geometry-and-label-context.mjs:70-73` | Hole diameter dimensions can display corrupted text instead of the intended diameter symbol/prefix. | Normalize annotation text encoding and use a tested UTF-8 diameter symbol or an ASCII-safe product-approved prefix. |
| F67 | Low | Rendering interaction | Fallback / hidden default | Hole pattern annotation falls back to hardcoded `bolts.rows`/`bolts.columns` parameter paths | `bobercad/app/rendering/annotations/dimensions/hole-pattern.mjs:11-16` | A dimension spec missing explicit row/column paths can still render and edit against bolt-specific paths, which couples a generic hole-pattern handler to one component parameter naming convention. It can hide... | Require `rowsParameter` and `columnsParameter` in dimension specs for editable pattern count annotations, or move the bolt-path default into a named component-level preset before the generic handler runs. |
| F68 | Medium | Engine geometry | Fallback / hidden default | Annotation perpendicular-axis helpers are duplicated between drafting and dimension context modules | `bobercad/app/rendering/annotations/drafting-dimensions.mjs:17-28`<br>`bobercad/app/rendering/annotations/dimension-geometry-and-label-context.mjs:162-173` | Tolerance behavior and fallback marker-axis selection can drift between generated dimension lines and higher-level dimension context calculations. | Import the exported drafting helpers into the dimension context, or move them into one annotation geometry helper used by both files. |
| F69 | Medium | Rendering interaction | Fallback / hidden default | Member-create interaction reimplements member profile/default fallback outside the creation API | `bobercad/app/rendering/interaction/member-create-controller.mjs:112-180`<br>`bobercad/app/engine/api/project/member-factory.mjs:52-55` | Viewer member creation can mask a missing required type default by supplying an existing-member or first-catalog profile, while non-viewer callers using the same public creation API fail. The UI also has a different... | Move the effective member-creation profile/default resolver into the project API/store layer, return explicit diagnostics when the project lacks required defaults, and let the interaction controller either call that resolver or pass only... |
| F70 | Low | Rendering interaction | Contract drift | Member transform commits are assembled in the interaction controller instead of one store transaction/API | `bobercad/app/rendering/interaction/member-transform-edit-controller.mjs:971-1004`<br>`bobercad/app/engine/store/project-command-store.mjs:829-862` | One user-visible transform can become multiple store commits with controller-owned ordering, relation side effects, regeneration timing, affected-object calculation, and local preview rollback. Undo/history,... | Add a store-level member transform operation/transaction that accepts the member operation plus relation intent and returns the committed project with changed/regenerated/removed ids. Reuse the same operation for live draft generation so... |
| F71 | Medium | Rendering interaction | Compatibility layer | Member-edit tuning settings are read from `authoringSettings` but exist only as code fallbacks | `bobercad/app/rendering/interaction/member-transform-edit-controller.mjs:211-219` | Performance, snapping, Smart Component rebuild delay, and relation conflict tolerance appear configurable from the code shape, but are effectively hidden constants. Operators cannot tune them through the authoritative... | Either add the intended fields to `viewer-settings.json` and `viewer-settings.schema.json`, or stop reading them as settings and move the constants into a named member-edit policy module with tests. |
| F72 | Medium | UI commands / metadata | Fallback / hidden default | Plate bend creation owns bend ids and default bend parameters in the UI controller instead of the project API | `bobercad/app/rendering/interaction/plate-bend-controller.mjs:6-25`<br>`bobercad/app/engine/api/project/plate-sketch/bend-normalization.mjs:6-35`<br>`bobercad/app/ui/commands/inspector-editable-object-property-metadata.mjs:288-290` | Any other caller that wants to create a bend must rediscover the UI controller's id/default policy or hand-author every field. Bend ids also use a third local sanitization rule, reinforcing the broader id-policy split... | Move target-to-bend-id and default bend construction into the project bend API, for example `createPlateBendForTarget(plate, target, options)`, and let the UI controller pass only target/direction/user overrides. |
| F73 | Low | Rendering interaction | Fallback / hidden default | Manual plate creation has UI-local defaults instead of inheriting project plate defaults | `bobercad/app/rendering/interaction/plate-create-controller.mjs:11-14` | A manually created plate ignores `modelDefaults.collections.plates` and material defaults by writing explicit S355/8 mm overrides from the UI path. Preview can also diverge from future store/default behavior because it... | Add a project API/store helper for manual plate creation and preview drafting that resolves project defaults once. The controller should pass points, work-plane/placement intent, and user overrides, not default material/thickness/id policy. |
| F74 | Medium | Viewer dimensions | Fallback / hidden default | Plate-sketch dimension overlay reads styling settings that are not in the viewer settings contract | `bobercad/app/rendering/interaction/plate-sketch/dimension-overlay.mjs:80-88` | Dimension overlay style appears configurable to the renderer but is not represented in the authoritative settings file/schema, so changes must be made in code and cannot be validated or customized consistently with... | Add these fields to the viewer settings schema/config if they are intended runtime settings, or move them into named rendering constants so they are not pseudo-settings. |
| F75 | Medium | Viewer UI | Fallback / hidden default | Plate-sketch dimension edit/toggle workflow is duplicated between the overlay drag actions and the inspector panel | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-dimensions.mjs:31-83`<br>`bobercad/app/ui/viewer/panels/contributions/plate-sketch-inspector.mjs:131-152` | Double-click overlay editing and inspector editing can diverge on numeric parsing, default rounding, selected-relation recovery after mutation, redundant relation messaging, and driving/reference behavior. The core API... | Extract a shared plate-sketch dimension action service for UI callers. It should own value parsing/default formatting, relation lookup after mutation, type-specific value/mode dispatch, and redundant/conflicted status mapping, while both... |
| F76 | Medium | Rendering interaction | Duplicated logic | Feature-body sketch editing is a parallel mutation adapter outside the normal plate-sketch store API | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-mutations.mjs:21-32`<br>`bobercad/app/engine/store/project-store-plate-sketch-methods.mjs:27-121`<br>`bobercad/app/engine/store/project-command-store.mjs:1000-1007`<br>`bobercad/app/rendering/interaction/plate-sketch/drag-edit-targets.mjs:79-93` | Plate sketch edits and feature-body sketch edits share data functions but not one public mutation/result contract. Insert/notch operations can return result metadata for normal plates and feature bodies through... | Add a project/store "sketch host" mutation API that accepts a plate object, sketch object, or feature-body target and returns one normalized mutation result. Rendering should call that host API instead of manually branching between plate... |
| F77 | Medium | Rendering interaction | Fallback / hidden default | Plate-sketch overlay styling and hit-target controls are pseudo-settings outside the viewer settings contract | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-overlays.mjs:37-39` | The overlay looks configurable from code, but the authoritative settings contract cannot provide or validate those fields. This creates hidden UI tuning constants and splits plate-sketch styling between real settings,... | Either add the intended overlay fields to `viewer-settings.json` and the schema, or move them into a named plate-sketch overlay style policy/constants module and stop reading them as runtime settings. |
| F78 | Low | Rendering interaction | API bypass | Plate-sketch snap logic creates persisted relation patches in the rendering layer | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-snap.mjs:54-66`<br>`bobercad/app/rendering/interaction/plate-sketch-drag-edit-controller.mjs:872-895` | Drag snapping can author constraints from rendering heuristics, outside the project relation inference policy tracked in F30 and outside the relation-operation registry proposed in F28. Relation provenance is still... | Move snap relation suggestion into a project/API relation policy layer or return explicit provenance metadata for snap-generated relations. Rendering can still request candidates, but the relation patch/spec should come from the same... |
| F79 | High | Rendering interaction | Compatibility layer | Plate-sketch edit thresholds are read as settings but are not in the settings contract | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-snap.mjs:84`<br>`bobercad/app/rendering/interaction/plate-sketch-drag-edit-controller.mjs:959` | Geometry/edit thresholds look configurable but cannot be set or validated through `viewer-settings.json`. If they are invariants, they should be named policy constants; if they are tunable, they belong in the schema. | Promote these thresholds to validated authoring settings or move them to a named plate-sketch edit policy module with tests around edge-collapse and insert-drag behavior. |
| F80 | Low | Rendering interaction | Contract drift | Editing a box feature-body sketch silently converts the feature body to polygonal-prism | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-targets.mjs:23-29` | A user can edit a rectangular box cutter through the sketch editor and lose the parametric `box` body shape without an explicit conversion result or warning. Downstream feature editors and schemas must then handle the... | Route feature-body sketch edits through a feature body API that returns conversion metadata, or require an explicit "convert box to polygonal prism" operation before arbitrary outline editing. If silent conversion is intended, surface it... |
| F81 | Medium | Rendering interaction | Duplicated logic | Plate-sketch relation action patch mapping is duplicated between the overlay helper and the controller | `bobercad/app/rendering/interaction/plate-sketch/drag-edit-geometry.mjs:161-185`<br>`bobercad/app/rendering/interaction/plate-sketch-drag-edit-controller.mjs:409-443`<br>`bobercad/app/rendering/interaction/plate-sketch/drag-edit-overlays.mjs:419-461` | Adding/changing a relation action requires keeping the overlay handle shape, preview patch builder, and controller commit patch builder in sync. A relation can preview correctly but commit differently, or vice versa. | Extract a shared relation action spec/service that builds preview and commit patches from one table and supplies prompt requirements for value-based relations. |
| F82 | Medium | Rendering interaction | Compatibility layer | Reference-plane editor keeps `size` and invented extents fallbacks despite the extents-only contract | `bobercad/app/rendering/interaction/reference-plane-edit-controller.mjs:19-30`<br>`bobercad/app/engine/geometry/reference-plane.mjs:49-50`<br>`bobercad/app/engine/api/model/semantic-builders.mjs:144-146` | Invalid or old reference-plane data can still be displayed and edited in the interaction overlay with invented extents instead of surfacing validation failure. Editing writes an `extents` patch but does not explicitly... | Remove runtime `size`/default-extents fallback from the editor once migrations are complete, or move it into an explicit one-time migration/import path. The editor should require contract-valid `extents`. |
| F83 | Medium | Rendering interaction | Fallback / hidden default | Pick-face identity uses multiple member/object aliases instead of one canonical target DTO | `bobercad/app/rendering/interaction/selection-controller.mjs:4-7`<br>`bobercad/app/rendering/interaction/snap-visibility-policy.mjs`<br>`bobercad/app/rendering/interaction/snap-candidate-providers.mjs`<br>`bobercad/app/rendering/scene/scene-member-geometry-adapters.mjs` | Selection, snapping, visibility, and scene metadata must all remember the same alias set. New face producers can choose the wrong field and still partly work in some consumers, making picker behavior dependent on... | Define one pick-face target DTO, for example `{ collection, objectId, subId, ownerId }`, migrate producers to emit it, and keep any alias migration in one adapter before selection/snap code sees it. |
| F84 | Low | Rendering interaction | Fallback / hidden default | Manual sketch creation hardcodes project object defaults in the rendering controller | `bobercad/app/rendering/interaction/sketch-create-controller.mjs:74-91`<br>`bobercad/app/engine/api/project/plate-sketch/model-and-placement.mjs:81-91` | Manual sketch objects can diverge from other sketch creation paths because UI owns semantic defaults that should be a shared project-level draft/factory policy. Display defaults, provenance, and id-prefix behavior must... | Add a project/API helper for manual sketch drafts or sketch creation defaults, then let the interaction controller submit only captured points/work-plane context. |
| F85 | Medium | Rendering interaction | Compatibility layer | Snap providers actively emit hybrid old/new candidate DTOs | `bobercad/app/rendering/interaction/snap-candidate-providers.mjs:113-124` | The canonical snap-candidate contract is not authoritative at provider boundaries; the solver compatibility layer from F14 remains required because current providers still depend on mixed shapes. | Make provider helper functions return one canonical DTO shape, or introduce a named provider-adapter step and prohibit direct flat candidate emission outside it. |
| F86 | Medium | Rendering interaction | Fallback / hidden default | Reference-plane snap provider invents axes/spans outside the strict reference-plane contract | `bobercad/app/rendering/interaction/snap-candidate-providers.mjs:582-594` | Invalid or partial reference-plane data can still produce snap axes, even though other reference-plane APIs reject missing extents or `size`-style data. This reinforces F82 from a different runtime path. | Route reference-plane snapping through the same strict reference-plane evaluator used by geometry/API validation, and keep any missing-extents migration outside normal snapping. |
| F87 | Low | Rendering interaction | API bypass | Snap provider registry is incomplete; construction providers are appended outside it | `bobercad/app/rendering/interaction/snap-candidate-providers.mjs:1293-1360` | Provider capability/budget metadata is not the full provider surface. New construction-guide behavior can bypass registry-level policy, diagnostics, and future provider-level configuration. | Register construction, active-reference, drag-guide, and composite providers in the same provider table or move the table out of this file if it is only documentation. |
| F88 | Low | Viewer UI | Fallback / hidden default | Snap tuning defaults are split across manager, provider, controller context, and settings | `bobercad/app/rendering/interaction/snap-manager.mjs:108-129`<br>`bobercad/app/rendering/interaction/snap-candidate-providers.mjs:552`<br>`bobercad/app/ui/viewer/viewer-settings.json:244` | Snap performance and visible guide size are tuned from multiple independent places, so changing a snapping profile can leave hardcoded provider/controller behavior unchanged. | Put snap provider budgets and world-span defaults into the snap profile/settings contract, then pass a normalized config to providers. |
| F89 | Low | Rendering interaction | Standardization | Snap diagnostics are built by two independent APIs | `bobercad/app/rendering/interaction/snap-diagnostics.mjs:1-13`<br>`bobercad/app/engine/api/interaction/snap-solver.mjs:176-203` | Diagnostics emitted by manager/scope/visibility paths and solver ranking paths can have different fields for the same conceptual event. QA code and debug UI must tolerate both shapes instead of one diagnostic DTO. | Move snap diagnostic DTO builders to one shared module, or export the solver diagnostic builder and use it from rendering-level filters. |
| F90 | Low | Viewer UI | Fallback / hidden default | Snap profile defaults have two sources of truth | `bobercad/app/rendering/interaction/snap-profiles.mjs:5-80`<br>`bobercad/app/ui/viewer/viewer-settings.json:169-224`<br>`bobercad/app/rendering/interaction/snap-profiles.mjs:130-146` | Viewer settings are documented as the source of truth, but important profile values such as point/axis/intersection bias, `showLabels`, and the `training` profile live only in runtime code. Tuning a profile can require... | Make viewer settings contain the complete snap profile contract, or move defaults to one shared JSON/config module consumed by runtime and UI metadata. |
| F91 | Low | UI commands / metadata | Fallback / hidden default | Snap scope defaults and labels have multiple partial sources | `bobercad/app/rendering/interaction/snap-selection-manager.mjs:3-18`<br>`bobercad/app/ui/viewer/viewer-settings.json:225-240`<br>`bobercad/app/schemas/viewer-settings.schema.json:286-332`<br>`bobercad/app/ui/commands/snap-metadata.mjs:36-52` | Adding or exposing a snap scope key requires coordinated edits across runtime defaults, JSON settings, schema, and UI metadata. Hidden keys can remain configurable in settings but unreachable from controls/commands. | Define one snap scope spec with defaults, schema keys, labels, and collection mapping, then generate or import it in runtime, schema validation, and UI command metadata. |
| F92 | Medium | Rendering interaction | Duplicated logic | Smart Component selection scope reimplements ownership traversal instead of using the project dependency API | `bobercad/app/rendering/interaction/snap-selection-manager.mjs:55-85`<br>`bobercad/app/engine/api/project/dependencies.mjs:175-199` | Current-smart-component selection can disagree with store/API ownership, detached object ids, connection-zone object ids, filtering, and cycle behavior handled by the public dependency helper. | Import or expose the project dependency/store helper to snap selection instead of maintaining a rendering-local Smart Component traversal. |
| F93 | Low | Rendering interaction | API bypass | Snap visibility policy hardcodes physical provider identity outside the provider registry | `bobercad/app/rendering/interaction/snap-visibility-policy.mjs:37-40`<br>`bobercad/app/rendering/interaction/snap-candidate-providers.mjs:1293-1360` | Adding a new physical provider or changing provider ids requires a second edit in visibility policy, otherwise occlusion penalties and solid-visibility filtering silently do not apply to that provider. | Put physical/visibility behavior in provider metadata or candidate visibility fields and let visibility policy read that metadata instead of duplicating provider ids. |
| F94 | Low | Rendering interaction | Compatibility layer | Work-plane creation helper lives under plate-sketch API | `bobercad/app/rendering/interaction/work-plane-controller.mjs:3`<br>`bobercad/app/engine/api/project/plate-sketch/sketch-geometry-and-relations.mjs:143-160`<br>`bobercad/app/engine/api/project/work-plane.mjs` | A generic work-plane operation depends on the plate-sketch barrel, so future work-plane consumers can accidentally import plate-sketch geometry APIs or duplicate the helper elsewhere. | Move `workPlaneFromThreePoints` into `engine/api/project/work-plane.mjs` and reexport it from the plate-sketch barrel only for backward compatibility if needed. |
| F95 | Medium | Viewer UI | Fallback / hidden default | Scene thumbnail renderer is a parallel mini-renderer with its own projection and styling | `bobercad/app/rendering/preview/scene-thumbnail-renderer.mjs:85-92` | Smart Component thumbnails can drift from actual viewer framing/color/camera behavior because they do not share viewer camera projection, scene color policy, or design tokens. | Either document this module as an intentionally approximate thumbnail renderer, or feed it shared camera/color settings from the viewer render pipeline. |
| F96 | Low | Rendering interaction | Fallback / hidden default | Member manipulator overlays duplicate coordinate-space defaults next to the shared member-axis helper | `bobercad/app/rendering/scene/authoring/member-manipulator-overlays.mjs:1-5`<br>`bobercad/app/rendering/scene/authoring/member-axis-space.mjs:5-18` | Manipulator handles can drift from the coordinate-space labels/normalization used by member overlays, transform editing, member creation, and QA. | Make `memberManipulatorHandles` consume fully normalized axis specs from `member-axis-space.mjs`, or import `normalizeCoordinateSpace`/`coordinateSpaceLabel` and remove local defaults. |
| F97 | Medium | Viewer UI | Fallback / hidden default | Authoring overlay fallback settings are scattered and sometimes differ from viewer settings | `bobercad/app/rendering/scene/authoring/member-overlays.mjs:44`<br>`bobercad/app/ui/viewer/viewer-settings.json:245` | Any caller that does not pass fully normalized viewer settings gets a different authoring overlay than the configured product defaults. New overlay settings can also become pseudo-settings when schema/settings are not... | Normalize authoring overlay settings once before rendering overlays, and make overlay modules read from that normalized object without local product-value fallbacks. |
| F98 | Medium | Rendering interaction | Compatibility layer | Snap overlay rendering ignores the candidate preview contract and reconstructs overlays from transitional fields | `bobercad/app/rendering/interaction/snap-candidate-providers.mjs:93-94`<br>`bobercad/app/rendering/interaction/snap-manager.mjs:71-76`<br>`bobercad/app/rendering/scene/authoring/snap-overlays.mjs:21-135` | Provider preview metadata, solver result preview, and overlay rendering can drift. New snap candidate shapes must keep legacy `sources` conventions alive to render authoring guides. | Make `snap.preview` the canonical overlay descriptor, and keep any legacy `sources` adapter in one bridge near snap result normalization. |
| F99 | Medium | UI commands / metadata | Fallback / hidden default | Bend relief effective-size rules are split between renderer, controller, inspector, and normalization | `bobercad/app/rendering/scene/plate-bend-geometry.mjs:10-17`<br>`bobercad/app/engine/api/project/plate-sketch/bend-normalization.mjs:53-70`<br>`bobercad/app/rendering/interaction/plate-bend-controller.mjs:72-80`<br>`bobercad/app/ui/commands/inspector-editable-object-property-metadata.mjs:289-290` | Rendering, creation defaults, and editable UI can disagree about the actual relief size for auto/manual/none/width/radius cases. | Add one `effectiveBendRelief`/`bendReliefSize` helper next to bend normalization and use it from renderer, bend controller, and inspector metadata. |
| F100 | Medium | Rendering interaction | Fallback / hidden default | Feature reference/basis evaluation is duplicated across renderer, fastener evaluator, and snap providers | `bobercad/app/rendering/scene/scene-feature-cutters.mjs:22-58`<br>`bobercad/app/engine/geometry/evaluators/fastener-evaluator.mjs:54-98`<br>`bobercad/app/rendering/interaction/snap-candidate-providers.mjs:877-892` | Cuts, fastener placement, and snap candidates can disagree about the same feature reference, especially around plate face offsets, `"plate-center"`, station reference interfaces, and local axis defaults. | Promote one shared `featureReferenceBasis(project, profiles, feature)` evaluator and make cutters, fastener evaluation, and snap candidate providers consume it or a deliberate preview adapter around it. |
| F101 | Low | Rendering scene | Fallback / hidden default | Render scene output carries the mutable project model into WebGL runtime paths | `bobercad/app/rendering/scene/scene-geometry-builder.mjs:57-80`<br>`bobercad/app/rendering/webgl/webgl-highlight-policy.mjs:7-8`<br>`bobercad/app/rendering/webgl/webgl-object-preview.mjs:123-125` | Rendering/WebGL code can observe or mutate project state through the scene object instead of the store/API path, so preview patches and highlight policies become hidden model access layers. | Return only render-scene metadata needed by WebGL, such as object collection lookup and preview member bases, or expose an explicit immutable scene model context instead of carrying the full project. |
| F102 | Low | Rendering interaction | Compatibility layer | Render mesh edge/tolerance policy is hardcoded in scene assembly | `bobercad/app/rendering/scene/scene-line-face-assembly.mjs:128-140`<br>`bobercad/app/rendering/annotations/dimension-geometry-and-label-context.mjs:426-446` | Visible crease edges, evaluated-edge snap refs, and annotation grouping can diverge when project tolerance, unit scale, or tessellation detail changes. | Introduce a named render geometry tolerance/crease policy, preferably derived from project/viewer settings or the CSG tolerance layer, and use it from scene assembly and annotation grouping instead of scattering numeric thresholds. |
| F103 | Low | Rendering scene | API bypass | Member feature CSG application is implemented as a renderer-private pipeline | `bobercad/app/rendering/scene/scene-member-geometry-adapters.mjs:209-260`<br>`bobercad/app/engine/geometry/member-evaluator.mjs` | Any future exporter, analyzer, thumbnail, or non-scene geometry path must either call rendering code or reimplement the feature dispatch, making member geometry semantics renderer-owned. | Move feature application into an engine geometry evaluator such as `evaluateMemberPolygons`/`applyMemberFeatures`, with scene adapters only converting evaluated polygons to faces/edges. |
| F104 | Low | UI commands / metadata | Fallback / hidden default | Boolean feature operation values are repeated across schema, UI, and scene adapters | `bobercad/app/schemas/project.schema.json:1822-1827`<br>`bobercad/app/ui/commands/inspector-editable-object-property-metadata.mjs:4-7`<br>`bobercad/app/rendering/scene/scene-object-geometry-adapters.mjs:142-147`<br>`bobercad/app/rendering/scene/scene-member-geometry-adapters.mjs:233-237` | Adding or renaming a boolean operation requires synchronized edits across schema, inspector UI, object rendering, and member rendering. Defaults can also drift because the UI assumes `BOOLEAN_CUT` when absent while... | Export one boolean feature operation registry/metadata object used by schema generation or schema-adjacent validation, inspector options, and render/evaluator operation dispatch. |
| F105 | Medium | Rendering scene | Contract drift | Plate solid feature application is renderer-private and bent plates have a separate unsupported path | `bobercad/app/rendering/scene/scene-object-geometry-adapters.mjs:72-89` | Flat plates and bent plates follow different geometry contracts, and non-render consumers cannot reuse an engine-level evaluated plate solid. Feature support can diverge between viewer, export, and future analysis paths. | Move plate solid evaluation, including flat/bent feature handling, into an engine geometry evaluator; keep the scene adapter responsible only for converting evaluated panels/polygons to faces and edges. |
| F106 | Medium | Rendering scene | Fallback / hidden default | Fastener visual assembly dimensions are derived only in the scene adapter | `bobercad/app/engine/geometry/evaluators/fastener-evaluator.mjs:153-179`<br>`bobercad/app/rendering/scene/scene-object-geometry-adapters.mjs:154-201` | Rendering can show bolt/nut/washer proportions that are not part of the fastener evaluator/catalog contract, so export, selection envelopes, thumbnails, and future fabrication checks may disagree about the same... | Extend the fastener evaluator with an evaluated assembly/visual envelope DTO, or create a shared fastener geometry policy consumed by renderer and any export/analysis path. |
| F107 | Medium | Viewer UI | Duplicated logic | Camera direction-to-angle conversion is duplicated in QA view code | `bobercad/app/rendering/webgl/webgl-view-state.mjs:7-12`<br>`bobercad/app/ui/viewer/viewer-qa-bridge.mjs:125-130` | QA screenshots/smoke views can drift from runtime viewer orientation math, so automated visual checks may pass/fail against a camera state that normal UI paths cannot reproduce. | Import `cameraAnglesForDirection`/orientation helpers from `webgl-view-state.mjs` into QA bridge, and keep any QA-specific view names as data that uses the shared conversion. |
| F108 | Medium | Viewer dimensions | Compatibility layer | Dimension label presentation is resolved separately in WebGL and DOM overlay paths | `bobercad/app/rendering/webgl/text-label-renderer.mjs:7-29`<br>`bobercad/app/ui/viewer/dimensions/dimension-overlay-ui.mjs:5` | Inactive WebGL labels and active/editable DOM labels can render with different fallback font/color/min-size behavior even though they represent the same dimension labels. | Extract a shared dimension label presentation resolver that returns text, font family/weight/size thresholds, hover color, and fallback color for both renderers. |
| F109 | Low | Rendering WebGL | Fallback / hidden default | WebGL object preview mutates the live render scene as a parallel scene patch store | `bobercad/app/rendering/webgl/webgl-object-preview.mjs:172-199` | The preview controller can diverge from `buildScene`/render scheduler semantics and must manually remember every scene-owned cache/collection to invalidate or patch. It also gives preview code a hidden model mutation... | Define an explicit scene patch API/DTO owned by the render scheduler or scene builder, and keep preview controllers from mutating arbitrary scene collections/project state directly. |
| F110 | Low | Rendering interaction | Compatibility layer | WebGL picker hit tolerances are hardcoded separately from snap/viewer policy | `bobercad/app/rendering/webgl/webgl-picker.mjs:3-18`<br>`bobercad/app/rendering/interaction/snap-visibility-policy.mjs` | Picking, orbit anchors, and snap visibility can disagree about what is close enough, especially between CPU/GPU pick paths and snap profile settings. | Move picker tolerances into a named WebGL picking policy or pass them from the existing snap/viewer settings context so picker, snap visibility, and interaction profiles share one contract. |
| F111 | Low | Rendering WebGL | Standardization | Instanced-member shader normal transform is malformed | `bobercad/app/rendering/webgl/webgl-programs.mjs:139-145` | Instanced member lighting is wrong and the shader may fail to compile on GLSL implementations that reject vector-plus-scalar arithmetic. | Change the normal transform to `normalize(aAxisX * aLocalNormal.x + aAxisY * aLocalNormal.y + aAxisZ * aLocalNormal.z)` and add a render smoke check for instanced member shading. |
| F112 | Medium | Rendering WebGL | Compatibility layer | WebGL shader/program creation is duplicated outside the program registry | `bobercad/app/rendering/webgl/webgl-programs.mjs:41-55`<br>`bobercad/app/rendering/webgl/text-label-renderer.mjs:49-77` | Shader error handling, lifecycle, and future WebGL compatibility fixes must be applied in multiple places. | Export a small shared WebGL program helper or let `webgl-programs.mjs` own text label program creation too. |
| F113 | High | Viewer UI | Contract drift | Dimension hide-behind-geometry setting is ignored by the WebGL orchestrator | `bobercad/app/rendering/webgl/webgl-render-orchestrator.mjs:98-100`<br>`bobercad/app/ui/viewer/viewer-settings.json:338`<br>`bobercad/app/schemas/viewer-settings.schema.json:678-679` | The schema/settings surface promises a dimension occlusion option that runtime rendering can never enable. | Resolve this through a named render-dimension policy, or at minimum return `settings.render.dimensions?.hideBehindGeometry === true` from the orchestrator helper. |
| F114 | Low | Rendering WebGL | Compatibility layer | `appendWorldVertex` carries a dual calling convention compatibility overload | `bobercad/app/rendering/webgl/webgl-render-orchestrator.mjs:225-239` | Static render cache construction can accept mismatched argument shapes without a clear failure, and future buffer writers must preserve both calling conventions. | Split this into explicit helpers, for example `appendGroupVertex(group, point, rgba, pickRgba)` and `appendArrayVertex(positions, colors, point, rgba)`, so render-group and raw-array writes have one contract each. |
| F115 | Low | Rendering WebGL | Contract drift | WebGL pointer controls hardcode trim-region click priority | `bobercad/app/rendering/webgl/webgl-viewer-controls.mjs:305-310` | Component-specific click behavior is embedded in low-level WebGL pointer controls, so other click entry points can drift and future component pick priorities require editing this controls layer. | Move click-priority policy into the picker/runtime interaction contract, for example as a supplied click-pick profile or click handler option, and keep `webgl-viewer-controls.mjs` responsible only for event sequencing. |
| F116 | Medium | Rendering WebGL | Compatibility layer | WebGL large-scene threshold is duplicated as an unnamed constant | `bobercad/app/rendering/webgl/webgl-highlight-policy.mjs:3-5`<br>`bobercad/app/rendering/webgl/webgl-viewer-runtime.mjs:242-244` | Highlight optimization and pick strategy can diverge when performance thresholds change, and the value is not visible through viewer settings or a named WebGL policy. | Move the threshold into one WebGL scene-performance policy, preferably with separate named fields if highlight and picking should eventually differ. |
| F117 | Medium | Rendering interaction | Fallback / hidden default | Authoring handle contracts are implicit across overlay generation, hit testing, and rendering | `bobercad/app/rendering/webgl/webgl-viewer-runtime.mjs:525-619`<br>`bobercad/app/rendering/webgl/webgl-render-orchestrator.mjs:109-176`<br>`bobercad/app/rendering/scene/authoring/member-manipulator-overlays.mjs:115-155` | A new handle shape must be manually understood by at least the overlay generator, WebGL hit-test path, and WebGL render path, with no schema or adapter enforcing the shared contract. | Add a small authoring-handle descriptor API that normalizes type/kind/default/tolerance fields, then make generators, hit testing, and rendering consume that normalized contract. |
| F118 | Low | UI commands / metadata | Fallback / hidden default | Ribbon section grouping is inferred from command-id substrings | `bobercad/app/ui/commands/command-group-metadata.mjs:80-104` | Renaming command ids or adding a command with an overlapping substring can silently move it to a different UI section; command grouping is not validated as command metadata. | Add explicit `ribbonSection` or `section` metadata to command specs, keep this inference only as a migration/default bridge, and validate section ids against `RIBBON_SECTION_ORDER`. |
| F119 | Low | UI commands / metadata | Standardization | Generated command action names depend on UI labels | `bobercad/app/ui/commands/command-registry.mjs:247-265` | Changing visible labels or punctuation can change the handler action string, so display copy edits can break command execution wiring. | Generate action names from stable ids or define explicit `action` fields in the source metadata specs, then treat labels as presentation-only. |
| F120 | Medium | UI commands / metadata | Fallback / hidden default | Data library descriptor default status is effectively unreachable with omitted config | `bobercad/app/ui/commands/data-surface-metadata.mjs:67` | UI/search metadata can report an undeclared library as declared, hiding the distinction between default/fallback library specs and configured library packs. | Default `config` to `null`, or compute declaration from actual config keys such as `libraryId`, `version`, or `path`. |
| F121 | Low | UI commands / metadata | Compatibility layer | Inspector section placement is inferred from ids, labels, and field shapes | `bobercad/app/ui/commands/inspector-property-metadata.mjs:75-83` | Renaming section ids/labels or changing one field type can move a section between inspector layout regions without any explicit metadata change. | Require explicit `placement` in section factories, or use named section-builder helpers that set placement while keeping this heuristic only as a temporary compatibility bridge. |
| F122 | Low | UI commands / metadata | Standardization | Active-tool guidance is keyed separately from command metadata | `bobercad/app/ui/commands/inspector-property-metadata.mjs:15-49`<br>`bobercad/app/ui/commands/command-registry.mjs:17-122` | Adding, renaming, or deleting commands requires synchronized edits in command registry and inspector metadata, and missing hints are silent. | Add optional guidance/hints to the command specs or a command metadata extension table validated against `commandPaletteSpecs()`. |
| F123 | Low | UI commands / metadata | Fallback / hidden default | Grid inspector falls back from missing grid level ids to every project level | `bobercad/app/ui/commands/inspector-support-object-property-metadata.mjs:219-226` | A grid system with no explicit `levelIds` appears linked to all project levels, and edits in the grid inspector can target global levels that were not actually attached to the grid. | Treat missing `levelIds` as empty/unlinked in the inspector, or make the “all project levels” behavior an explicit project/grid default resolved before UI metadata. |
| F124 | Medium | UI commands / metadata | Compatibility layer | Model search descriptors keep multiple semantic alias fields alive | `bobercad/app/ui/commands/model-collection-metadata.mjs:129-138` | Search metadata continues to recognize deprecated or non-canonical field names, including `componentRef`, even when the core model contract should have one canonical field shape. | Move semantic search extraction to a model metadata adapter that documents canonical vs legacy aliases, then remove aliases once project/schema migrations are complete. |
| F125 | Medium | UI commands / metadata | Duplicated logic | Display mode normalization is duplicated between UI metadata and WebGL view state | `bobercad/app/ui/commands/view-metadata.mjs:102-108`<br>`bobercad/app/rendering/webgl/webgl-view-state.mjs:3-5` | Adding or renaming a display mode can update command/UI metadata without updating WebGL runtime normalization, or vice versa. | Route WebGL display-mode normalization through the canonical view metadata contract, or move shared display/view normalization to a neutral module used by both UI commands and WebGL runtime. |
| F126 | Low | UI design / CSS | API bypass | Design-system disclosure sections own workspace persistence | `bobercad/app/ui/design-system/ui-elements.mjs:2` | A generic disclosure UI element now has shell/workspace storage side effects, so using it outside workspace-backed panels can accidentally participate in persisted workspace state. | Split this into a pure `disclosureSection` and a shell-level `workspaceDisclosureSection` wrapper that wires storage/reset behavior explicitly. |
| F127 | Medium | UI design / CSS | Fallback / hidden default | Command enabled/disabled semantics are duplicated across UI command surfaces | `bobercad/app/ui/shell/command-palette.mjs:413-418`<br>`bobercad/app/ui/shell/feature-navbar.mjs:279-284`<br>`bobercad/app/ui/shell/workspace-customizer-toolbar-dom.mjs:45-54`<br>`bobercad/app/ui/design-system/ui-elements.mjs:176-208` | A future command-state field or disabled-reason policy can update one surface without the others, producing inconsistent toolbar, palette, navbar, and status behavior. | Move command-state normalization into one shared command metadata/helper module and have all UI surfaces, including `applyCommandState`, consume the normalized contract. |
| F128 | Low | UI shell / workspace | API bypass | Command palette recents use a separate browser storage path outside workspace preferences | `bobercad/app/ui/shell/command-palette.mjs:4`<br>`bobercad/app/ui/shell/workspace-storage.mjs:7-23` | Recents are not covered by workspace schema versioning, import/export, reset, or migration behavior, so shell preference state has two persistence APIs with different lifecycle rules. | Either move recents into the workspace preference envelope or document them as intentionally device-local ephemeral state with their own reset/migration boundary. |
| F129 | High | UI shell / workspace | Fallback / hidden default | Dock tab reveal logic bypasses the panel reveal synchronization API | `bobercad/app/ui/shell/dock-tabs.mjs:28-37`<br>`bobercad/app/ui/shell/workspace-customizer-manager.mjs:943-999`<br>`bobercad/app/ui/shell/workspace-customizer-panel-dock.mjs:67-75` | Clicking a dock tab can reveal an auto-hidden dock without running the same offset/icon/label synchronization used by the manager, so panel chrome can drift when reveal behavior changes. | Expose a small shell-level `revealPanelDock(panel)` helper or callback from the workspace manager and have `dock-tabs` call it instead of writing panel datasets and button state directly. |
| F130 | Medium | UI shell / workspace | Fallback / hidden default | Inspector dock reimplements dock-tab behavior instead of sharing the dock-tab controller | `bobercad/app/ui/shell/dock-tabs.mjs:4-199`<br>`bobercad/app/ui/shell/inspector-dock.mjs:4-269` | Fixes to dock tab keyboard behavior, reveal/pin chrome placement, ARIA attributes, or panel cleanup can land in one implementation and miss the other. | Factor a shared dock-tab core that accepts availability and panel-mount hooks, then implement normal data docks and inspector docks through that core. |
| F131 | Medium | UI shell / workspace | Duplicated logic | Viewer overlay corner specs are duplicated across workspace modules | `bobercad/app/ui/shell/workspace-customizer-state.mjs:15-21`<br>`bobercad/app/ui/shell/workspace-customizer-commands.mjs:7-12`<br>`bobercad/app/ui/shell/workspace-customizer-dialog.mjs:9-14` | Adding, renaming, or reordering overlay corners can update workspace normalization without updating command-search commands or dialog controls, causing mismatched labels/short labels/options. | Import `VIEWER_OVERLAY_CORNER_SPECS` from `workspace-customizer-state.mjs` in commands/dialog modules instead of redefining it. |
| F132 | Medium | UI shell / workspace | Fallback / hidden default | Workspace customizer label fallback uses a local `titleCase` instead of the shared labels helper | `bobercad/app/ui/shell/workspace-customizer-labels.mjs:3-7`<br>`bobercad/app/ui/shell/workspace-customizer-dialog.mjs:16-20` | Unknown ids containing underscores or other separators can display differently in the workspace dialog than in other workspace command/label surfaces. | Import `titleCase` or a more specific label helper from `workspace-customizer-labels.mjs` and remove the dialog-local implementation. |
| F133 | Medium | UI shell / workspace | Fallback / hidden default | Toolbar dock default is duplicated instead of sourced from the workspace state normalizer | `bobercad/app/ui/shell/workspace-customizer-state.mjs:7`<br>`bobercad/app/ui/shell/workspace-customizer-manager.mjs:11`<br>`bobercad/app/ui/shell/workspace-customizer-dialog.mjs:8` | If toolbar docking is reintroduced or the default changes, state normalization, manager DOM state, and dialog defaults can diverge. | Export a canonical toolbar dock default or use `normalizeToolbarDock()` wherever a default dock value is needed. |
| F134 | Low | UI shell / workspace | CSS / selector issue | Workspace toolbar drag target is hardcoded to `#modeling-toolbar` | `bobercad/app/ui/shell/workspace-customizer-manager.mjs:16` | The manager appears reusable for a toolbar argument, but drag-reorder only works correctly for the element with id `modeling-toolbar`; another toolbar instance or renamed id silently breaks. | Scope drag target lookup to the supplied `toolbar` element, for example by checking `toolbar.contains(candidate)` or querying with a class selector relative to the manager instance. |
| F135 | Low | UI shell / workspace | API bypass | Workspace ordering repeats the same list-move algorithms per collection | `bobercad/app/ui/shell/workspace-customizer-ordering.mjs:3-21` | Boundary behavior and identity-preserving no-op returns can drift when one collection gets a reorder bug fix and the others do not. | Extract two small pure helpers, e.g. `moveIdByDirection(ids, id, direction)` and `moveIdBefore(ids, sourceId, targetId)`, then wrap them with the collection-specific normalizers. |
| F136 | Medium | UI shell / workspace | Duplicated logic | Workspace/id validators are duplicated across shell modules | `bobercad/app/ui/shell/workspace-customizer-state.mjs:520-522`<br>`bobercad/app/ui/shell/workspace-storage.mjs:131-132`<br>`bobercad/app/ui/shell/command-palette.mjs:399-400` | Workspace sections, collapsed groups/panel tab ids, and command-palette recents can drift if one validator changes. The current repeated regex also hides which id grammar is canonical for shell state. | Export a shared shell id validator, with named wrappers only where a clearer error/domain label is useful. |
| F137 | Medium | UI design / CSS | CSS / selector issue | CSS selector escaping fallback is duplicated and inconsistent | `bobercad/app/ui/shell/workspace-customizer-toolbar-dom.mjs:125-128`<br>`bobercad/app/ui/shell/feature-navbar.mjs:312-314` | Feature navbar and workspace toolbar selectors can handle uncommon ids differently when `CSS.escape` is unavailable, especially ids containing backslashes or other selector-sensitive characters. | Put selector escaping in one shell/design-system helper and use it from both modules. |
| F138 | High | UI shell / workspace | Compatibility layer | Workspace import uses a partial manual shape check instead of the UI workspace schema contract | `bobercad/app/ui/shell/workspace-storage.mjs:56-62`<br>`bobercad/app/schemas/ui-workspace.schema.json:7-19` | A user-imported workspace can carry unknown top-level or nested keys and still enter runtime storage as long as the small manual predicate passes, while committed/default workspaces are validated against the stricter... | Use the same schema validator for workspace import, or make the manual import predicate mirror the schema completely and reject additional properties before storage. |
| F139 | High | Viewer dimensions | API bypass | Dimension overlay rendering bypasses the Smart Component API accessor | `bobercad/app/ui/viewer/dimensions/dimension-edit-controller.mjs:137-154`<br>`bobercad/app/ui/viewer/smart-component-parameter-ui.mjs:845` | Dimension rendering can use a different missing-instance/status/access path than Smart Component editing and panels, so future validation, lazy loading, or accessor behavior can be bypassed by the overlay. | Render dimensions through `api.smartComponent(smartComponentId)` or add an explicit nullable Smart Component accessor instead of reaching into `project().model`. |
| F140 | Medium | UI design / CSS | Duplicated logic | Dimension overlay reimplements tooltip/control primitives outside the shared UI helpers | `bobercad/app/ui/viewer/dimensions/dimension-overlay-ui.mjs:22-98`<br>`bobercad/app/ui/design-system/ui-elements.mjs:48-156`<br>`bobercad/app/ui/viewer/panels/panel-elements.mjs:108-139` | Overlay menus, buttons, tooltips, invalid states, and keyboard behavior can diverge from the rest of the app. Because this is a canvas overlay, it may need special positioning, but the behavior contracts should still... | Extract overlay-compatible tooltip/control helpers or adapt the design-system primitives so dimension overlay can share invalid-state, button, tooltip, and keyboard behavior. |
| F141 | Low | Viewer UI | Duplicated logic | Plate sketch inspector duplicates public sketch geometry helper APIs | `bobercad/app/ui/viewer/panels/contributions/plate-sketch-inspector.mjs:911-920`<br>`bobercad/app/engine/api/project/plate-sketch-relations-and-bends.mjs:27-39`<br>`bobercad/app/engine/api/project/plate-sketch/sketch-geometry-and-relations.mjs:465-478` | Inspector-only sketch point/edge lookup can drift from the API helper behavior, including validation and error handling for missing edges/vertices. | Import `sketchVertexPointMap` and `sketchEdgePoints` from `plate-sketch-relations-and-bends.mjs` and remove the local copies; if UI needs tolerant lookup, expose a named tolerant helper instead. |
| F142 | Medium | Viewer UI | Fallback / hidden default | Create-plate-from-sketch UI action hardcodes plate defaults instead of delegating to the model/default API | `bobercad/app/ui/viewer/panels/contributions/plate-sketch-inspector.mjs:52-58`<br>`bobercad/app/engine/api/project/plate-sketch/model-and-placement.mjs:94-113` | Plate thickness for this authoring path is hidden in a UI contribution rather than coming from `modelDefaults`, a plate-authoring default, or the create-plate API contract. Changing the default requires finding this... | Move the default thickness/id policy into the create-plate-from-sketch API or a shared authoring defaults module, then let the panel call the API with only the selected sketch id unless the user supplied overrides. |
| F143 | Low | Viewer UI | Compatibility layer | Smart Component quick-parameter wrapper keeps a local catalog-options compatibility adapter | `bobercad/app/ui/viewer/panels/contributions/smart-component-properties.mjs:27-29`<br>`bobercad/app/engine/api/model/smart-component-parameter-values.mjs:103-108`<br>`bobercad/app/ui/viewer/smart-component-parameter-ui.mjs:295-300` | Inspector quick fields can drift from the full Smart Component parameter UI if catalog option behavior, current-value injection, or quick-field limits change in the canonical descriptor path. | Use one spec-based catalog option helper for inspector and full Smart Component parameter UI, and let `uiQuickParameterEntries()` own the default limit unless a caller is intentionally overriding it. |
| F144 | Low | UI commands / metadata | Duplicated logic | Feature editing has two parallel descriptor/commit stacks for the same store APIs | `bobercad/app/ui/viewer/panels/feature-editor-panel.mjs:34-60`<br>`bobercad/app/ui/viewer/panels/inspector-property-bindings.mjs:162-170`<br>`bobercad/app/ui/commands/inspector-editable-object-property-metadata.mjs:57-147` | Feature body center/size/radius/depth/boolean enabled behavior can diverge between the object inspector and the feature editor. Any validation, min/max options, vector handling, affected-object calculation, or... | Define one feature-edit descriptor/action contract and let the compact object inspector and expanded feature editor select different subsets/views of that same contract, or route both namespaces through one adapter with shared patch... |
| F145 | Low | Viewer UI | Contract drift | Generated property binding silently no-ops on unknown commit/action/command ids | `bobercad/app/ui/viewer/panels/generated-property-bindings.mjs:28-38`<br>`bobercad/app/ui/viewer/viewer-app-controller.mjs:42-45` | A typo or stale action id in generated property metadata can render an apparently editable field/button that does nothing, hiding broken API wiring until manual UI testing. | Add a descriptor-binding validation mode or fail/log loudly for unknown commit/action/command ids at bind time. Intentional optional actions should be marked explicitly as optional/disabled instead of falling through optional chaining. |
| F146 | Medium | Viewer UI | Fallback / hidden default | Custom-profile authoring defaults are owned by the inspector UI instead of the profile API/defaults | `bobercad/app/ui/viewer/panels/inspector-panel.mjs:37`<br>`bobercad/app/engine/store/project-command-store.mjs:1026-1036` | Custom profile ids, seed geometry, designation policy, and parsing rules can differ between inspector-created profiles and any other caller of the public store API. | Move default custom-profile draft geometry, id prefix policy, and point-list parsing into a named profile authoring helper/API. The inspector should pass user edits or omit the id to inherit the store default. |
| F147 | Low | Viewer UI | Fallback / hidden default | Grid level creation uses a UI-local next-elevation policy | `bobercad/app/ui/viewer/panels/inspector-panel.mjs:532-538`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:315-331`<br>`bobercad/app/engine/store/project-store-model-helpers.mjs:299-315` | UI-created levels follow a different implicit spacing/naming policy than non-UI callers. Future level tools, Smart Component workflows, or importers can create different datum layouts from the same model API. | Add a canonical project datum authoring helper such as `nextLevelDefaults(project, options)` and have both inspector UI and store/create commands use it explicitly. |
| F148 | High | Viewer UI | Fallback / hidden default | Quick-action rendering bypasses the shared action descriptor validation used by nearby panel controls | `bobercad/app/ui/viewer/panels/panel-elements.mjs:782-805` | The same descriptor shape can be hidden in one control but rendered or throw during listener attachment in `quickActions()`. That makes generated action metadata harder to validate and reinforces F145's silent... | Route `quickActions()` through the same descriptor-action normalization used by `actionButton()`/`descriptorActions()`, or make missing callbacks an explicit disabled-action state with validation. |
| F149 | Low | UI design / CSS | Duplicated logic | Project data/files/properties panels duplicate the same panel controller/search/filter shell | `bobercad/app/ui/viewer/project-data-panel.mjs:24-113`<br>`bobercad/app/ui/viewer/project-files-panel.mjs:31-121`<br>`bobercad/app/ui/viewer/project-properties-panel.mjs:36-93` | Keyboard focus, dataset search coverage, empty-state behavior, and row-action wiring can drift across the three adjacent data panels. Adding a fourth data surface would likely copy the same controller again. | Extract a shared data-panel controller/filter helper that takes namespace, dataset keys, section builder, and row matcher hooks; keep only domain row construction in each panel. |
| F150 | Medium | Viewer UI | Compatibility layer | Project properties panel owns a runtime compatibility layer for metadata aliases instead of a model/store metadata contract | `bobercad/app/ui/viewer/project-properties-panel.mjs:19-33`<br>`bobercad/app/ui/viewer/viewer-app-controller.mjs:23-25`<br>`bobercad/app/engine/store/project-command-store.mjs:661-669` | Old or alternate project metadata keys remain active in normal UI reads. Editing a value can add the canonical BIM property path while leaving the legacy alias in place, so two fields can coexist with different values... | Move project metadata alias normalization into a named migration/import or store-level accessor contract. The panel should read/write canonical fields only, or explicitly show legacy aliases as diagnostics rather than silently merging... |
| F151 | Low | Viewer UI | Contract drift | Smart Component connection artwork is chosen by UI keyword heuristics instead of component metadata | `bobercad/app/ui/viewer/smart-component-browser.mjs:26-35`<br>`bobercad/app/engine/modules/smart-components/smart-component-parameters-and-definition.mjs:245-260` | Renaming a preset or definition can change its image without any catalog/schema validation, and new connection kinds fall back to generic end-plate/connection artwork until UI keyword code is updated. | Add explicit artwork/thumbnail metadata to the Smart Component preset/preview contract, or keep the generated preview service as the only preview source and remove keyword artwork inference. |
| F152 | Low | Viewer UI | Compatibility layer | Smart Component connection parameter tabs are rewritten by runtime text heuristics despite an explicit `ui.tabs` schema | `bobercad/app/ui/viewer/smart-component-parameter-tabs.mjs:1-4`<br>`bobercad/app/schemas/smart-component.schema.json:410-447` | Authored tab layout is not authoritative for connection components. Renaming labels or parameter paths can move controls between Properties/Bolts/Anchors without schema validation, and new fastener terminology needs UI... | Make connection tab grouping explicit in component `ui.tabs` or schema-supported metadata. If backward compatibility is needed, isolate this as a named migration/normalizer and remove text guessing from normal runtime rendering. |
| F153 | Medium | Viewer UI | Fallback / hidden default | Stair route-module domain rules are duplicated between the parameter UI and the stair solver | `bobercad/app/ui/viewer/smart-component-parameter-ui.mjs:312-352`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/config.json:55-65`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/solver.mjs:225-235` | The route editor can offer, default, or normalize route modules differently from the solver that consumes them. Adding a route type or changing a default requires synchronized edits in UI code and component solver... | Move route module type definitions/defaults/field specs into stair-system component metadata or a shared component helper consumed by both UI and solver. The generic Smart Component parameter UI should render descriptors, not encode... |
| F154 | Medium | Viewer UI | Fallback / hidden default | Smart Component preview generation builds its own UI-side ProjectStore and member-pair resolver outside the viewer app API | `bobercad/app/ui/viewer/smart-component-preview-service.mjs:3` | Preview behavior has a separate orchestration path from normal viewer/app commands. Candidate member pairing, context cleanup, sample-project store construction, and preview fallback messages can drift from the main... | Move preview orchestration and candidate-pair policy behind an engine/app preview API. The UI service should request a preview by preset/selection/context and render the returned preview state, not construct stores and pair candidates... |
| F155 | Low | Viewer UI | Fallback / hidden default | Viewer render scheduler owns a separate project-object LOD estimator instead of using scene geometry metadata | `bobercad/app/ui/viewer/viewer-render-scheduler.mjs:53-74`<br>`bobercad/app/rendering/scene/scene-geometry-builder.mjs:12-36`<br>`bobercad/app/rendering/webgl/webgl-viewer-runtime.mjs:227-234` | Large-project detail selection is decided by a pre-build approximation while final visibility is decided by scene-derived bounds. New object kinds, curved members, hidden/display rules, feature ownership, or generated... | Move the "which objects deserve detail" policy behind a scene/rendering metadata API, or add a shared object visual-bounds service consumed by both the scheduler and scene builder. Keep scheduler responsibilities to timing, invalidation,... |
| F156 | Medium | UI design / CSS | Compatibility layer | Viewer runtime infers trim-editor active state from DOM classes and title text | `bobercad/app/ui/viewer/viewer-runtime.mjs:779-787`<br>`bobercad/app/ui/viewer/panels/trim-joint-editor-panel.mjs:1615-1620` | CSS class changes, panel title changes, localization, or render-layout changes can alter runtime selection behavior without changing the editor state. This makes UI markup a hidden compatibility contract between the... | Replace `trimJointEditorPanelActive()` with an explicit trim editor API state, for example `trimJointEditorApi.isActive()` or an expanded `keepsSceneFocus()` contract that covers create-mode/card-active cases. |
| F157 | High | UI design / CSS | CSS / selector issue | Design-system command palette stylesheet ends with an unterminated media query | `bobercad/app/ui/design-system/command-palette.css:296`<br>`bobercad/app/ui/design-system/components.css:4` | The intended mobile command palette rules are missing, and CSS parser recovery at EOF leaves an empty or malformed breakpoint block. Future edits may accidentally land inside an unclosed media scope. | Either remove the stray media query if no mobile overrides are needed or add the intended mobile rules plus closing brace. |
| F158 | High | UI design / CSS | CSS / selector issue | Design-system panels/controls stylesheet ends inside an unclosed `.bc-keycap` rule | `bobercad/app/ui/design-system/panels-and-controls.css:1537-1549`<br>`bobercad/app/ui/design-system/components.css:3` | Parser recovery at EOF may preserve earlier rules, but the last component rule is malformed and future appended styles would be parsed inside the keycap block until manually closed. | Add the missing closing brace and run a CSS syntax check; consider adding a repository check for balanced CSS blocks so shared stylesheets cannot end in open rules. |
| F159 | Medium | UI design / CSS | CSS / selector issue | Shell dock layout state is split across multiple CSS files instead of one standardized dock contract | `bobercad/app/ui/shell/workspace-shell.css:186-306`<br>`bobercad/app/ui/shell/dock-tabs.css:188-207`<br>`bobercad/app/ui/shell/inspector-dock.css:56-99` | A new dock state, side-dock behavior, tab rail behavior, or non-side dock mode must be synchronized across base shell CSS, library-tab CSS, and inspector-dock CSS. The CSS selectors become an implicit layout API, and... | Introduce one generic dock shell/body/rail selector contract for `data-workspace-panel-*` states, then keep `dock-tabs.css` and `inspector-dock.css` limited to content density and panel-specific visual overrides. |
| F160 | Medium | UI design / CSS | CSS / selector issue | Nav cube right-dock clearance logic is duplicated between CSS selectors and runtime JS | `bobercad/app/ui/viewer/nav-cube.css:43-47`<br>`bobercad/app/ui/viewer/viewer-workspace-bindings.mjs:223-234` | The CSS and JS must agree exactly on what counts as dock occupancy. A future workspace state such as a renamed dock attribute, non-side dock exception, authoring exception, or revealed-state change can update one path... | Put the occupancy policy behind one workspace/dock helper contract. Either expose a single data attribute such as `data-nav-cube-clearance-active` from JS and let CSS read only that state, or centralize the selector predicate with the... |
| F161 | High | UI design / CSS | CSS / selector issue | Smart Component browser preview tile hardcodes a light background outside theme tokens | `bobercad/app/ui/viewer/smart-component-browser.css:48-57`<br>`bobercad/app/ui/viewer/style.css:13` | The tile preview surface can stay light in dark theme or drift from future theme/background tokens. This is small, but it is exactly the kind of one-off style value that bypasses the design-system token contract. | Replace the raw hex with an existing theme token if one fits, or add a named preview/thumbnail background token to `tokens.css` plus both theme files. |
| F162 | Medium | UI design / CSS | CSS / selector issue | Viewer editor panels stylesheet keeps an unused trim inspector-context selector contract | `bobercad/app/ui/viewer/index.html:45-47`<br>`bobercad/app/ui/viewer/viewer-editor-panels.css:21-32`<br>`bobercad/app/ui/viewer/panels/trim-joint-editor-panel.mjs:1449` | The stylesheet carries a stale or alternate mount contract. Future trim-panel work can assume a trim inspector slot exists because styles are present, while runtime actually mounts/styles the panel through... | Remove the unused `[data-inspector-context-panel="trim"]` selectors if they are stale, or add/register a real trim inspector slot through the same shell/panel metadata path if that mount point is intended. |
| F163 | Medium | Docs / audit records | Contract drift | Architecture audit docs report zero active architecture findings while the current manual standardization audit has active fallback/compatibility findings | `docs/architecture/app-architecture-audit.md:9-18`<br>`docs/architecture/app-architecture-audit.md:20-29`<br>`docs/architecture/app-architecture-line-findings.csv:1`<br>`docs/architecture/app-architecture-rescan-compact-table.md:7-19` | The architecture docs and scan artifacts can be read as an authoritative clean bill of health even though the deeper manual audit found active compatibility layers, duplicated API surfaces, hidden defaults, and contract drift. | Mark the June 20 architecture audit as historical/threshold-based, link it to this newer manual standardization audit, and explain that `architecture_line_scan.js` is not sufficient to prove absence of fallback, compatibility, duplication, or API-bypass issues. |
| F164 | Medium | Docs / audit records | Contract drift | Folder structure documentation still describes obsolete API/data-library locations | `docs/architecture/folder-structure.md:66-79`<br>`docs/architecture/folder-structure.md:157-160`<br>`docs/architecture/app-architecture-remediation-log.md:77-83`<br>`docs/architecture/folder-structure.md:236-242` | Stale path examples can make future work place API code in old folders, reintroduce data-library UI/runtime coupling, or miss the current `engine/api/model` and `engine/api/interaction` boundaries. | Refresh `folder-structure.md` from the current app/data layout and explicitly mark removed data-library UI paths as obsolete/not allowed. |
| F165 | Medium | Docs / audit records | Contract drift | Active Viewer MVP plan still describes plate rendering from `width`/`height` instead of the current sketch source-of-truth | `docs/exec-plans/active/0001-viewer-mvp.md:20-24`<br>`docs/architecture/data-model.md:192-202`<br>`docs/architecture/data-model.md:221-232` | Because the plan is still under `docs/exec-plans/active`, an agent can follow the old plate contract and reintroduce `width`/`height` fallback behavior or treat sketch data as optional. | Update the Viewer MVP plan to mark it historical or revise required behavior to render plates from `sketch` plus placement axes. |
| F166 | Medium | Docs / audit records | Contract drift | Active Authoring/NC1 plan contains stale validation and feature-field instructions | `docs/exec-plans/active/0002-authoring-patterns-and-nc1-data.md:139`<br>`docs/exec-plans/active/0002-authoring-patterns-and-nc1-data.md:177`<br>`docs/exec-plans/active/0002-authoring-patterns-and-nc1-data.md:335-343`<br>`docs/architecture/app-architecture-remediation-log.md:111-114` | The active plan can send future work toward obsolete field names and a new Python quality-check path instead of the existing JS domain validator. | Revise the active plan to current terms: `featureIds`, `holePatternRef`, and `scripts/validate_domain_model.js` as the semantic quality checker wired through `check_repo.js`. |
| F167 | Medium | Docs / audit records | Contract drift | Active Smart Component generator plan still uses `componentRef` as a selection/composition contract | `docs/exec-plans/active/0003-smart-component-generator-template-and-stair-system.md:95-99`<br>`docs/exec-plans/active/0003-smart-component-generator-template-and-stair-system.md:274-280`<br>`docs/architecture/data-model.md:279-295` | New generator work can reintroduce a `componentRef` compatibility contract even though current app/schema review found no active `componentRefs` implementation path. | Replace `componentRef` language with current registered Smart Component/sourceComponent terminology and specify exact project fields used for child component provenance and lifecycle. |
| F168 | High | Engine model / schema | Contract drift | Member geometry has two active authoritative paths: `start/end` and analytic `centerline` | `docs/architecture/data-model.md:73-75`<br>`docs/exec-plans/active/0005-stair-gallery-qa-curved-members-and-reusable-connections.md:14-20`<br>`bobercad/app/schemas/project.schema.json:1459-1467`<br>`bobercad/app/engine/api/model/transport-sectioning.mjs:85-112`<br>`bobercad/app/rendering/scene/scene-member-geometry-adapters.mjs:277-384` | Transport length, curved rendering, snapping, editing, object bounds, trim/weld evaluation, and Smart Component creation can each read a different axis/path policy. | Decide the canonical member geometry contract and update every member API, evaluator, snap provider, editor, schema, and data-model rule around that one contract. |
| F169 | Medium | Docs / audit records | Contract drift | Active stair visual QA plan checks old/loose plate and fastener fields instead of current sketch/hole-pattern contracts | `docs/exec-plans/active/0004-stair-system-visual-quality-and-variant-acceptance.md:303-305`<br>`docs/architecture/data-model.md:117-120`<br>`docs/architecture/data-model.md:192-202` | The QA plan can pass or demand data shapes that are looser than the current model, encouraging direct bolt-position fallbacks or generic plate `shape` fields. | Update the QA checklist to require `holePatternRef`/hosted `model.holePatterns` for bolt positions and `plate.sketch` for plate shape checks. |
| F170 | Medium | Docs / audit records | Contract drift | Active snapping plan still describes missing split modules after the snap manager implementation landed | `docs/exec-plans/active/0006-unified-snapping-and-selection-manager.md:158-166`<br>`docs/exec-plans/active/0006-unified-snapping-and-selection-manager.md:667-672`<br>`bobercad/app/ui/viewer/toolbar/modeling-toolbar.mjs:143-165` | A future pass can create duplicate snap-result/toolbar modules because the active plan says they are expected, even though current implementation folded those responsibilities into existing modules. | Update plan 0006 to reflect current module ownership, or intentionally create the missing modules and move code there. |
| F171 | Medium | Docs / audit records | Contract drift | Active Tekla roadmap treats implemented domain validation and stale-pattern guards as future/new work | `docs/exec-plans/active/0008-tekla-killer-1-year-roadmap.md:342-357`<br>`docs/exec-plans/active/0008-tekla-killer-1-year-roadmap.md:620-625`<br>`scripts/check_repo.js:5-9`<br>`scripts/validate_domain_model.js:443-452`<br>`scripts/check_repo_contracts.js:907` | A future agent can create a second domain validator/check script next to the existing one, split the quality gate, or duplicate stale-pattern checks instead of extending the canonical validator. | Update plan 0008 so Month 1 work says "extend `scripts/validate_domain_model.js` and `check_repo.js`" rather than "add a skeleton/new check"; keep stale script cleanup as still-active until the referenced tool files are migrated or deleted. |
| F172 | Medium | Docs / audit records | Contract drift | Active Smart Component preview plan still names a planned engine preview service and `evaluateSmartComponentPreset(...)` API after the vertical slice landed under a different canonical store/UI path | `docs/exec-plans/active/0009-real-preview-icons-and-connection-filtering.md:40-79`<br>`docs/exec-plans/active/0009-real-preview-icons-and-connection-filtering.md:139-142`<br>`docs/exec-plans/active/0009-real-preview-icons-and-connection-filtering.md:183-186`<br>`bobercad/app/engine/store/project-store-smart-component-methods.mjs:256-272`<br>`bobercad/app/engine/api/api-register.json:615-622` | A future implementation pass can add an engine preview-service and `evaluateSmartComponentPreset` beside the existing store/UI preview service, creating a duplicated eligibility/dry-run API and another compatibility layer around connection filtering. | Update plan 0009 to declare `store.previewSmartComponentFromPreset(...)` plus `createSmartComponentPreviewService(...)` as the current canonical preview path, or intentionally migrate the code to the engine-level service in one coordinated change with the API register and contracts. |
| F173 | Medium | Docs / audit records | Contract drift | Junior review documents are stale audit artifacts but are not marked historical/current-status-bound | `docs/junior review/codereview.txt:45-72`<br>`docs/junior review/codereview.txt:82-91`<br>`docs/junior review/codereview-verified.txt:7-47`<br>`bobercad/app/ui/viewer/panels/trim-joint-editor-panel.mjs:3`<br>`bobercad/app/engine/store/project-command-store.mjs:5`<br>`scripts/contracts/project_store_contracts.js:64-72` | A future cleanup pass can treat stale review text as current evidence and re-open fixed P0/API-register work, or apply old recommendations without respecting current source-of-truth rules and newer contracts. | Add a status header to both junior review files marking them historical inputs, point readers to the current manual audit/table and current contract scripts, and move fixed P0 items under "resolved/historical examples". |
| F174 | Medium | Docs / audit records | Contract drift | Validation documentation still describes domain validation as future work even though `check_repo.js` runs the domain and viewer-runtime gates today | `docs/quality/validation.md:13-24`<br>`docs/quality/validation.md:36-39`<br>`scripts/check_repo.js:4-9`<br>`scripts/validate_domain_model.js:437-452`<br>`scripts/check_viewer_runtime.js:52-1218` | A future agent can create a second validator or treat domain/runtime checks as missing because the quality document says they are future work. | Update `docs/quality/validation.md` so `check_repo.js` is documented as the canonical aggregate: branch guard, repo structure/contracts, domain model validation, and viewer runtime checks. Replace "future domain validation" with guidance to extend the existing validator/runtime checks. |
| F175 | Medium | Docs / audit records | Contract drift | Integrator workflow mixes the local file queue with GitHub PR label/PR operations in a single loop | `docs/workflows/codex-workflow.md:60-62`<br>`docs/workflows/codex-workflow.md:75-85`<br>`docs/workflows/codex-workflow.md:87-93`<br>`docs/workflows/codex-workflow.md:97-110`<br>`AGENTS.md:57-58` | In local file mode, an integrator can try to label/comment/merge a PR or treat the fixed worktree request as a PR branch instead of using one local status/lock path. | Split the integrator loop into two explicit paths: local file workflow using `STATUS: READY` plus local status transitions around fixed worktrees, and GitHub PR workflow using labels/comments/PR merge steps. Avoid PR terminology in the local path. |
| F176 | Medium | Tooling / scripts | Contract drift | Architecture line scan is documented as clean but is not part of the standard check gate and currently reports findings when run | `docs/architecture/app-architecture-audit.md:20-29`<br>`scripts/check_repo.js:5-10`<br>`scripts/architecture_line_scan.js:5-16`<br>`scripts/architecture_line_scan.js:209-225`<br>`scripts/architecture_line_scan.js:227-241` | `check_repo.js` can pass while the architecture scan that the audit document treats as clean has active findings, so future agents can trust stale zero-finding architecture docs. | Either wire `architecture_line_scan.js` into the standard check gate and keep generated docs current, or mark the architecture scan/audit artifacts as historical/manual-only. |
| F177 | Medium | Tooling / scripts | Contract drift | Branch guard allows any non-main or detached branch, so fixed agent worktree branch assignments are not enforced | `AGENTS.md:41-45`<br>`docs/workflows/multi-agent-codex.md:7-10`<br>`scripts/check_branch_guard.js:35-38`<br>`scripts/check_repo.js:5-10` | `check_repo.js` can pass in `C:\boberos\agent5` while the folder is on another agent branch, a random branch, or detached HEAD. | Extend the guard for the fixed local layout: require `codex/agentN` inside `C:\boberos\agentN`, keep `main` rules for `C:\boberos\main`, and allow exceptional bypass only when user-approved. |
| F178 | Medium | Tooling / scripts | Standardization | Root repository contract checker still owns large hardcoded contract families instead of delegating them to focused contract suites | `scripts/check_repo_contracts.js:14-195`<br>`scripts/check_repo_contracts.js:361-457`<br>`scripts/check_repo_contracts.js:526-732`<br>`scripts/check_repo_contracts.js:943-1132` | The root checker remains a mixed registry, architecture policy, domain check, UI contract check, and snap API check, so future changes can keep adding brittle string probes to one file. | Keep `check_repo_contracts.js` as an orchestrator plus tiny shared helpers. Move required-file/layout, plate-sketch, app/evaluator boundary, and viewer snap/settings checks into focused `scripts/contracts/` modules. |
| F179 | Medium | Tooling / scripts | Duplicated logic | Project semantic validation is duplicated between `check_repo_contracts.js` and `validate_domain_model.js` | `scripts/check_repo_contracts.js:794-941`<br>`scripts/validate_domain_model.js:138-175`<br>`scripts/validate_domain_model.js:330-350`<br>`scripts/validate_domain_model.js:356-389`<br>`scripts/validate_domain_model.js:443-452` | Project semantic rules now live in two scripts with different helpers and error text, so model migrations can update one path while the other stays stale. | Make `validate_domain_model.js` the canonical project semantic validator and remove or reuse the duplicate project checks in `check_repo_contracts.js`. |
| F180 | Medium | Tooling / scripts | Standardization | Viewer runtime smoke test is one giant `main()` with repeated inline fixtures instead of focused runtime contract suites | `scripts/check_viewer_runtime.js:7-24`<br>`scripts/check_viewer_runtime.js:36-1218`<br>`scripts/check_viewer_runtime.js:216-258`<br>`scripts/check_viewer_runtime.js:605-627`<br>`scripts/check_viewer_runtime.js:938-1063` | Failure ownership is hard to trace because geometry, snap, overlay, Smart Component dimensions, and controller behavior all share one procedural test body with repeated fixtures. | Split viewer runtime checks into focused suites under `scripts/contracts/`, keep shared project/snap fixtures in `viewer_runtime_contract_helpers.js`, and leave `check_viewer_runtime.js` as a small orchestrator. |
| F181 | Medium | Tooling / scripts | Standardization | Model authoring contract suite mixes model API, snap/rendering UI, manipulator, and generic API contracts under one module name | `scripts/contracts/model_authoring_contracts.js:15-374`<br>`scripts/contracts/model_authoring_contracts.js:18-24`<br>`scripts/contracts/model_authoring_contracts.js:376-548`<br>`scripts/contracts/model_authoring_contracts.js:550-556` | The filename suggests one model-authoring contract, but the suite is a catch-all for model API, snap behavior, scene overlays, manipulator math, and transport sectioning. | Split this file into focused suites such as `member_authoring_contracts.js`, `snap_contracts.js`, `manipulator_contracts.js`, `generic_model_api_contracts.js`, and `transport_sectioning_contracts.js`, then have the root checker orchestrate them. |
| F182 | Medium | Tooling / scripts | Standardization | Smart Component lifecycle contract suite also owns preview rendering, scene visibility, and stair-system generator contracts | `scripts/contracts/smart_component_lifecycle_contracts.js:63-382`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:65-80`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:89-258`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:268-380`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:385-641` | A single failure can belong to Smart Component lifecycle, preview UI, renderer visibility, annotation dimensions, fastener catalogs, or stair generation, but the file name hides that spread. | Split the suite into focused contracts for lifecycle/dependency cleanup, preview/thumbnail service, scene visibility/rendering, fastener/hole derivation, and stair-system generator/compliance/sample-health. Keep shared catalog/sample setup in a helper. |
| F183 | Medium | Tooling / scripts | Standardization | Smart Component quick-property contract repeats the generated descriptor/action taxonomy as source-text token checks | `scripts/contracts/smart_component_quick_property_contracts.js:188-247`<br>`scripts/contracts/smart_component_quick_property_contracts.js:238-435` | The generated descriptor API has a second manual source of truth in a contract file: action names, descriptor types, CSS classes, forbidden helper names, and commit/custom action shapes are duplicated as strings. | Promote descriptor/action ids, generated field types, and shared CSS class contracts into exported constants or focused fixtures. Convert positive checks to behavior-driven descriptor generation/rendering tests and keep static forbidden-helper checks small. |
| F184 | Medium | Tooling / scripts | Duplicated logic | Contract helper standardization is partial: shared helpers exist, but adjacent contract suites still define local copies | `scripts/contracts/ui_contract_helpers.js:6-26`<br>`scripts/contracts/ui_contract_helpers.js:109-116`<br>`scripts/contracts/model_authoring_contracts.js:7-13`<br>`scripts/contracts/project_schema_contracts.js:6-15`<br>`scripts/contracts/project_store_contracts.js:7-17`<br>`scripts/contracts/smart_component_lifecycle_contracts.js:7-17`<br>`scripts/contracts/smart_component_quick_property_contracts.js:7-12` | Contract suites now have two helper layers: a shared helper module and repeated local mini-helpers, so error formatting, root handling, line-number calculation, and fixture loading can drift. | Promote common non-UI helpers into a general `contract_helpers.js`, keep UI helpers focused on UI fixture text, and migrate all suites to shared helper imports. |
| F185 | Medium | Tooling / scripts | Standardization | UI workspace contract is still a giant detailed suite even after some sub-contract extraction | `scripts/contracts/ui_workspace_contracts.js:5-8`<br>`scripts/contracts/ui_workspace_contracts.js:34-113`<br>`scripts/contracts/ui_workspace_contracts.js:146-831` | The file is both a root orchestrator and a large detailed UI policy suite; failures can belong to command metadata, workspace persistence, data panels, inspector dock, bottom/settings strips, or snap/view state. | Keep `checkUiWorkspace` as a small orchestrator/context builder and split detailed assertions into focused command metadata, workspace storage, data dock, project data, feature navbar, bottom/settings strip, and inspector dock contract suites. |
| F186 | Medium | Tooling / scripts | Standardization | Generated-panel UI contract is a second broad catch-all for generated properties, data panels, model browser, Smart Component browser, and design-system CSS | `scripts/contracts/ui_workspace_generated_panel_contracts.js:6-69`<br>`scripts/contracts/ui_workspace_generated_panel_contracts.js:70-75`<br>`scripts/contracts/ui_workspace_generated_panel_contracts.js:434-557`<br>`scripts/contracts/ui_workspace_generated_panel_contracts.js:596-907` | The sub-contract still mixes generated properties, design-system primitives, left-dock data panels, model browser, Smart Component browser assets, and viewer CSS/import wiring in one file. | Split this suite into focused contracts for generated-properties renderer/bindings, panel-elements/design-system primitives, data-panel shared helpers, Model Browser, Project Files/Data panels, Smart Component Browser artwork, and viewer CSS/import wiring. |
| F187 | High | Smart Components | Contract drift | Smart Component child-component DSL still uses `componentRef` as the nested component selector | `bobercad/app/engine/modules/smart-components/smart-component-build.mjs:113`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:250-254`<br>`bobercad/app/engine/modules/smart-components/smart-component-build.mjs:264-270`<br>`bobercad/data/libraries/smart-components/components/buildings/warehouse/build.mjs:25,51`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/build.mjs:182,233,252,268,286,300,322,375,403` | Project JSON stores `sourceComponent`, but the authoring DSL and library generators still use `componentRef`, keeping a parallel component-reference API alive. | Rename the child-component authoring config to a current field such as `presetId` or `sourceComponentId`, update builder/library generators/docs/contracts together, and keep any `componentRef` importer as an explicit migration adapter only. |
| F188 | Medium | Tooling / scripts | Standardization | Inspector object contract mixes object descriptor shape checks with broad generated-binding factory integration tests | `scripts/contracts/ui_workspace_inspector_object_contracts.js:6-69`<br>`scripts/contracts/ui_workspace_inspector_object_contracts.js:70-789` | Object descriptor shape, focused editor descriptors, support-object descriptors, object reference descriptors, and binding-factory action routing all fail through one contract. | Split into focused suites for object descriptor dispatch, plate/sketch/bend descriptors, feature editor descriptors, trim summary/editor routing, fastener/support-object descriptors, object references, and generated inspector binding factory routing. |
| F189 | Medium | Tooling / scripts | Standardization | Left-dock shell contract is another broad UI integration catch-all under a narrow file name | `scripts/contracts/ui_workspace_left_dock_shell_contracts.js:6-100`<br>`scripts/contracts/ui_workspace_left_dock_shell_contracts.js:103-287`<br>`scripts/contracts/ui_workspace_left_dock_shell_contracts.js:289-668`<br>`scripts/contracts/ui_workspace_left_dock_shell_contracts.js:683-959` | Failures can belong to Smart Component browser metadata, command palette routing, viewer runtime, Inspector panel, Feature Editor, Trim Editor, Member Transform, Project Files/Data, frame libraries, or Inspector Dock state. | Split into focused contracts for Smart Component browser, command palette, viewer left-dock runtime, focused editors, Member Transform, Inspector panel shell, Project Files/Data runtime, and Inspector Dock. |
| F190 | Medium | Tooling / scripts | Standardization | Snap/view workspace contract mixes snap/view metadata with broad shell, customizer, CSS, and toolbar contracts | `scripts/contracts/ui_workspace_snap_view_workspace_contracts.js:6-80`<br>`scripts/contracts/ui_workspace_snap_view_workspace_contracts.js:81-390`<br>`scripts/contracts/ui_workspace_snap_view_workspace_contracts.js:408-830`<br>`scripts/contracts/ui_workspace_snap_view_workspace_contracts.js:842-1206` | Snap command metadata, view orientation, shell CSS, NavCube positioning, Feature Navbar, workspace customization, bottom/status strip behavior, and toolbar command defaults all fail through one broad contract. | Split into focused contracts for snap command metadata, view/display orientation metadata, viewer settings strip, render visibility, NavCube shell/CSS, Feature Navbar, workspace customizer primitives, panel/tab customization, bottom/status strip, snap settings control, and toolbar command ordering. |
| F191 | Medium | Tooling / scripts | API bypass | Stair sample generation uses two active creation paths for the same Smart Component family | `scripts/generate_stair_samples.mjs:3-5`<br>`scripts/generate_stair_samples.mjs:303-327`<br>`scripts/generate_stair_samples.mjs:328-331`<br>`scripts/generate_stair_samples.mjs:349-363` | Single-sample JSON and gallery JSON can pass through different reconciliation, command metadata, object-id reporting, and store-level validation paths. | Route gallery generation through the same project-store facade as per-variant generation, or add an explicit batch/gallery API that owns placement, metadata, update, validation, and final project extraction. |
| F192 | Medium | Tooling / scripts | API bypass | Tread-variant sample generator bypasses the project store and reimplements Smart Component parameter defaults | `scripts/generate_stair_tread_variants_sample.mjs:3-4`<br>`scripts/generate_stair_tread_variants_sample.mjs:12-35`<br>`scripts/generate_stair_tread_variants_sample.mjs:73-78`<br>`scripts/generate_stair_tread_variants_sample.mjs:176-199` | Parameter defaults, derived values, write-path semantics, store reconciliation, and command metadata can diverge from the normal project-store/Smart Component parameter APIs. | Use the project-store facade for tread variant generation and a shared Smart Component parameter resolver for defaults/derived values, or expose a dedicated sample-generation API wrapping runtime creation, parameter resolution, metadata updates, and validation. |
| F193 | Medium | Tooling / scripts | Duplicated logic | Stair sample generator scaffolding is duplicated across separate scripts | `scripts/generate_stair_samples.mjs:11-109`<br>`scripts/generate_stair_tread_variants_sample.mjs:8-94`<br>`scripts/generate_stair_treads_only_gallery.mjs:9-108` | Sample route shapes, base-project clearing, file-fetch library loading, fixture metadata, and direct runtime/store usage can drift between stair sample generators. | Extract a shared stair sample generation helper or CLI module that owns base project clearing, library loading, route variant builders, placement, metadata, store/runtime creation path, and output validation. |
| F194 | Medium | Tooling / scripts | Contract drift | Local JSON schema validator ignores `propertyNames` even though the UI workspace schema uses it | `bobercad/app/schemas/ui-workspace.schema.json:76-84`<br>`scripts/validate_json_schema.js:63-166`<br>`scripts/contracts/ui_workspace_contracts.js:258-260`<br>`scripts/contracts/ui_workspace_contracts.js:335-336` | Invalid persisted workspace section keys can pass local schema validation, and contracts can assert the schema keyword exists while the validator does not enforce it. | Add `propertyNames` support and keyword-coverage tests to `validate_json_schema.js`, or replace the local subset validator with a full JSON Schema implementation. |
| F195 | High | Tooling / scripts | Contract drift | Warehouse hall demo generator still writes the obsolete `model.connections` data path | `tools/demo/generate_warehouse_hall_12x24.mjs:30-50`<br>`tools/demo/generate_warehouse_hall_12x24.mjs:94-108`<br>`tools/demo/generate_warehouse_hall_12x24.mjs:193-224`<br>`tools/demo/generate_warehouse_hall_12x24.mjs:336-358`<br>`scripts/validate_domain_model.js:445-446` | Running this demo tool can regenerate a sample project with a stale data-model shape that the current domain validator explicitly rejects. | Migrate the generator to the current Smart Component/store API and `model.smartComponentInstances`, replace `connectionIds`/`modelDefaults.collections.connections` with current object/Smart Component references, and run `scripts/check_repo.js` after regenerating. |
| F196 | Medium | Tooling / scripts | Standardization | No-cache Python dev server bypasses the canonical worktree preview server contract | `tools/dev/no_cache_http_server.py:148-181`<br>`tools/dev/no_cache_http_server.py:223-232`<br>`scripts/serve_viewer.js:118-135` | This parallel preview server can produce URLs without the assigned agent port or `/codex/agentN/` branch prefix, making user review ambiguous. | Retire this server, or turn it into a live-reload wrapper around `scripts/serve_viewer.js` that keeps `dev_server.config.json`, agent ports, branch prefixes, and replacement policy authoritative. |
| F197 | Medium | Tooling / scripts | Standardization | Connection-view QA capture tool defaults to the non-canonical preview server path | `tools/qa/capture_connection_views.mjs:11`<br>`tools/qa/capture_connection_views.mjs:130-158`<br>`tools/qa/capture_connection_views.mjs:358-365`<br>`tools/qa/capture_connection_views.mjs:388-404` | QA artifacts can be produced from `127.0.0.1:8000` without an agent port or `/codex/agentN/` prefix, making the reviewed worktree ambiguous. | Make this runner use `scripts/serve_viewer.js`/`dev_server.config.json` to derive the default viewer URL, or require `--viewer-url` when no canonical agent preview is already running. |
| F198 | Medium | Tooling / scripts | Standardization | Stair QA runner hardcodes a non-canonical viewer URL and duplicates broad model validation | `tools/qa/qa_stair_variants.mjs:651-653`<br>`tools/qa/qa_stair_variants.mjs:895-899`<br>`tools/qa/qa_stair_variants.mjs:153-237`<br>`tools/qa/qa_stair_variants.mjs:296-610` | Review artifacts can point to port `8765` without the assigned agent port or `/codex/agentN/` prefix, and local QA model checks can drift from the canonical domain validator. | Accept the canonical viewer base URL from `scripts/serve_viewer.js` output or a required `--viewer-url`, and reuse `scripts/validate_domain_model.js` plus narrow stair-only QA checks. |
| F199 | High | Tooling / scripts | Contract drift | Member-move stress benchmark still targets the obsolete connection API | `tools/stress/benchmark_member_move.mjs:79`<br>`tools/stress/benchmark_member_move.mjs:83-91`<br>`tools/stress/benchmark_member_move.mjs:155`<br>`tools/stress/benchmark_member_move.mjs:162`<br>`bobercad/app/engine/store/project-command-store.mjs:1087-1093` | The benchmark can report zero affected connections for current Smart Component projects, measure the wrong regeneration path, or throw on the removed `regenerateMemberConnections` call. | Migrate it to `model.smartComponentInstances` and Smart Component dependency/store APIs, use `{ regenerateSmartComponents: false }` for pure move timing, and call `store.regenerateMemberSmartComponents(memberId)` for regeneration timing. |
| F200 | High | Tooling / scripts | Contract drift | Eiffel Tower stress generator still writes obsolete connection data | `tools/stress/generate_eiffel_tower_stress.mjs:109-130`<br>`tools/stress/generate_eiffel_tower_stress.mjs:249-258`<br>`tools/stress/generate_eiffel_tower_stress.mjs:656-659`<br>`tools/stress/generate_eiffel_tower_stress.mjs:727-745`<br>`scripts/validate_domain_model.js:446` | Regenerated stress output can use a `model.connections` shape that current validators reject, and its diagnostics/counts describe the removed connection architecture. | Rebuild the generator around `model.smartComponentInstances`, connection zones, interfaces, and current store Smart Component preset creation; remove connection defaults, assembly `connectionIds`, and all `model.connections` reads. |
| F201 | High | Tooling / scripts | Contract drift | Random-beam stress generator emits an empty obsolete `model.connections` collection | `tools/stress/generate_random_beam_stress_projects.mjs:103-109`<br>`tools/stress/generate_random_beam_stress_projects.mjs:151-156`<br>`tools/stress/generate_random_beam_stress_projects.mjs:235`<br>`scripts/validate_domain_model.js:446` | The generated fixtures can fail current domain validation even though the member records are simple, because an empty obsolete collection is still present. | Remove `libraries.connections` and the empty `model.connections` field; put any no-connection stress metadata under `model.addonData.stressTest`. |
| F202 | High | Tooling / scripts | Contract drift | Warehouse-hall stress duplicator reintroduces the obsolete `connections` collection around a current Smart Component sample | `tools/stress/generate_warehouse_hall_stress.mjs:23-40`<br>`tools/stress/generate_warehouse_hall_stress.mjs:72-93`<br>`tools/stress/generate_warehouse_hall_stress.mjs:96-104`<br>`tools/stress/generate_warehouse_hall_stress.mjs:237-248`<br>`scripts/validate_domain_model.js:446` | A valid Smart Component-based warehouse sample can be expanded into an invalid stress fixture by adding an empty removed collection and old connection-library metadata. | Remove `connections` from the collection prefix, model template, library patching, hall metadata, and console counts; count Smart Component instances or connection zones instead if needed. |
| F203 | Medium | Tooling / scripts | Standardization | Interactive member-drag stress runner bypasses canonical preview and dependency setup | `tools/stress/interactive_member_drag.mjs:8`<br>`tools/stress/interactive_member_drag.mjs:9-10`<br>`tools/stress/interactive_member_drag.mjs:48-52`<br>`package.json:3-7` | Interactive performance results can come from the wrong server/worktree, and browser automation setup is invisible to normal repo scripts/checks. | Require or derive a canonical `scripts/serve_viewer.js` worktree URL with branch prefix, and move browser automation setup into a documented repo script or checked bootstrap. |
| F204 | Medium | Smart Components | API bypass | Smart Component data-library hardware generators bypass the shared fastener/hole derivation contract | `bobercad/data/libraries/smart-components/components/shared/connections/stair-and-splice-hardware-connections.mjs:223-265`<br>`bobercad/data/libraries/smart-components/components/shared/connections/stair-and-splice-hardware-connections.mjs:276-281`<br>`bobercad/data/libraries/smart-components/components/shared/connections/stair-and-splice-hardware-connections.mjs:412-416`<br>`bobercad/data/libraries/smart-components/components/shared/connections/stair-and-splice-hardware-connections.mjs:809-818` | Library-generated hardware can disagree with engine connection primitives and UI-derived fastener hole values when catalog/tolerance/custom-diameter rules change. | Move fastener/hole diameter resolution into a shared Smart Component/runtime helper that accepts `fastenerRef`, tolerance, and optional custom diameter, then make library hardware generators call it. |
| F205 | Medium | Smart Components | Fallback / hidden default | Rolled centerline helper invents missing analytic path axes and center values | `bobercad/data/libraries/smart-components/components/shared/geometry/rolled-centerline.mjs:4-15`<br>`bobercad/data/libraries/smart-components/components/shared/geometry/rolled-centerline.mjs:22`<br>`bobercad/data/libraries/smart-components/components/shared/geometry/rolled-centerline.mjs:31`<br>`bobercad/data/libraries/smart-components/components/shared/geometry/rolled-centerline.mjs:35-37` | Curved support/railing Smart Components can generate plausible analytic centerlines from incomplete path data instead of failing at the authoring boundary. | Normalize and validate the source path before constructing the rolled centerline, require explicit center/axes/angles for arc/helix inputs, and keep preview-only defaults in a named adapter. |
| F206 | Medium | Smart Components | Fallback / hidden default | Landing generator falls back from solved footprints and axes to rectangular defaults | `bobercad/data/libraries/smart-components/components/stairs/landings/shared-landing.mjs:9-10`<br>`bobercad/data/libraries/smart-components/components/stairs/landings/shared-landing.mjs:65-74`<br>`bobercad/data/libraries/smart-components/components/stairs/landings/shared-landing.mjs:87-95` | A bad stair solver landing footprint can still generate plausible rectangular landing geometry, hiding route/landing solver problems. | Require a valid solved landing footprint and axes for generated landing geometry, or move the rectangular fallback into a named preview/import adapter with diagnostics. |
| F207 | Medium | Smart Components | Fallback / hidden default | Railing generator repeats hidden route-axis fallbacks and a direct fastener-hole path | `bobercad/data/libraries/smart-components/components/stairs/railings/shared/railing-path-and-panel-system.mjs:19-22`<br>`bobercad/data/libraries/smart-components/components/stairs/railings/shared/railing-path-and-panel-system.mjs:80-84`<br>`bobercad/data/libraries/smart-components/components/stairs/railings/shared/railing-path-and-panel-system.mjs:112-114`<br>`bobercad/data/libraries/smart-components/components/stairs/railings/shared/railing-path-and-panel-system.mjs:195-298` | Incomplete stair route stations can still generate plausible railing geometry with invented axes, and railing fastener holes can drift from the shared fastener/tolerance policy. | Require solved railing station frames, route fastener/hole values through the F204 resolver, and keep preview-only axis defaults in a named degraded-preview adapter with diagnostics. |
| F208 | Medium | Smart Components | API bypass | Transport section generator bypasses a canonical Smart Component ownership/output API | `bobercad/data/libraries/smart-components/components/stairs/sections/transport-sections/build.mjs:15-29`<br>`bobercad/data/libraries/smart-components/components/stairs/sections/transport-sections/build.mjs:31-57`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/build.mjs:402-410` | Sectioning depends on internal Smart Component instance storage instead of a declared source-object output contract. | Expose and use one runtime/library API for source owned objects, such as `ctx.smartComponentObjectIds(componentId)` or declared child outputs. |
| F209 | Medium | Smart Components | Compatibility layer | Stair-system child selection has a local component-family alias outside the register | `bobercad/data/libraries/smart-components/components/stairs/stair-system/build.mjs:192-195`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/build.mjs:181-186`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/build.mjs:299-313` | Component id selection is partly register-driven and partly a hardcoded local exception, so aliases can drift from the Smart Component register. | Replace local `componentRefForRailing(...)` with a registered Smart Component family/preset resolver and migrate child selection away from raw `componentRef`. |
| F210 | Medium | Smart Components | Fallback / hidden default | Stair solver normalizes incomplete route geometry into generated defaults | `bobercad/data/libraries/smart-components/components/stairs/stair-system/solver.mjs:7-19`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/solver.mjs:77-87`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/solver.mjs:225-235`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/solver.mjs:720-750` | Incomplete route/module/landing data can become plausible generated stair geometry before child components see the issue. | Split default resolution from solving; require explicit route modules, station frames, landing geometry, and curved-flight data in the solver, with defaults only in preset/preview adapters. |
| F211 | Medium | Smart Components | Fallback / hidden default | Support generator falls back from solved support axes/elevation to defaults | `bobercad/data/libraries/smart-components/components/stairs/supports/shared/support-system.mjs:11-13`<br>`bobercad/data/libraries/smart-components/components/stairs/supports/shared/support-system.mjs:138`<br>`bobercad/data/libraries/smart-components/components/stairs/supports/shared/support-system.mjs:152-177` | Missing support segment lateral vectors or ambiguous spiral core elevation can still create plausible stringers/columns that propagate into hardware and sectioning. | Require explicit support segment lateral vectors and core elevation, use nullish checks for zero elevations, and keep rolled centerline validation strict through F205. |
| F212 | Medium | Smart Components | Fallback / hidden default | Tread generator falls back from solved tread frames to parameter/default geometry | `bobercad/data/libraries/smart-components/components/stairs/treads/shared/tread-set.mjs:23-27`<br>`bobercad/data/libraries/smart-components/components/stairs/treads/shared/tread-set.mjs:190-211`<br>`bobercad/data/libraries/smart-components/components/stairs/treads/shared/tread-set.mjs:196-203`<br>`bobercad/data/libraries/smart-components/components/stairs/treads/shared/tread-set.mjs:345-357` | Bad solved tread frames can still generate rectangular treads and risers from broader parameters, hiding solver issues. | Treat solved `layout.treads` frames as authoritative and move parameter/default fallback to an explicit preset/preview resolver before generation. |
| F213 | Medium | Smart Components | Contract drift | Smart Component config contracts still expose raw hole-diameter parameters beside the newer derived fastener-hole model | `bobercad/data/libraries/smart-components/components/connections/apex-gusset/config.json:44-52`<br>`bobercad/data/libraries/smart-components/components/connections/base-plate/config.json:45-53`<br>`bobercad/data/libraries/smart-components/components/connections/fin-plate/config.json:365-400`<br>`bobercad/data/libraries/smart-components/components/stairs/stair-system/config.json:916-969` | Current Smart Component definitions expose two public hole-sizing standards, so users and generators can drift between raw diameters and fastener/tolerance/custom derivation. | Standardize Smart Component config schemas on the derived fastener-hole contract, keeping direct diameter only as `customDiameter` or migration input. |
| F214 | Medium | Smart Components | Contract drift | Generated stair project provenance persists raw hole-diameter parameter paths beside generated hole patterns | `bobercad/data/projects/sample_stair_all_variants.json:254944`<br>`bobercad/data/projects/sample_stair_curved.json:22069`<br>`bobercad/data/projects/sample_stair_glass_rail.json:14472`<br>`bobercad/data/projects/sample_stair_glass_rail.json:15048` | Sample projects no longer store the obsolete connection collection, but managed fastener provenance still embeds old raw diameter parameter paths. | Migrate generated fastener-group provenance paths to the derived fastener/tolerance/custom-diameter contract while keeping numeric `model.holePatterns.*.holeDiameter` as generated geometry output. |
| F215 | Medium | Docs / audit records | Contract drift | Root filename/location audit artifact reports stale current coverage | `filename-location-audit.txt:2-7` | The tracked root report still presents a 273-file, zero-finding result without a historical/current-status banner, while the current repo manifest and manual audit are larger. | Mark it historical or move it under an audit archive, and link to the current manual standardization audit/table. |
| F216 | Low | UI shell / workspace | Fallback / hidden default | Root `index.html` carries a separate hardcoded default viewer demo from the canonical dev-server default | `index.html:6`<br>`index.html:12`<br>`scripts/dev_server.config.json:5`<br>`scripts/serve_viewer.js:237-243` | Direct file/root opens and canonical dev-server root opens show different default demos, so QA screenshots can diverge by entry point. | Generate the root redirect from `scripts/dev_server.config.json`, or remove the hardcoded demo-specific redirect from `index.html`. |

## Findings By Area

### Rendering interaction (40)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F06 | Medium | Duplicated logic | 2D/3D geometry primitives are partly duplicated | Promote a shared 2D polygon predicate and vec/point validator API, then call it from model geometry, snap, and polygon modules. |
| F11 | Medium | Compatibility layer | Datum/grid geometry has hidden fallback axes despite the schema requiring explicit grid axes | Split strict datum evaluation from any legacy importer. Runtime rendering/snapping should require schema-valid axes; a migration/import path can convert old `rotation`-style grid data once. |
| F14 | Medium | Compatibility layer | Snap solver contains an active compatibility layer for candidate shape | Define one canonical snap-candidate DTO and either migrate all providers to emit it or move this normalizer to an explicitly named one-time/provider-adapter layer with diagnostics for legacy flat fields. |
| F16 | Low | Standardization | Secondary-web connection primitives can use either injected cached context or recomputed context | Make connection context creation an explicit pipeline step or shared runtime input, then remove the implicit "use cached if present, else recompute" branch from primitive helpers. |
| F22 | Medium | Compatibility layer | Axis relation project API directly parses rendering snap DTOs and carries grid-line defaults/aliases | Move snap-to-axis-source conversion into the interaction layer or a named adapter, then make project relations accept a canonical `source` object with explicit grid system, level, axis group, and axis id requirements. |
| F23 | High | API bypass | Snap selection bypasses the central Smart Component ownership/dependency API | Replace the local snap-selection traversal with `smartComponentObjectIds(project, instance)` from `dependencies.mjs`, plus a small wrapper that resolves `smartComponentId` to the instance. |
| F24 | Low | Fallback / hidden default | Member creation has a parallel `modelDefaults.collections.members` resolver instead of using the canonical effective-object merge rule | Extract a shared member-default/effective-new-object helper or make `createMemberObject` compose the same default merge contract before applying creation-only fields such as id, mark, endpoints, and authoring metadata. |
| F25 | Low | Fallback / hidden default | Member read APIs mix raw stored members and effective/default-merged members behind similar names | Name the distinction explicitly, for example `storedMemberById` versus `effectiveMemberById`, and route public reads through one effective-object API. Mutation helpers can still request stored objects, but their names should make that bypass visible. |
| F33 | Low | Compatibility layer | Profile catalog APIs keep two live catalog shapes: raw profile map and wrapped `{ profiles }` library pack | Split the shapes explicitly, for example `profileMapFromLibraryPack(pack)` and `projectProfileCatalog(project, libraryProfileMap)`, then make public lookup functions accept only the merged raw map. Keep any wrapped-pack adapter at import/load boundaries. |
| F36 | Medium | Fallback / hidden default | Work-plane API uses an implicit global-XY fallback as active geometry state | Treat global XY as a named default work-plane policy in viewer/editor state or project settings, and make callers pass an explicit default-plane request. Keep strict project reference-plane resolution separate from the UI fallback. |
| F41 | High | Compatibility layer | Trim operation evaluation still falls back from missing/unknown operation types instead of requiring the schema/API contract | Remove the runtime default or move it into a named legacy importer/migration. Evaluators should call the canonical trim operation type validator and throw on missing/unsupported `operation.type`. |
| F49 | Medium | Compatibility layer | Smart Component connection object creation is implemented twice with separate defaults | Extract a shared connection-object factory for Smart Component runtime that accepts a top-level or nested mode/options object, and make both creation paths use it. |
| F63 | Low | Fallback / hidden default | Datum/grid creation methods are misplaced inside the Smart Component store-method module | Move level/grid creation into a dedicated project datum/grid store module or into the central project command store next to other generic project object creation methods. |
| F67 | Low | Fallback / hidden default | Hole pattern annotation falls back to hardcoded `bolts.rows`/`bolts.columns` parameter paths | Require `rowsParameter` and `columnsParameter` in dimension specs for editable pattern count annotations, or move the bolt-path default into a named component-level preset before the generic handler runs. |
| F69 | Medium | Fallback / hidden default | Member-create interaction reimplements member profile/default fallback outside the creation API | Move the effective member-creation profile/default resolver into the project API/store layer, return explicit diagnostics when the project lacks required defaults, and let the interaction controller either call that resolver or pass only a user-selected... |
| F70 | Low | Contract drift | Member transform commits are assembled in the interaction controller instead of one store transaction/API | Add a store-level member transform operation/transaction that accepts the member operation plus relation intent and returns the committed project with changed/regenerated/removed ids. Reuse the same operation for live draft generation so preview and commit... |
| F71 | Medium | Compatibility layer | Member-edit tuning settings are read from `authoringSettings` but exist only as code fallbacks | Either add the intended fields to `viewer-settings.json` and `viewer-settings.schema.json`, or stop reading them as settings and move the constants into a named member-edit policy module with tests. |
| F73 | Low | Fallback / hidden default | Manual plate creation has UI-local defaults instead of inheriting project plate defaults | Add a project API/store helper for manual plate creation and preview drafting that resolves project defaults once. The controller should pass points, work-plane/placement intent, and user overrides, not default material/thickness/id policy. |
| F76 | Medium | Duplicated logic | Feature-body sketch editing is a parallel mutation adapter outside the normal plate-sketch store API | Add a project/store "sketch host" mutation API that accepts a plate object, sketch object, or feature-body target and returns one normalized mutation result. Rendering should call that host API instead of manually branching between plate store methods and... |
| F77 | Medium | Fallback / hidden default | Plate-sketch overlay styling and hit-target controls are pseudo-settings outside the viewer settings contract | Either add the intended overlay fields to `viewer-settings.json` and the schema, or move them into a named plate-sketch overlay style policy/constants module and stop reading them as runtime settings. |
| F78 | Low | API bypass | Plate-sketch snap logic creates persisted relation patches in the rendering layer | Move snap relation suggestion into a project/API relation policy layer or return explicit provenance metadata for snap-generated relations. Rendering can still request candidates, but the relation patch/spec should come from the same relation operation... |
| F79 | High | Compatibility layer | Plate-sketch edit thresholds are read as settings but are not in the settings contract | Promote these thresholds to validated authoring settings or move them to a named plate-sketch edit policy module with tests around edge-collapse and insert-drag behavior. |
| F80 | Low | Contract drift | Editing a box feature-body sketch silently converts the feature body to polygonal-prism | Route feature-body sketch edits through a feature body API that returns conversion metadata, or require an explicit "convert box to polygonal prism" operation before arbitrary outline editing. If silent conversion is intended, surface it in the UI/status and... |
| F81 | Medium | Duplicated logic | Plate-sketch relation action patch mapping is duplicated between the overlay helper and the controller | Extract a shared relation action spec/service that builds preview and commit patches from one table and supplies prompt requirements for value-based relations. |
| F82 | Medium | Compatibility layer | Reference-plane editor keeps `size` and invented extents fallbacks despite the extents-only contract | Remove runtime `size`/default-extents fallback from the editor once migrations are complete, or move it into an explicit one-time migration/import path. The editor should require contract-valid `extents`. |
| F83 | Medium | Fallback / hidden default | Pick-face identity uses multiple member/object aliases instead of one canonical target DTO | Define one pick-face target DTO, for example `{ collection, objectId, subId, ownerId }`, migrate producers to emit it, and keep any alias migration in one adapter before selection/snap code sees it. |
| F84 | Low | Fallback / hidden default | Manual sketch creation hardcodes project object defaults in the rendering controller | Add a project/API helper for manual sketch drafts or sketch creation defaults, then let the interaction controller submit only captured points/work-plane context. |
| F85 | Medium | Compatibility layer | Snap providers actively emit hybrid old/new candidate DTOs | Make provider helper functions return one canonical DTO shape, or introduce a named provider-adapter step and prohibit direct flat candidate emission outside it. |
| F86 | Medium | Fallback / hidden default | Reference-plane snap provider invents axes/spans outside the strict reference-plane contract | Route reference-plane snapping through the same strict reference-plane evaluator used by geometry/API validation, and keep any missing-extents migration outside normal snapping. |
| F87 | Low | API bypass | Snap provider registry is incomplete; construction providers are appended outside it | Register construction, active-reference, drag-guide, and composite providers in the same provider table or move the table out of this file if it is only documentation. |
| F89 | Low | Standardization | Snap diagnostics are built by two independent APIs | Move snap diagnostic DTO builders to one shared module, or export the solver diagnostic builder and use it from rendering-level filters. |
| F92 | Medium | Duplicated logic | Smart Component selection scope reimplements ownership traversal instead of using the project dependency API | Import or expose the project dependency/store helper to snap selection instead of maintaining a rendering-local Smart Component traversal. |
| F93 | Low | API bypass | Snap visibility policy hardcodes physical provider identity outside the provider registry | Put physical/visibility behavior in provider metadata or candidate visibility fields and let visibility policy read that metadata instead of duplicating provider ids. |
| F94 | Low | Compatibility layer | Work-plane creation helper lives under plate-sketch API | Move `workPlaneFromThreePoints` into `engine/api/project/work-plane.mjs` and reexport it from the plate-sketch barrel only for backward compatibility if needed. |
| F96 | Low | Fallback / hidden default | Member manipulator overlays duplicate coordinate-space defaults next to the shared member-axis helper | Make `memberManipulatorHandles` consume fully normalized axis specs from `member-axis-space.mjs`, or import `normalizeCoordinateSpace`/`coordinateSpaceLabel` and remove local defaults. |
| F98 | Medium | Compatibility layer | Snap overlay rendering ignores the candidate preview contract and reconstructs overlays from transitional fields | Make `snap.preview` the canonical overlay descriptor, and keep any legacy `sources` adapter in one bridge near snap result normalization. |
| F100 | Medium | Fallback / hidden default | Feature reference/basis evaluation is duplicated across renderer, fastener evaluator, and snap providers | Promote one shared `featureReferenceBasis(project, profiles, feature)` evaluator and make cutters, fastener evaluation, and snap candidate providers consume it or a deliberate preview adapter around it. |
| F102 | Low | Compatibility layer | Render mesh edge/tolerance policy is hardcoded in scene assembly | Introduce a named render geometry tolerance/crease policy, preferably derived from project/viewer settings or the CSG tolerance layer, and use it from scene assembly and annotation grouping instead of scattering numeric thresholds. |
| F110 | Low | Compatibility layer | WebGL picker hit tolerances are hardcoded separately from snap/viewer policy | Move picker tolerances into a named WebGL picking policy or pass them from the existing snap/viewer settings context so picker, snap visibility, and interaction profiles share one contract. |
| F117 | Medium | Fallback / hidden default | Authoring handle contracts are implicit across overlay generation, hit testing, and rendering | Add a small authoring-handle descriptor API that normalizes type/kind/default/tolerance fields, then make generators, hit testing, and rendering consume that normalized contract. |

### Viewer UI (25)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F09 | Medium | Fallback / hidden default | Shortcut setting fallback logic is duplicated | Import the existing keyboard shortcut helper in `panel-elements.mjs`. |
| F20 | Medium | Compatibility layer | Smart Component diagnostics still emit and display a legacy `parameters` alias while new APIs reject `parameters` | Migrate UI display to `parameterPaths`, add a temporary read adapter only if old project files need it, and stop emitting `parameters` from new runtime diagnostics. |
| F29 | Low | API bypass | Plate-sketch dimension APIs can silently downgrade requested driving dimensions to driven dimensions | Make redundancy downgrades explicit in the API result or move the policy into the relation-operation registry proposed in F28. If automatic downgrade is intended, return metadata/diagnostics saying the requested driving dimension was stored as driven;... |
| F35 | Medium | Compatibility layer | Trim operation member references keep scalar and array forms live at the same time | Choose one canonical shape per trim operation kind. For profile-cope, make arrays canonical and derive scalar first ids only in adapters that still need them, or migrate schema/store/UI to a tagged operation spec where scalar and multi-member operation... |
| F50 | Medium | Fallback / hidden default | Smart Component instance input accessors exist, but many callers still read raw `inputs` shapes directly | Promote this helper module into the canonical Smart Component input accessor surface, add any missing permissive/read-only helpers needed by UI preview, and migrate store/runtime callers off raw `inputs.*` reads where validation or null semantics matter. |
| F75 | Medium | Fallback / hidden default | Plate-sketch dimension edit/toggle workflow is duplicated between the overlay drag actions and the inspector panel | Extract a shared plate-sketch dimension action service for UI callers. It should own value parsing/default formatting, relation lookup after mutation, type-specific value/mode dispatch, and redundant/conflicted status mapping, while both overlay handles and... |
| F88 | Low | Fallback / hidden default | Snap tuning defaults are split across manager, provider, controller context, and settings | Put snap provider budgets and world-span defaults into the snap profile/settings contract, then pass a normalized config to providers. |
| F90 | Low | Fallback / hidden default | Snap profile defaults have two sources of truth | Make viewer settings contain the complete snap profile contract, or move defaults to one shared JSON/config module consumed by runtime and UI metadata. |
| F95 | Medium | Fallback / hidden default | Scene thumbnail renderer is a parallel mini-renderer with its own projection and styling | Either document this module as an intentionally approximate thumbnail renderer, or feed it shared camera/color settings from the viewer render pipeline. |
| F97 | Medium | Fallback / hidden default | Authoring overlay fallback settings are scattered and sometimes differ from viewer settings | Normalize authoring overlay settings once before rendering overlays, and make overlay modules read from that normalized object without local product-value fallbacks. |
| F107 | Medium | Duplicated logic | Camera direction-to-angle conversion is duplicated in QA view code | Import `cameraAnglesForDirection`/orientation helpers from `webgl-view-state.mjs` into QA bridge, and keep any QA-specific view names as data that uses the shared conversion. |
| F113 | High | Contract drift | Dimension hide-behind-geometry setting is ignored by the WebGL orchestrator | Resolve this through a named render-dimension policy, or at minimum return `settings.render.dimensions?.hideBehindGeometry === true` from the orchestrator helper. |
| F141 | Low | Duplicated logic | Plate sketch inspector duplicates public sketch geometry helper APIs | Import `sketchVertexPointMap` and `sketchEdgePoints` from `plate-sketch-relations-and-bends.mjs` and remove the local copies; if UI needs tolerant lookup, expose a named tolerant helper instead. |
| F142 | Medium | Fallback / hidden default | Create-plate-from-sketch UI action hardcodes plate defaults instead of delegating to the model/default API | Move the default thickness/id policy into the create-plate-from-sketch API or a shared authoring defaults module, then let the panel call the API with only the selected sketch id unless the user supplied overrides. |
| F143 | Low | Compatibility layer | Smart Component quick-parameter wrapper keeps a local catalog-options compatibility adapter | Use one spec-based catalog option helper for inspector and full Smart Component parameter UI, and let `uiQuickParameterEntries()` own the default limit unless a caller is intentionally overriding it. |
| F145 | Low | Contract drift | Generated property binding silently no-ops on unknown commit/action/command ids | Add a descriptor-binding validation mode or fail/log loudly for unknown commit/action/command ids at bind time. Intentional optional actions should be marked explicitly as optional/disabled instead of falling through optional chaining. |
| F146 | Medium | Fallback / hidden default | Custom-profile authoring defaults are owned by the inspector UI instead of the profile API/defaults | Move default custom-profile draft geometry, id prefix policy, and point-list parsing into a named profile authoring helper/API. The inspector should pass user edits or omit the id to inherit the store default. |
| F147 | Low | Fallback / hidden default | Grid level creation uses a UI-local next-elevation policy | Add a canonical project datum authoring helper such as `nextLevelDefaults(project, options)` and have both inspector UI and store/create commands use it explicitly. |
| F148 | High | Fallback / hidden default | Quick-action rendering bypasses the shared action descriptor validation used by nearby panel controls | Route `quickActions()` through the same descriptor-action normalization used by `actionButton()`/`descriptorActions()`, or make missing callbacks an explicit disabled-action state with validation. |
| F150 | Medium | Compatibility layer | Project properties panel owns a runtime compatibility layer for metadata aliases instead of a model/store metadata contract | Move project metadata alias normalization into a named migration/import or store-level accessor contract. The panel should read/write canonical fields only, or explicitly show legacy aliases as diagnostics rather than silently merging them into editable... |
| F151 | Low | Contract drift | Smart Component connection artwork is chosen by UI keyword heuristics instead of component metadata | Add explicit artwork/thumbnail metadata to the Smart Component preset/preview contract, or keep the generated preview service as the only preview source and remove keyword artwork inference. |
| F152 | Low | Compatibility layer | Smart Component connection parameter tabs are rewritten by runtime text heuristics despite an explicit `ui.tabs` schema | Make connection tab grouping explicit in component `ui.tabs` or schema-supported metadata. If backward compatibility is needed, isolate this as a named migration/normalizer and remove text guessing from normal runtime rendering. |
| F153 | Medium | Fallback / hidden default | Stair route-module domain rules are duplicated between the parameter UI and the stair solver | Move route module type definitions/defaults/field specs into stair-system component metadata or a shared component helper consumed by both UI and solver. The generic Smart Component parameter UI should render descriptors, not encode stair-specific domain... |
| F154 | Medium | Fallback / hidden default | Smart Component preview generation builds its own UI-side ProjectStore and member-pair resolver outside the viewer app API | Move preview orchestration and candidate-pair policy behind an engine/app preview API. The UI service should request a preview by preset/selection/context and render the returned preview state, not construct stores and pair candidates itself. |
| F155 | Low | Fallback / hidden default | Viewer render scheduler owns a separate project-object LOD estimator instead of using scene geometry metadata | Move the "which objects deserve detail" policy behind a scene/rendering metadata API, or add a shared object visual-bounds service consumed by both the scheduler and scene builder. Keep scheduler responsibilities to timing, invalidation, and patch strategy. |

### UI commands / metadata (20)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F04 | High | Compatibility layer | Planned commands are visible product surface with no real implementation path | Either hide unimplemented commands from runtime surfaces or centralize planned-command state/handler behavior from command metadata instead of registering bespoke no-op handlers. |
| F07 | Low | Duplicated logic | Inspector metadata duplicates formatting and object-reference action helpers | Extract shared `inspector-formatting` and `object-ref-actions` helpers under `bobercad/app/ui/commands/`, then make editable/support/main metadata use the same surface. |
| F08 | Low | Duplicated logic | Snap strength is defined in two places | Put strength ids/specs in one runtime-safe shared module and let UI add labels/descriptions around the shared ids. |
| F30 | Low | Fallback / hidden default | Plate-sketch relation inference is both implicit and explicit, with no single policy/provenance layer | Centralize sketch-relation inference policy. Creation/topology APIs should take a documented `inferRelations` policy, and inferred relation records should either carry provenance metadata or be produced only through a named inference operation whose result... |
| F34 | Medium | Duplicated logic | Trim operation type metadata is duplicated across engine, UI commands, and rendering icons | Create one trim operation spec registry with ids and engine semantics, then layer UI labels/icons/rendering-only metadata on top of those ids instead of redefining the list. |
| F37 | Medium | API bypass | Number formatting is reimplemented across core, rendering, and UI instead of using one formatting contract | Promote `formatNumber` as the shared primitive and add small named presets for control values, drafting dimensions, inspector values, and parameter labels. Local formatters should import those presets instead of reimplementing rounding. |
| F59 | Medium | Fallback / hidden default | Regenerated Smart Component commands can under-report changed objects | Either let the command registry derive ids for regeneration commands, or have Smart Component patch/reconcile return the full touched-id set and pass that complete set through command metadata. |
| F72 | Medium | Fallback / hidden default | Plate bend creation owns bend ids and default bend parameters in the UI controller instead of the project API | Move target-to-bend-id and default bend construction into the project bend API, for example `createPlateBendForTarget(plate, target, options)`, and let the UI controller pass only target/direction/user overrides. |
| F91 | Low | Fallback / hidden default | Snap scope defaults and labels have multiple partial sources | Define one snap scope spec with defaults, schema keys, labels, and collection mapping, then generate or import it in runtime, schema validation, and UI command metadata. |
| F99 | Medium | Fallback / hidden default | Bend relief effective-size rules are split between renderer, controller, inspector, and normalization | Add one `effectiveBendRelief`/`bendReliefSize` helper next to bend normalization and use it from renderer, bend controller, and inspector metadata. |
| F104 | Low | Fallback / hidden default | Boolean feature operation values are repeated across schema, UI, and scene adapters | Export one boolean feature operation registry/metadata object used by schema generation or schema-adjacent validation, inspector options, and render/evaluator operation dispatch. |
| F118 | Low | Fallback / hidden default | Ribbon section grouping is inferred from command-id substrings | Add explicit `ribbonSection` or `section` metadata to command specs, keep this inference only as a migration/default bridge, and validate section ids against `RIBBON_SECTION_ORDER`. |
| F119 | Low | Standardization | Generated command action names depend on UI labels | Generate action names from stable ids or define explicit `action` fields in the source metadata specs, then treat labels as presentation-only. |
| F120 | Medium | Fallback / hidden default | Data library descriptor default status is effectively unreachable with omitted config | Default `config` to `null`, or compute declaration from actual config keys such as `libraryId`, `version`, or `path`. |
| F121 | Low | Compatibility layer | Inspector section placement is inferred from ids, labels, and field shapes | Require explicit `placement` in section factories, or use named section-builder helpers that set placement while keeping this heuristic only as a temporary compatibility bridge. |
| F122 | Low | Standardization | Active-tool guidance is keyed separately from command metadata | Add optional guidance/hints to the command specs or a command metadata extension table validated against `commandPaletteSpecs()`. |
| F123 | Low | Fallback / hidden default | Grid inspector falls back from missing grid level ids to every project level | Treat missing `levelIds` as empty/unlinked in the inspector, or make the “all project levels” behavior an explicit project/grid default resolved before UI metadata. |
| F124 | Medium | Compatibility layer | Model search descriptors keep multiple semantic alias fields alive | Move semantic search extraction to a model metadata adapter that documents canonical vs legacy aliases, then remove aliases once project/schema migrations are complete. |
| F125 | Medium | Duplicated logic | Display mode normalization is duplicated between UI metadata and WebGL view state | Route WebGL display-mode normalization through the canonical view metadata contract, or move shared display/view normalization to a neutral module used by both UI commands and WebGL runtime. |
| F144 | Low | Duplicated logic | Feature editing has two parallel descriptor/commit stacks for the same store APIs | Define one feature-edit descriptor/action contract and let the compact object inspector and expanded feature editor select different subsets/views of that same contract, or route both namespaces through one adapter with shared patch construction. |

### Smart Components (31)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F05 | Medium | Duplicated logic | Validation helpers are copied across engine API modules instead of inherited from one validator contract | Add one shared validation module with domain error prefixes/context adapters, then migrate module-local required/optional helpers onto it. |
| F10 | Medium | Duplicated logic | Project model/object access helpers are duplicated across API, store, and Smart Component runtime layers | Promote one public project object-access module that owns collection registration, objectIndex validation, collection lookup, and typed `byId` behavior; let store and Smart Component helpers wrap that only for mutation-specific behavior. |
| F18 | Medium | Duplicated logic | Smart Component semantic builder has public methods and normalization rules outside the API register/store contract | Make `api-register.json` enumerate the real semantic builder surface, split read access from creation namespaces where possible, and route trim builder operations through the same normalization/validation helper used by project-store trim APIs. |
| F19 | Medium | Fallback / hidden default | Smart Component parameter values are resolved differently in UI and runtime, so derived values are reimplemented instead of inherited | Promote one Smart Component parameter resolver/derive schema used by UI, runtime build context, validation, and generator APIs. Derived display-only parameters should be explicitly marked as UI-only; generator-consumed derived values should resolve through... |
| F21 | Low | Contract drift | Transport sectioning exposes undocumented `ctx.*` methods and downgrades named strategies to count-based object batching | Register the `ctx.transport*` APIs explicitly, then either implement strategy-specific sectioning using split frames/stations or rename the current behavior as count-based grouping and keep station metadata out of the strategy contract. |
| F26 | Medium | Duplicated logic | Object/index collection helpers are duplicated with different missing-collection and cloning behavior | Export the collection registry and strict/patch-mode collection helpers from one project object module. Smart Component patch code can opt into a clearly named "create missing patch collection" mode, but it should inherit the same collection list and... |
| F39 | High | Compatibility layer | Fastener catalog access has both strict wrapped and loose raw-map shapes | Add one shared `fastenerCatalogEntries` helper in a runtime-safe catalog API module and make renderer/store/Smart Component code all use the same strict or explicitly versioned compatibility behavior. |
| F44 | Low | Contract drift | Reference-plane API documentation still advertises `size` although schema/runtime require `extents` | Update `api-register.json` to say `extents` only, and keep any historical `size` migration in a named importer rather than public API docs. |
| F46 | Low | Fallback / hidden default | Smart Component interface resolution applies a hardcoded geometry nudge outside the shared member-interface resolver | Move this connection-facing disambiguation into the shared member/interface geometry resolver as a named option or semantic side-selection rule, and replace the magic distance with an explicit tolerance/profile-scaled policy. |
| F47 | High | Duplicated logic | Smart Component source resolution is duplicated and ignores the stored `library`/`version` contract | Add one shared Smart Component source resolver in `smart-component-catalog.mjs` that validates `library`, `id`, and an explicit version policy. Make registry/store/scripts call that helper, and use a separate preset-id API for transient preset previews... |
| F48 | Medium | Compatibility layer | Smart Component creation splits preset and definition resolution across the caller boundary | Make `createProjectSmartComponentFromPreset` resolve the definition through the canonical catalog/source helper. Keep any test-only or preview-only definition override as an explicit, validated override that asserts `definition.type === preset.type`. |
| F51 | Medium | Duplicated logic | `setSmartComponentRoleActive` has a runtime implementation but the store reimplements it with different validation | Make the store commit through the runtime `setSmartComponentRoleActive` path, or move the shared role validation/toggle function behind one helper used by both runtime and store wrappers. |
| F52 | Medium | Contract drift | Smart Component outputs use suffix-based validation instead of a declared output contract | Add a declared output contract to Smart Component definitions and validate `ctx.output` against it. Keep the `Id`/`Ids` suffix rule only as a temporary lint/backstop, not the primary contract. |
| F53 | Medium | Contract drift | Runtime Smart Component definition validation is weaker than the JSON schema contract | Make runtime definition loading validate against the same schema or mirror the schema's top-level known-key set, including `$schema` policy. If JS-only fields such as `build` are allowed, isolate them as runtime-only extensions with an explicit allowlist. |
| F55 | Low | Duplicated logic | Smart Component catalog loading is not concurrency-safe | Replace the bare boolean with a cached `loadPromise` that is assigned before async work starts; subsequent callers should await that promise. Keep `loaded` only as derived state if still useful. |
| F56 | Low | Standardization | Id sanitization is split between Smart Component `safeId` and the project object API `cleanId` | Put id sanitization in one project id helper and make Smart Component generation call it with an explicit mode if it needs stricter ids. |
| F57 | Medium | Contract drift | Smart Component diagnostic display is implemented both in runtime overrides and the project store | Export one diagnostic display/apply helper from the Smart Component runtime layer and make the store call that helper when it has to mark reconciliation errors. |
| F58 | Medium | Fallback / hidden default | Smart Component store reconciliation performs hidden geometry repair after generation | Move the beam-to-trim fitting into the relevant Smart Component/connection primitive or expose it as an explicit model operation/solver result. The store should commit the result, not own geometry repair logic. |
| F62 | Medium | Fallback / hidden default | Smart Component deletion keeps a fallback removal algorithm that is weaker than the canonical helper | Remove the fallback and require `smartComponentRemovalObjectIds`, or export one canonical removal-id helper that both command store and Smart Component store methods must call. |
| F187 | High | Contract drift | Smart Component child-component DSL still uses `componentRef` as the nested component selector | Rename the child-component authoring config to a current field such as `presetId` or `sourceComponentId`, update builder/library generators/docs/contracts together, and keep any `componentRef` importer as an explicit migration adapter only. |
| F204 | Medium | API bypass | Smart Component data-library hardware generators bypass the shared fastener/hole derivation contract | Move fastener/hole diameter resolution into a shared Smart Component/runtime helper that accepts `fastenerRef`, tolerance, and optional custom diameter, then make library hardware generators call it. |
| F205 | Medium | Fallback / hidden default | Rolled centerline helper invents missing analytic path axes and center values | Normalize and validate the source path before constructing the rolled centerline, require explicit center/axes/angles for arc/helix inputs, and keep preview-only defaults in a named adapter. |
| F206 | Medium | Fallback / hidden default | Landing generator falls back from solved footprints and axes to rectangular defaults | Require a valid solved landing footprint and axes for generated landing geometry, or move the rectangular fallback into a named preview/import adapter with diagnostics. |
| F207 | Medium | Fallback / hidden default | Railing generator repeats hidden route-axis fallbacks and a direct fastener-hole path | Require solved railing station frames, route fastener/hole values through the F204 resolver, and keep preview-only axis defaults in a named degraded-preview adapter with diagnostics. |
| F208 | Medium | API bypass | Transport section generator bypasses a canonical Smart Component ownership/output API | Expose and use one runtime/library API for source owned objects, such as `ctx.smartComponentObjectIds(componentId)` or declared child outputs. |
| F209 | Medium | Compatibility layer | Stair-system child selection has a local component-family alias outside the register | Replace local `componentRefForRailing(...)` with a registered Smart Component family/preset resolver and migrate child selection away from raw `componentRef`. |
| F210 | Medium | Fallback / hidden default | Stair solver normalizes incomplete route geometry into generated defaults | Split default resolution from solving; require explicit route modules, station frames, landing geometry, and curved-flight data in the solver, with defaults only in preset/preview adapters. |
| F211 | Medium | Fallback / hidden default | Support generator falls back from solved support axes/elevation to defaults | Require explicit support segment lateral vectors and core elevation, use nullish checks for zero elevations, and keep rolled centerline validation strict through F205. |
| F212 | Medium | Fallback / hidden default | Tread generator falls back from solved tread frames to parameter/default geometry | Treat solved `layout.treads` frames as authoritative and move parameter/default fallback to an explicit preset/preview resolver before generation. |
| F213 | Medium | Contract drift | Smart Component config contracts still expose raw hole-diameter parameters beside the newer derived fastener-hole model | Standardize Smart Component config schemas on the derived fastener-hole contract, keeping direct diameter only as `customDiameter` or migration input. |
| F214 | Medium | Contract drift | Generated stair project provenance persists raw hole-diameter parameter paths beside generated hole patterns | Migrate generated fastener-group provenance paths to the derived fastener/tolerance/custom-diameter contract while keeping numeric `model.holePatterns.*.holeDiameter` as generated geometry output. |

### UI design / CSS (13)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F13 | Low | Compatibility layer | Left-dock data panels share DOM primitives but still duplicate panel lifecycle/search/focus scaffolding | Introduce a small data-panel controller/helper for query state, search rendering, focus restoration, and row lookup. Keep each panel responsible only for its domain rows/sections. |
| F126 | Low | API bypass | Design-system disclosure sections own workspace persistence | Split this into a pure `disclosureSection` and a shell-level `workspaceDisclosureSection` wrapper that wires storage/reset behavior explicitly. |
| F127 | Medium | Fallback / hidden default | Command enabled/disabled semantics are duplicated across UI command surfaces | Move command-state normalization into one shared command metadata/helper module and have all UI surfaces, including `applyCommandState`, consume the normalized contract. |
| F137 | Medium | CSS / selector issue | CSS selector escaping fallback is duplicated and inconsistent | Put selector escaping in one shell/design-system helper and use it from both modules. |
| F140 | Medium | Duplicated logic | Dimension overlay reimplements tooltip/control primitives outside the shared UI helpers | Extract overlay-compatible tooltip/control helpers or adapt the design-system primitives so dimension overlay can share invalid-state, button, tooltip, and keyboard behavior. |
| F149 | Low | Duplicated logic | Project data/files/properties panels duplicate the same panel controller/search/filter shell | Extract a shared data-panel controller/filter helper that takes namespace, dataset keys, section builder, and row matcher hooks; keep only domain row construction in each panel. |
| F156 | Medium | Compatibility layer | Viewer runtime infers trim-editor active state from DOM classes and title text | Replace `trimJointEditorPanelActive()` with an explicit trim editor API state, for example `trimJointEditorApi.isActive()` or an expanded `keepsSceneFocus()` contract that covers create-mode/card-active cases. |
| F157 | High | CSS / selector issue | Design-system command palette stylesheet ends with an unterminated media query | Either remove the stray media query if no mobile overrides are needed or add the intended mobile rules plus closing brace. |
| F158 | High | CSS / selector issue | Design-system panels/controls stylesheet ends inside an unclosed `.bc-keycap` rule | Add the missing closing brace and run a CSS syntax check; consider adding a repository check for balanced CSS blocks so shared stylesheets cannot end in open rules. |
| F159 | Medium | CSS / selector issue | Shell dock layout state is split across multiple CSS files instead of one standardized dock contract | Introduce one generic dock shell/body/rail selector contract for `data-workspace-panel-*` states, then keep `dock-tabs.css` and `inspector-dock.css` limited to content density and panel-specific visual overrides. |
| F160 | Medium | CSS / selector issue | Nav cube right-dock clearance logic is duplicated between CSS selectors and runtime JS | Put the occupancy policy behind one workspace/dock helper contract. Either expose a single data attribute such as `data-nav-cube-clearance-active` from JS and let CSS read only that state, or centralize the selector predicate with the same helper that... |
| F161 | High | CSS / selector issue | Smart Component browser preview tile hardcodes a light background outside theme tokens | Replace the raw hex with an existing theme token if one fits, or add a named preview/thumbnail background token to `tokens.css` plus both theme files. |
| F162 | Medium | CSS / selector issue | Viewer editor panels stylesheet keeps an unused trim inspector-context selector contract | Remove the unused `[data-inspector-context-panel="trim"]` selectors if they are stale, or add/register a real trim inspector slot through the same shell/panel metadata path if that mount point is intended. |

### UI shell / workspace (12)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F01 | Medium | Compatibility layer | Workspace storage keeps a legacy toolbar compatibility layer | If migration from the old localStorage/export format is no longer required, remove `hasLegacyModelingToolbar`/`legacyModelingToolbar`; otherwise move it to a named one-time importer and keep runtime storage current-shape-only. |
| F128 | Low | API bypass | Command palette recents use a separate browser storage path outside workspace preferences | Either move recents into the workspace preference envelope or document them as intentionally device-local ephemeral state with their own reset/migration boundary. |
| F129 | High | Fallback / hidden default | Dock tab reveal logic bypasses the panel reveal synchronization API | Expose a small shell-level `revealPanelDock(panel)` helper or callback from the workspace manager and have `dock-tabs` call it instead of writing panel datasets and button state directly. |
| F130 | Medium | Fallback / hidden default | Inspector dock reimplements dock-tab behavior instead of sharing the dock-tab controller | Factor a shared dock-tab core that accepts availability and panel-mount hooks, then implement normal data docks and inspector docks through that core. |
| F131 | Medium | Duplicated logic | Viewer overlay corner specs are duplicated across workspace modules | Import `VIEWER_OVERLAY_CORNER_SPECS` from `workspace-customizer-state.mjs` in commands/dialog modules instead of redefining it. |
| F132 | Medium | Fallback / hidden default | Workspace customizer label fallback uses a local `titleCase` instead of the shared labels helper | Import `titleCase` or a more specific label helper from `workspace-customizer-labels.mjs` and remove the dialog-local implementation. |
| F133 | Medium | Fallback / hidden default | Toolbar dock default is duplicated instead of sourced from the workspace state normalizer | Export a canonical toolbar dock default or use `normalizeToolbarDock()` wherever a default dock value is needed. |
| F134 | Low | CSS / selector issue | Workspace toolbar drag target is hardcoded to `#modeling-toolbar` | Scope drag target lookup to the supplied `toolbar` element, for example by checking `toolbar.contains(candidate)` or querying with a class selector relative to the manager instance. |
| F135 | Low | API bypass | Workspace ordering repeats the same list-move algorithms per collection | Extract two small pure helpers, e.g. `moveIdByDirection(ids, id, direction)` and `moveIdBefore(ids, sourceId, targetId)`, then wrap them with the collection-specific normalizers. |
| F136 | Medium | Duplicated logic | Workspace/id validators are duplicated across shell modules | Export a shared shell id validator, with named wrappers only where a clearer error/domain label is useful. |
| F138 | High | Compatibility layer | Workspace import uses a partial manual shape check instead of the UI workspace schema contract | Use the same schema validator for workspace import, or make the manual import predicate mirror the schema completely and reject additional properties before storage. |
| F216 | Low | Fallback / hidden default | Root `index.html` carries a separate hardcoded default viewer demo from the canonical dev-server default | Generate the root redirect from `scripts/dev_server.config.json`, or remove the hardcoded demo-specific redirect from `index.html`. |

### Rendering scene (6)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F38 | Low | Standardization | Feature cutter routing is split between the generic cut-body API and scene-only hole/slot cutters | Centralize feature-to-cutter routing in one registry/API, or make `cutBodiesForFeature` return an explicit "handled by scene feature cutter" result instead of an indistinguishable empty list for active cut features. |
| F40 | Medium | Fallback / hidden default | Feature reference basis resolution is duplicated between fastener rendering and feature cutters | Extract a shared feature-reference basis resolver under `engine/geometry` and have fastener evaluation plus scene feature cutters call it with their own error prefix/metadata needs. |
| F101 | Low | Fallback / hidden default | Render scene output carries the mutable project model into WebGL runtime paths | Return only render-scene metadata needed by WebGL, such as object collection lookup and preview member bases, or expose an explicit immutable scene model context instead of carrying the full project. |
| F103 | Low | API bypass | Member feature CSG application is implemented as a renderer-private pipeline | Move feature application into an engine geometry evaluator such as `evaluateMemberPolygons`/`applyMemberFeatures`, with scene adapters only converting evaluated polygons to faces/edges. |
| F105 | Medium | Contract drift | Plate solid feature application is renderer-private and bent plates have a separate unsupported path | Move plate solid evaluation, including flat/bent feature handling, into an engine geometry evaluator; keep the scene adapter responsible only for converting evaluated panels/polygons to faces and edges. |
| F106 | Medium | Fallback / hidden default | Fastener visual assembly dimensions are derived only in the scene adapter | Extend the fastener evaluator with an evaluated assembly/visual envelope DTO, or create a shared fastener geometry policy consumed by renderer and any export/analysis path. |

### Rendering WebGL (6)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F109 | Low | Fallback / hidden default | WebGL object preview mutates the live render scene as a parallel scene patch store | Define an explicit scene patch API/DTO owned by the render scheduler or scene builder, and keep preview controllers from mutating arbitrary scene collections/project state directly. |
| F111 | Low | Standardization | Instanced-member shader normal transform is malformed | Change the normal transform to `normalize(aAxisX * aLocalNormal.x + aAxisY * aLocalNormal.y + aAxisZ * aLocalNormal.z)` and add a render smoke check for instanced member shading. |
| F112 | Medium | Compatibility layer | WebGL shader/program creation is duplicated outside the program registry | Export a small shared WebGL program helper or let `webgl-programs.mjs` own text label program creation too. |
| F114 | Low | Compatibility layer | `appendWorldVertex` carries a dual calling convention compatibility overload | Split this into explicit helpers, for example `appendGroupVertex(group, point, rgba, pickRgba)` and `appendArrayVertex(positions, colors, point, rgba)`, so render-group and raw-array writes have one contract each. |
| F115 | Low | Contract drift | WebGL pointer controls hardcode trim-region click priority | Move click-priority policy into the picker/runtime interaction contract, for example as a supplied click-pick profile or click handler option, and keep `webgl-viewer-controls.mjs` responsible only for event sequencing. |
| F116 | Medium | Compatibility layer | WebGL large-scene threshold is duplicated as an unnamed constant | Move the threshold into one WebGL scene-performance policy, preferably with separate named fields if highlight and picking should eventually differ. |

### Connection primitives (5)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F12 | Low | API bypass | Fastener/hole diameter derivation is implemented in one primitive but bypassed in other connection primitives | Extract a shared fastener/hole derivation primitive used by all through-bolting generators, then let configs expose tolerance/custom diameter instead of raw diameter where appropriate. |
| F15 | Low | Compatibility layer | Fin-plate bolting diagnostics still reference the old/direct hole diameter parameter path | Replace `holes.diameter` diagnostic paths with `holes.tolerance` and `holes.customDiameter` where relevant, and finish standardizing all connection primitives on the same hole-diameter derivation contract. |
| F17 | Low | Fallback / hidden default | Connection primitive generators use both raw `ctx.param` and default/diagnostic-aware `ctx.parameterValue` for required parameters | Standardize generator parameter access: reserve `ctx.param` for explicitly raw reads, otherwise migrate required primitive parameters to `ctx.parameterValue`/`ctx.params` with clear required/default behavior. |
| F42 | Medium | Contract drift | Weld reference dispatch is a catch-all instead of a canonical reference-kind contract | Define a shared weld reference-kind registry/validator. `evaluateWeld`, semantic builders, schema, and store updates should all reject unknown kinds or route them through explicit adapters. |
| F43 | Medium | Fallback / hidden default | Local frame/up-axis fallback policy is implemented independently in member, path, and connection geometry | Promote a shared frame/basis resolver with named policies (`global-up`, `preferred-up`, `interface-z`, etc.) and make member/path/connection code call it instead of embedding local fallback chains. |

### Engine store (5)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F31 | High | Fallback / hidden default | Direct plate-sketch vertex moves silently remove touching equal-length relations | Move relation relaxation into the relation-operation policy layer from F28 and make mutation APIs return or emit metadata for removed/relaxed constraints. If silent removal is intentional for drag editing, it should be an explicit direct-edit option rather... |
| F32 | Low | Contract drift | Plate-sketch topology edits silently prune dependent bends and can change plate type | Return mutation metadata for pruned bend ids and type changes, or route topology edits through a shared dependency/mutation-result contract. If automatic bend pruning is required, make it explicit in store/API result payloads and UI status messages. |
| F60 | Medium | Duplicated logic | Store interface validation is weaker than `project.schema.json` | Replace manual interface validation with schema-backed validation or move the schema's conditional interface contract into one shared model validator used by both store and JSON checks. |
| F61 | Low | Fallback / hidden default | Store default-generation policies are hardcoded instead of centralized | Extract grid-axis and trim-operation default policies into named project/model API helpers with documented inputs. If these are UI convenience defaults, keep them out of generic store helpers and call them explicitly from UI-facing commands. |
| F64 | Medium | Duplicated logic | Trim joint store methods validate operations but not the top-level trim-joint schema contract | Add a top-level `validateTrimJoint` helper that enforces schema-level required fields, operation count, participant count, and known keys, and call it from `replaceTrimJoint` and create/update/remove trim paths. |

### Engine geometry (4)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F03 | Medium | Compatibility layer | Object trim region keys still carry legacy aliases | Migrate existing sample/project data to canonical region keys, then remove the alias path; until then, document it as an explicit compatibility layer. |
| F45 | Low | Fallback / hidden default | Trim region geometry silently substitutes a unit profile span when profile data is missing | Require a profile for trim geometry that depends on section size. If a UI marker really needs a fallback span, pass it explicitly as a named rendering fallback, not as the default behavior of `sectionMaxSpan`. |
| F66 | Low | Standardization | Hole diameter annotation text contains an encoding artifact | Normalize annotation text encoding and use a tested UTF-8 diameter symbol or an ASCII-safe product-approved prefix. |
| F68 | Medium | Fallback / hidden default | Annotation perpendicular-axis helpers are duplicated between drafting and dimension context modules | Import the exported drafting helpers into the dimension context, or move them into one annotation geometry helper used by both files. |

### Viewer dimensions (4)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F65 | Low | Fallback / hidden default | Dimension annotations have their own Smart Component role/interface/parameter resolution layer | Expose a read-only Smart Component annotation context/helper from runtime that resolves role objects, role interfaces, and parameter values once, and make dimension annotations consume that instead of manually walking project/model shapes. |
| F74 | Medium | Fallback / hidden default | Plate-sketch dimension overlay reads styling settings that are not in the viewer settings contract | Add these fields to the viewer settings schema/config if they are intended runtime settings, or move them into named rendering constants so they are not pseudo-settings. |
| F108 | Medium | Compatibility layer | Dimension label presentation is resolved separately in WebGL and DOM overlay paths | Extract a shared dimension label presentation resolver that returns text, font family/weight/size thresholds, hover color, and fallback color for both renderers. |
| F139 | High | API bypass | Dimension overlay rendering bypasses the Smart Component API accessor | Render dimensions through `api.smartComponent(smartComponentId)` or add an explicit nullable Smart Component accessor instead of reaching into `project().model`. |

### Engine project API (2)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F27 | High | Fallback / hidden default | Plate-sketch normalization bypasses effective `modelDefaults` and reintroduces required/defaulted plate fields | Make plate-sketch operations normalize against an effective plate view or a shared default resolver, then write only intentional object overrides. Replace hardcoded `S355` fallbacks with the project/material default path or require callers to pass a material... |
| F28 | Low | Standardization | Plate-sketch relation behavior is repeated across assert, diagnostic, rank-analysis, solver, and mutation code instead of coming from one relation operation registry | Introduce one relation-operation registry/spec per relation type that owns target ids, satisfaction residual, diagnostic message, rank equations, solver target choice, and optional solve projection. Then make analysis, solver, preview, and mutation modules... |

### Tooling / scripts (29)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F02 | Medium | Compatibility layer | Dev server accepts both canonical and legacy encoded branch URL prefixes | Remove `legacyEncodedBranchPathPrefix` once old links are not needed, or document the deprecation window. |
| F54 | Medium | Fallback / hidden default | Smart Component preview context ids are only strictly validated in a contract script, while runtime silently ignores unknown ids | Move known-context validation into runtime definition loading or share the preview-context registry with `defineSmartComponent`, and make unknown context ids fail at catalog load time. |
| F176 | Medium | Contract drift | Architecture line scan is documented as clean but is not part of the standard check gate and currently reports findings when run | Either wire `architecture_line_scan.js` into the standard check gate and keep generated docs current, or mark the architecture scan/audit artifacts as historical/manual-only. |
| F177 | Medium | Contract drift | Branch guard allows any non-main or detached branch, so fixed agent worktree branch assignments are not enforced | Extend the guard for the fixed local layout: require `codex/agentN` inside `C:\boberos\agentN`, keep `main` rules for `C:\boberos\main`, and allow exceptional bypass only when user-approved. |
| F178 | Medium | Standardization | Root repository contract checker still owns large hardcoded contract families instead of delegating them to focused contract suites | Keep `check_repo_contracts.js` as an orchestrator plus tiny shared helpers. Move required-file/layout, plate-sketch, app/evaluator boundary, and viewer snap/settings checks into focused `scripts/contracts/` modules. |
| F179 | Medium | Duplicated logic | Project semantic validation is duplicated between `check_repo_contracts.js` and `validate_domain_model.js` | Make `validate_domain_model.js` the canonical project semantic validator and remove or reuse the duplicate project checks in `check_repo_contracts.js`. |
| F180 | Medium | Standardization | Viewer runtime smoke test is one giant `main()` with repeated inline fixtures instead of focused runtime contract suites | Split viewer runtime checks into focused suites under `scripts/contracts/`, keep shared project/snap fixtures in `viewer_runtime_contract_helpers.js`, and leave `check_viewer_runtime.js` as a small orchestrator. |
| F181 | Medium | Standardization | Model authoring contract suite mixes model API, snap/rendering UI, manipulator, and generic API contracts under one module name | Split this file into focused suites such as `member_authoring_contracts.js`, `snap_contracts.js`, `manipulator_contracts.js`, `generic_model_api_contracts.js`, and `transport_sectioning_contracts.js`, then have the root checker orchestrate them. |
| F182 | Medium | Standardization | Smart Component lifecycle contract suite also owns preview rendering, scene visibility, and stair-system generator contracts | Split the suite into focused contracts for lifecycle/dependency cleanup, preview/thumbnail service, scene visibility/rendering, fastener/hole derivation, and stair-system generator/compliance/sample-health. Keep shared catalog/sample setup in a helper. |
| F183 | Medium | Standardization | Smart Component quick-property contract repeats the generated descriptor/action taxonomy as source-text token checks | Promote descriptor/action ids, generated field types, and shared CSS class contracts into exported constants or focused fixtures. Convert positive checks to behavior-driven descriptor generation/rendering tests and keep static forbidden-helper checks small. |
| F184 | Medium | Duplicated logic | Contract helper standardization is partial: shared helpers exist, but adjacent contract suites still define local copies | Promote common non-UI helpers into a general `contract_helpers.js`, keep UI helpers focused on UI fixture text, and migrate all suites to shared helper imports. |
| F185 | Medium | Standardization | UI workspace contract is still a giant detailed suite even after some sub-contract extraction | Keep `checkUiWorkspace` as a small orchestrator/context builder and split detailed assertions into focused command metadata, workspace storage, data dock, project data, feature navbar, bottom/settings strip, and inspector dock contract suites. |
| F186 | Medium | Standardization | Generated-panel UI contract is a second broad catch-all for generated properties, data panels, model browser, Smart Component browser, and design-system CSS | Split this suite into focused contracts for generated-properties renderer/bindings, panel-elements/design-system primitives, data-panel shared helpers, Model Browser, Project Files/Data panels, Smart Component Browser artwork, and viewer CSS/import wiring. |
| F188 | Medium | Standardization | Inspector object contract mixes object descriptor shape checks with broad generated-binding factory integration tests | Split into focused suites for object descriptor dispatch, plate/sketch/bend descriptors, feature editor descriptors, trim summary/editor routing, fastener/support-object descriptors, object references, and generated inspector binding factory routing. |
| F189 | Medium | Standardization | Left-dock shell contract is another broad UI integration catch-all under a narrow file name | Split into focused contracts for Smart Component browser, command palette, viewer left-dock runtime, focused editors, Member Transform, Inspector panel shell, Project Files/Data runtime, and Inspector Dock. |
| F190 | Medium | Standardization | Snap/view workspace contract mixes snap/view metadata with broad shell, customizer, CSS, and toolbar contracts | Split into focused contracts for snap command metadata, view/display orientation metadata, viewer settings strip, render visibility, NavCube shell/CSS, Feature Navbar, workspace customizer primitives, panel/tab customization, bottom/status strip, snap settings control, and toolbar command ordering. |
| F191 | Medium | API bypass | Stair sample generation uses two active creation paths for the same Smart Component family | Route gallery generation through the same project-store facade as per-variant generation, or add an explicit batch/gallery API that owns placement, metadata, update, validation, and final project extraction. |
| F192 | Medium | API bypass | Tread-variant sample generator bypasses the project store and reimplements Smart Component parameter defaults | Use the project-store facade for tread variant generation and a shared Smart Component parameter resolver for defaults/derived values, or expose a dedicated sample-generation API wrapping runtime creation, parameter resolution, metadata updates, and validation. |
| F193 | Medium | Duplicated logic | Stair sample generator scaffolding is duplicated across separate scripts | Extract a shared stair sample generation helper or CLI module that owns base project clearing, library loading, route variant builders, placement, metadata, store/runtime creation path, and output validation. |
| F194 | Medium | Contract drift | Local JSON schema validator ignores `propertyNames` even though the UI workspace schema uses it | Add `propertyNames` support and keyword-coverage tests to `validate_json_schema.js`, or replace the local subset validator with a full JSON Schema implementation. |
| F195 | High | Contract drift | Warehouse hall demo generator still writes the obsolete `model.connections` data path | Migrate the generator to the current Smart Component/store API and `model.smartComponentInstances`, replace `connectionIds`/`modelDefaults.collections.connections` with current object/Smart Component references, and run `scripts/check_repo.js` after regenerating. |
| F196 | Medium | Standardization | No-cache Python dev server bypasses the canonical worktree preview server contract | Retire this server, or turn it into a live-reload wrapper around `scripts/serve_viewer.js` that keeps `dev_server.config.json`, agent ports, branch prefixes, and replacement policy authoritative. |
| F197 | Medium | Standardization | Connection-view QA capture tool defaults to the non-canonical preview server path | Make this runner use `scripts/serve_viewer.js`/`dev_server.config.json` to derive the default viewer URL, or require `--viewer-url` when no canonical agent preview is already running. |
| F198 | Medium | Standardization | Stair QA runner hardcodes a non-canonical viewer URL and duplicates broad model validation | Accept the canonical viewer base URL from `scripts/serve_viewer.js` output or a required `--viewer-url`, and reuse `scripts/validate_domain_model.js` plus narrow stair-only QA checks. |
| F199 | High | Contract drift | Member-move stress benchmark still targets the obsolete connection API | Migrate it to `model.smartComponentInstances` and Smart Component dependency/store APIs, use `{ regenerateSmartComponents: false }` for pure move timing, and call `store.regenerateMemberSmartComponents(memberId)` for regeneration timing. |
| F200 | High | Contract drift | Eiffel Tower stress generator still writes obsolete connection data | Rebuild the generator around `model.smartComponentInstances`, connection zones, interfaces, and current store Smart Component preset creation; remove connection defaults, assembly `connectionIds`, and all `model.connections` reads. |
| F201 | High | Contract drift | Random-beam stress generator emits an empty obsolete `model.connections` collection | Remove `libraries.connections` and the empty `model.connections` field; put any no-connection stress metadata under `model.addonData.stressTest`. |
| F202 | High | Contract drift | Warehouse-hall stress duplicator reintroduces the obsolete `connections` collection around a current Smart Component sample | Remove `connections` from the collection prefix, model template, library patching, hall metadata, and console counts; count Smart Component instances or connection zones instead if needed. |
| F203 | Medium | Standardization | Interactive member-drag stress runner bypasses canonical preview and dependency setup | Require or derive a canonical `scripts/serve_viewer.js` worktree URL with branch prefix, and move browser automation setup into a documented repo script or checked bootstrap. |

### Docs / audit records (13)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F163 | Medium | Contract drift | Architecture audit docs report zero active architecture findings while the current manual standardization audit has active fallback/compatibility findings | Mark the June 20 architecture audit as historical/threshold-based, link it to this newer manual standardization audit, and explain that `architecture_line_scan.js` is not sufficient to prove absence of fallback, compatibility, duplication, or API-bypass issues. |
| F164 | Medium | Contract drift | Folder structure documentation still describes obsolete API/data-library locations | Refresh `folder-structure.md` from the current app/data layout and explicitly mark removed data-library UI paths as obsolete/not allowed. |
| F165 | Medium | Contract drift | Active Viewer MVP plan still describes plate rendering from `width`/`height` instead of the current sketch source-of-truth | Update the Viewer MVP plan to mark it historical or revise required behavior to render plates from `sketch` plus placement axes. |
| F166 | Medium | Contract drift | Active Authoring/NC1 plan contains stale validation and feature-field instructions | Revise the active plan to current terms: `featureIds`, `holePatternRef`, and `scripts/validate_domain_model.js` as the semantic quality checker wired through `check_repo.js`. |
| F167 | Medium | Contract drift | Active Smart Component generator plan still uses `componentRef` as a selection/composition contract | Replace `componentRef` language with current registered Smart Component/sourceComponent terminology and specify exact project fields used for child component provenance and lifecycle. |
| F169 | Medium | Contract drift | Active stair visual QA plan checks old/loose plate and fastener fields instead of current sketch/hole-pattern contracts | Update the QA checklist to require `holePatternRef`/hosted `model.holePatterns` for bolt positions and `plate.sketch` for plate shape checks. |
| F170 | Medium | Contract drift | Active snapping plan still describes missing split modules after the snap manager implementation landed | Update plan 0006 to reflect current module ownership, or intentionally create the missing modules and move code there. |
| F171 | Medium | Contract drift | Active Tekla roadmap treats implemented domain validation and stale-pattern guards as future/new work | Update plan 0008 so Month 1 work says "extend `scripts/validate_domain_model.js` and `check_repo.js`" rather than "add a skeleton/new check"; keep stale script cleanup as still-active until the referenced tool files are migrated or deleted. |
| F172 | Medium | Contract drift | Active Smart Component preview plan still names a planned engine preview service and `evaluateSmartComponentPreset(...)` API after the vertical slice landed under a different canonical store/UI path | Update plan 0009 to declare `store.previewSmartComponentFromPreset(...)` plus `createSmartComponentPreviewService(...)` as the current canonical preview path, or intentionally migrate the code to the engine-level service in one coordinated change with the API register and contracts. |
| F173 | Medium | Contract drift | Junior review documents are stale audit artifacts but are not marked historical/current-status-bound | Add a status header to both junior review files marking them historical inputs, point readers to the current manual audit/table and current contract scripts, and move fixed P0 items under "resolved/historical examples". |
| F174 | Medium | Contract drift | Validation documentation still describes domain validation as future work even though `check_repo.js` runs the domain and viewer-runtime gates today | Update `docs/quality/validation.md` so `check_repo.js` is documented as the canonical aggregate: branch guard, repo structure/contracts, domain model validation, and viewer runtime checks. Replace "future domain validation" with guidance to extend the existing validator/runtime checks. |
| F175 | Medium | Contract drift | Integrator workflow mixes the local file queue with GitHub PR label/PR operations in a single loop | Split the integrator loop into two explicit paths: local file workflow using `STATUS: READY` plus local status transitions around fixed worktrees, and GitHub PR workflow using labels/comments/PR merge steps. Avoid PR terminology in the local path. |
| F215 | Medium | Contract drift | Root filename/location audit artifact reports stale current coverage | Mark it historical or move it under an audit archive, and link to the current manual standardization audit/table. |

### Engine model / schema (1)

| ID | Priority | Type | Finding | Suggested action |
| --- | --- | --- | --- | --- |
| F168 | High | Contract drift | Member geometry has two active authoritative paths: `start/end` and analytic `centerline` | Decide the canonical member geometry contract and update every member API, evaluator, snap provider, editor, schema, and data-model rule around that one contract. |
