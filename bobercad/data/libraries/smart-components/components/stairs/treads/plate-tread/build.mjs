import { buildTreadSet } from "../shared/tread-set.mjs?v=timber-backing-plate-1";

export function build(ctx) {
  buildTreadSet(ctx, { family: "plate-tread", title: "Plate tread", color: "#6b7280" });
}
