import { buildTreadSet } from "../shared/tread-set.mjs?v=tread-tray-inset-1";

export function build(ctx) {
  buildTreadSet(ctx, { family: "pan-tread", title: "Pan tread", color: "#78716c", plateType: "pan-tread" });
}
