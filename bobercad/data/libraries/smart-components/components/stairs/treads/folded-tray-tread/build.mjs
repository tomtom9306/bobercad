import { buildTreadSet } from "../shared/tread-set.mjs";

export function build(ctx) {
  buildTreadSet(ctx, { family: "folded-tray-tread", title: "Folded tray tread", color: "#71717a", plateType: "folded-tray-tread" });
}
