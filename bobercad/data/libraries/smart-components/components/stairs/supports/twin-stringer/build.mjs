import { buildSupportSystem } from "../shared/support-system.mjs";

export function build(ctx) {
  buildSupportSystem(ctx, { family: "twin-stringer", title: "Twin stringer support", color: "#3f657d" });
}
