import { buildSupportSystem } from "../shared/support-system.mjs";

export function build(ctx) {
  buildSupportSystem(ctx, { family: "spiral-column", title: "Spiral column support", color: "#2f5c73" });
}
