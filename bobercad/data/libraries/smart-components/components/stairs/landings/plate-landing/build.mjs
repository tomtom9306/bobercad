import { buildLandingSet } from "../shared-landing.mjs?v=landing-footprint-1";

export function build(ctx) {
  buildLandingSet(ctx, { family: "plate-landing", title: "Plate landing", color: "#7f8795" });
}
