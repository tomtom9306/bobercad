# Validation

Validation should be mechanical where possible.

## Current Checks

Use:

```powershell
node .\scripts\check_repo.js
```

This checks:

- required folders exist
- required JSON files exist
- JSON files parse
- `$schema` references point to existing local files
- project JSON files validate against their local schemas
- reference geometry and point-cloud chunk JSON files validate against their local schemas
- translator/importer discovery smoke tests compare advertised reference geometry, point-cloud chunk, adapter request, and adapter config schema names/versions against the actual local schema files, verify adapter-config discovery exposes checked-in wrapper metadata for the DWG/DXF, CAD/OBJ, and E57/XYZ bridge scripts including supported external tool argument env variants, argument-template precedence/defaults, E57 point-text parsing env options, and emitted canonical object kinds, and production translator/importer/adapter-request contracts derive that supported schema identity from the same schema files instead of hardcoded literals
- translator/importer adapter-request smoke tests compare valid request summaries and written `schemaVersions` payloads against the actual local schema files instead of hardcoded version literals
- translator source-description smoke tests verify no-launch adapter registry matching uses normalized canonical source formats, so adapter configs that declare CLI-only aliases such as `e57pointcloud` still satisfy E57 source discovery while preserving declared-format metadata for wrapper review; source-description discovery, each `--describe-source` result, and source-decision metadata also advertise requested-format alias families so UI can classify DXF, DWG, STEP, IFC, E57, E57 point-cloud, and canonical JSON sources before any translator or adapter process can run
- production adapter emitters for CAD OBJ and E57 XYZ outputs are smoke-tested to reject hardcoded canonical `schema` names/`schemaVersion` literals, derive names from local schema files, and propagate versions from validated adapter request `schemaVersions`
- the E57 XYZ bridge smoke test verifies chunked point-cloud object metadata summarizes the emitted chunk `pointAttributes` fields, so adapter automation can audit RGB, intensity, classification, and normal preservation without reading every chunk sidecar
- reference geometry schema validation rejects unknown or duplicate `metadata.pointAttributeFields` entries before adapter automation trusts manifest-level point-attribute summaries
- reference geometry schema validation rejects empty `metadata.pointTextColumns` entries and negative `metadata.skippedCartesianInvalidPointCount` / `metadata.skippedSphericalInvalidPointCount` values before adapter automation trusts E57 point-text parsing summaries
- reference geometry schema validation rejects malformed known CAD/OBJ bridge metadata values such as empty OBJ source names, invalid uniform vertex colors, and unknown display color source tokens while keeping object metadata extensible for adapter-specific provenance
- canonical reference output, domain, browser-loader, and direct scene validation reject path-like `asset.source.fileName` values, path-like visible display names (`asset.name`, `layers.*.name`, `objects.*.name`), unsafe diagnostic `code`/`message` text, and object/point-cloud chunk `metadata` records that carry private/local paths, path-like keys such as `sourcePath`, raw payload keys such as `rawPayload`, path separators, URI-style values, overlong strings, or oversized nested structures; validation errors report the source-provenance, display-name, diagnostic, or metadata boundary without echoing private values
- reference geometry and point-cloud chunk schema validation rejects unsupported manifest or sidecar `schemaVersion` values before import or project audit paths can accept them
- reference geometry and point-cloud chunk schema validation rejects zero point-count chunk metadata before domain validation or viewer preview budgeting can rely on it
- reference geometry and point-cloud chunk schema validation rejects empty line, mesh, inline point-cloud, chunked point-cloud, and sidecar point payload arrays, line-set/mesh objects with too few vertices, mixed inline/chunked point-cloud storage, chunked point-cloud object-level attributes, and duplicate point-cloud `chunkIds`, before domain validation inspects cross-reference consistency
- reference geometry schema and viewer runtime validation reject canonical objects that carry geometry payload fields from the wrong object kind, so line sets cannot carry mesh/point-cloud payloads, meshes cannot carry line-set/point-cloud payloads, and point clouds cannot carry line-set/mesh payloads into app-side runtime data
- reference geometry and point-cloud chunk schema validation rejects empty point attribute arrays before domain validation checks 1:1 point/attribute alignment
- reference geometry schema validation rejects empty diagnostic `code` and `message` fields before adapter automation relies on diagnostic counts or codes
- reference geometry schema, translator, domain, and runtime validation accept diagnostic `objectRefs` arrays for grouped translator warnings, preserve them in import audit diagnostic summaries, and reject empty, duplicate, unsafe, missing, or filtered diagnostic object reference ids before adapter automation links diagnostics to canonical objects; importer `--check-references` reports layer/object map-key id mismatches, reference object id collisions with `project.objectIndex`, degenerate asset coordinate-system axes, invalid canonical source format provenance, invalid or malformed canonical source requested-format/file-extension provenance, invalid canonical source identity checksum/stat provenance, invalid canonical source translator/adapter provenance, invalid canonical source file-size provenance, invalid canonical source modified-time provenance, non-path-free canonical source fileName provenance, non-path-free canonical object metadata, non-path-free point-cloud chunk metadata, wrong-kind geometry payload fields, empty inline point-cloud point payloads, empty chunked point-cloud chunk id payloads, mixed inline/chunked point-cloud storage modes, chunked point-cloud object-level attributes, inline point-cloud pointAttributes that do not match point payloads, duplicate point-cloud object `chunkIds`, empty line-set segment payloads, short line-set vertex payloads, degenerate line-set segments, line-set segments that reference missing vertices, empty mesh face payloads, short mesh vertex payloads, degenerate mesh faces, mesh faces that reference missing vertices, duplicate manifest chunk ids, point-cloud references to missing manifest chunks, point-cloud chunk owner mismatches, point-cloud chunks with missing owner objects, point-cloud chunks with non-point-cloud owner objects, unreferenced manifest chunks, missing diagnostic object refs, and missing object layer references through path-free manifest-level `invalid-reference` errors while preserving canonical-reference repair routing
- reference geometry schema validation rejects empty asset, layer, and object display names before UI or audit summaries consume adapter output
- reference geometry schema validation rejects empty source provenance strings such as `fileName`, `checksum`, `translator`, and `translatorVersion`, rejects source `checksum` values that are not 64-character lowercase SHA-256 hex, and rejects path-like translator/version provenance before import/check summaries consume adapter output
- translator/importer discovery smoke tests verify the canonical output contract exposes source provenance fields and policy, including optional `checksum`, the canonical checksum pattern, required source `format`, the `stat-sha256` stat fingerprint pattern, the canonical translator provenance policy, the external-adapter policy that discards unverified adapter-emitted checksums, safe external-adapter translator/version provenance patterns, reserved translator tokens, version length limits, final-manifest `requestedFormat` alias families, and final-manifest source `fileExtension` families
- reference geometry translator smoke tests verify external adapter output cannot promote unsafe/path-like `asset.source.translator` or `asset.source.translatorVersion` provenance into the canonical manifest; unsafe translator values are replaced with `external:<adapterKey>` and unsafe version values are replaced with `external` during final normalization
- reference geometry schema validation rejects malformed source format provenance tokens such as dotted, uppercase, or whitespace-bearing `requestedFormat`, `fileExtension`, and adapter request `sourceFileExtension` values before adapter output reaches import/check summaries
- reference geometry schema validation rejects canonical manifest `asset.source.requestedFormat` tokens that do not belong to the declared `asset.source.format` family and rejects known-format `asset.source.fileExtension` tokens outside the declared source format extension family while preserving legal STEP, IFC, and E57 alias provenance
- importer `--check-references` filters cross-family canonical `asset.source.requestedFormat` and `asset.source.fileExtension` values out of public audit summaries and reduces schema `allOf/then` source-family failures to the same path-free source-provenance class message
- importer `--check-references` filters malformed/path-like canonical `asset.source.requestedFormat` and `asset.source.fileExtension` values out of public audit summaries and reduces token-pattern schema failures to the same path-free source-provenance class message
- importer `--check-references` reduces `asset.source.format: "unknown"` manifests that still carry `requestedFormat` through the same path-free source-provenance class message, including schema validators that phrase the failure as a forbidden schema match
- importer `--check-references` filters overlong canonical `asset.source.fileName` values out of public audit summaries and reduces source-basename max-length schema failures to the same path-free source-fileName class message
- importer `--check-references` filters unsafe canonical `asset.source.translator`, `asset.source.translatorVersion`, and `asset.source.adapterKey` provenance out of public audit summaries and reduces their schema failures to the same path-free source-provenance class message
- importer `--check-references` filters unsafe canonical `asset.source.statFingerprint` and `asset.source.checksum` identity values out of public audit summaries and reduces their schema failures to the same path-free source-provenance class message
- importer `--check-references` reduces path-like canonical asset display names to a path-free display-name class message without echoing the display label or raw `$.asset.name` schema text, filters unsafe layer/object display names out of `referenceLayerEntries` / `referenceObjectEntries`, and filters unsafe object/layer/chunk ids plus diagnostic object refs/codes/messages out of bounded public audit summaries before publishing them
- adapter request schema and standalone request validation reject `requestedFormat` tokens and non-empty `sourceFileExtension` tokens that do not belong to the canonical `format` family before external DWG/STEP/IFC/E57 wrappers can receive semantically inconsistent request payloads
- translator/importer discovery smoke tests verify the adapter request contract advertises the same `requestedFormat` and `sourceFileExtension` family policies and alias/extension groups for DXF, DWG, STEP, IFC, and E57, so UI and wrapper automation can inspect the rules without parsing the JSON Schema directly
- reference geometry schema validation rejects malformed source stat fingerprints before import/check summaries use cheap source identity comparisons
- reference geometry schema validation rejects unsafe canonical asset/layer/object/chunk ids and object-map keys, while the DXF translator smoke test verifies unsafe source layer names are sanitized into stable canonical layer ids
- project schema validation rejects unsafe `referenceGeometry.assets.*.path` values such as URLs, absolute paths, backslashes, query paths, empty segments, nested manifest directories, missing or uppercase `.json` manifest extensions, traversal, percent-encoded filename characters, encoded slash/backslash/traversal, malformed percent escapes, control characters, trailing whitespace, and references outside direct `../references/<manifest>.json` pointers before domain validation, reference audits, or viewer runtime loading inspect them
- the viewer runtime smoke test verifies direct `buildScene` reference asset entries reject malformed `projectAsset` pointer envelopes, including unsafe paths, unsupported fields, non-boolean visibility/snap flags, invalid display containers/keys, invalid display `visible`/`pointSize` controls, and malformed or degenerate transforms before reference asset stats or primitives are emitted
- the viewer runtime smoke test verifies direct `buildScene` point-cloud previews reject malformed object and preloaded sidecar point-attribute metadata, including non-object object `pointAttributes`, unsupported object `pointAttributes` keys, non-object `loadedChunks`, non-object chunk entries, unsafe or mismatched chunk wrapper ids, missing sidecar `$schema`, unsupported sidecar fields, non-object sidecar `pointAttributes`, unsupported sidecar `pointAttributes` keys, and non-object sidecar metadata before reference primitives or preview-stat object entries are emitted
- the viewer runtime smoke test verifies QA reference geometry runtime status and Project Files/source-search runtime summaries ignore duplicate or spoofed loaded reference assets whose wrapper id does not match the canonical `data.asset.id` or whose source format is not a supported canonical token, so injected runtime data cannot replace source format, label, object count, or chunk metadata for a safe project pointer
- the viewer runtime smoke test verifies QA reference geometry runtime status and Project Files/source-search runtime summaries count only canonical layer/object map entries and manifest chunk ids with safe matching ids, so unsafe map keys, mismatched ids, duplicate chunks, or malformed chunk ids do not inflate runtime metadata counts
- the viewer runtime smoke test verifies Project Files/source-search runtime reference diagnostics are grouped only by safe project reference asset ids and reduce unsafe diagnostic codes/messages to bounded machine-token summaries, so path-like adapter/runtime details cannot leak into source descriptors or search text
- the viewer runtime smoke test verifies source descriptor, workspace-response, response-envelope, and session-state format metadata filters source/requested/family tokens and requested-format aliases before keywords/search text, stage decisions, or UI summaries are built, and keeps requested-format aliases tied to the resolved canonical source family, so path-like or mismatched source-format aliases cannot leak into Project Files, command-palette search, or import panel state
- the viewer runtime smoke test verifies browser manifest loading and direct scene construction reject `requestedFormat` on source formats without a declared alias family and reject `fileExtension` values outside known source format extension families, so fallback formats such as `unknown` cannot carry arbitrary safe-looking requested-format tokens and known formats cannot carry mismatched source extension tokens through runtime metadata
- the reference geometry translator smoke test verifies the canonical reference schema rejects `asset.source.requestedFormat` when `asset.source.format` has no declared alias family, rejects known-format source `fileExtension` tokens outside the canonical source family, and the discovery contract reports those policies
- the viewer runtime smoke test verifies the QA reference geometry runtime status counts a selected preview chunk as loaded only when the loaded chunk wrapper id is safe and matches the selected chunk id, and verifies Project Files/source-search runtime summaries count only declared manifest chunks whose loaded wrapper id is safe and matches the declared chunk id, so unsafe or injected loaded chunk wrappers are not reported as ready overlays
- the viewer runtime smoke test verifies the QA reference geometry runtime status ignores injected loaded reference data when the matching project reference pointer is invalid, keeping unsafe project pointers not-loaded and pointer-only even if direct QA/runtime hooks provide a manifest payload
- the reference geometry importer smoke test verifies `referenceProjectPointerContract` publishes the same schema-guarded project reference asset id pattern, reserved object-key exclusions, required/optional asset fields, display schema fields, transform fields, transform default policy, path pattern, unsafe-path reject flags, and manifest schema/version/id alignment through `--list-import-discovery`, checks the discovery metadata against the actual `project.schema.json` and `reference-geometry.schema.json` definitions read by the importer, distinguishes importer-supported display overrides such as `edgeColor`/`pointSize` that rely on project display `additionalProperties` rather than named display schema properties, and states that transform defaults are importer/runtime policy rather than JSON Schema defaults
- the reference geometry importer check-references smoke test verifies malformed project JSON still returns machine-readable audit JSON with path-free project parse/schema class messages instead of echoing absolute project paths or raw parser text
- the reference geometry importer check-references smoke test verifies unsafe `referenceGeometry.assets` project map keys are replaced with `<invalid-reference-asset-id>` in audit JSONPath text and are not copied into summary-only asset entries or fingerprints when the raw key looks like a private local path
- the reference geometry importer check-references smoke test verifies unsafe project reference pointer metadata values are filtered before audit publication: absolute/private pointer paths are not echoed or statted, display and transform summaries keep only valid supported fields, unsafe extra asset/transform JSONPath keys use fixed placeholders, and private path tokens are absent from full audit JSON
- the reference geometry importer check-references smoke test verifies malformed canonical reference manifests, canonical manifest schema-check failures, unsupported canonical manifest schema/schemaVersion values, and project asset key versus manifest `asset.id` mismatches report path-free manifest read/schema-check/unsupported-schema/asset-id class messages in `readError` and `errors[]` instead of echoing raw parser text, missing schema file names, raw domain/schema-validation text, absolute manifest paths, or private temporary directories
- the reference geometry translator/importer smoke tests verify side-effect-free discovery exposes `referenceTargetFormatCoverage` with `referenceTargetFormatCoverageFingerprint` for the target `dxf`, `dwg`, `step`, `ifc`, and `e57pointcloud` tokens, including built-in versus external-adapter routing, adapter-request capability, CLI-only E57 point-cloud alias status, and canonical `.e57` picker metadata
- the reference geometry translator/importer smoke tests verify adapter registry and preflight output exposes `adapterTargetFormatCoverage` with top-level and nested `adapterTargetFormatCoverageFingerprint`, distinguishing configured optional adapters for built-in target formats from missing required external adapters for DWG and E57 point-cloud before any conversion process is launched and carrying that fingerprint into UI-safe adapter-preflight workspace response summaries with dependency counts/names but without full adapter entries or canonical geometry payload fields; the final adapter-preflight evidence verifier recomputes that coverage fingerprint and requires the selected adapter key to appear in the matching target-format coverage entry, the completion-evidence builder rejects saved preflight evidence whose top-level adapter config, registry, preflight, or coverage fingerprints disagree with the summary fields used for sourceAdapter proof maps, and downstream completion/final-proof verifiers reject saved final-acceptance checks whose green gates disagree with summary-level accepted family, external-adapter, point-cloud, or remaining-action evidence
- the reference geometry goal-completion audit rejects hand-edited final-proof bundle checks whose saved input report path fields or saved proof-bundle report path are local or otherwise not path-free report file names, even when the bundle gates, fingerprints, and summary evidence remain green, and its contract publishes those path-privacy fields plus the required `finalProofBundleInputPathsOk` and aggregate `finalProofBundlePathPrivacyOk` final-evidence tokens
- the reference geometry goal-completion audit reports path-only final proof bundle failures as `path-free final proof bundle report path metadata` in `summary.remainingAreas`, instead of claiming missing real DWG/E57 adapter evidence when accepted private adapter/sourceAdapter proof is already present
- the reference geometry goal-completion audit reports path-only final proof bundle failures with a dedicated `completionBasis` stating that accepted private proof content is present and path-free final proof bundle report path metadata is still required
- the path-only `completionBasis` is used only when the final proof bundle check fingerprint matches the expected fingerprint and the goal-completion audit output is writable; if the expected fingerprint also mismatches, the audit reports the fingerprint gate separately instead of letting path metadata mask it
- the reference import workflow-runner smoke test verifies project-free `adapter-preflight` preserves `adapterTargetFormatCoverageFingerprint` through the trusted host response, compact workflow `responseEntries`, and top-level `finalFingerprintSummary`, so UI automation can compare adapter target-format coverage without reading source/project files
- the UI workspace contract verifies app-side reference import metadata exposes the same UI-safe target-format coverage on the file-picker descriptor and static import command metadata without importing translator or importer modules into app runtime
- the UI workspace contract verifies app-side reference import metadata blocks invalid browser-entered import inputs and options, including unknown workflow stage ids, path-like explicit format tokens, explicit asset ids, selected adapter names, unit tokens, boolean policy flags, adapter timeout/chunk-size values, display overrides, and transform basis values, before command-plan, workspace-request, or session state can hand work to the host runner
- the UI workspace contract verifies app-side reference import workspace responses strip canonical geometry payload fields and canonicalize runtime-boundary metadata before stored session state can advance the workflow
- the UI workspace contract verifies app-side reference import workspace responses filter `workflowStatus` to known workflow stage ids, completed/next/recommended stage routing, decision/fingerprint field names, no-write/promoted-write booleans, and boolean-only side-effect boundaries without carrying local project/input/reference paths, adapter request/staging/log paths, source file/stat metadata, raw result data, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace response envelopes filter stage ids to known workflow stages, filter response statuses to known workspace response tokens, and rebuild `fingerprintSummary` plus public response summary fingerprint fields from known stage fingerprint fields and valid `sha256:<64 lowercase hex>` values without carrying malformed fingerprint values, unknown fingerprint keys, local paths, raw result data, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace response ids are filtered through safe-id helpers and descriptor metadata (`requestKind`, `commandId`, label/icon/kind, and descriptor ids) is derived from app constants rather than pasted host response strings before session state consumes the envelope
- the UI workspace contract verifies app-side reference import source format, requested-format family, alias, file-extension, file-picker token, and accept-extension fields are filtered to safe lowercase token values before workspace responses or response envelopes reach panel/session state
- the UI workspace contract verifies app-side reference import adapter ids are filtered to the same safe id shape as adapter config keys, preserving legal case-sensitive keys while dropping path-like, traversal, reserved object-key, and free-form values from preflight, adapter-request, output, promotion, failure, and audit count summaries
- the UI workspace contract verifies app-side reference import execution/workflow modes, translation modes, and adapter output modes are filtered to known contract tokens before workspace responses or response envelopes can drive panel/session routing
- the UI workspace contract verifies app-side reference import stage-decision readiness, replacement, adapter, validation, and write-boundary booleans are filtered to real `true`/`false`/`null` values before workspace responses or response envelopes can drive panel/session routing
- the UI workspace contract verifies app-side reference import source, plan, adapter-request, output, promotion, and failure summary booleans are filtered to real `true`/`false`/`null` values before workspace response summaries can drive panel/session routing
- the UI workspace contract verifies app-side reference import fix-area, failure-kind, audit status/severity, diagnostic severity/code, object-kind, adapter error-code, and adapter-output validation-kind fields and count-record keys are filtered to known or safe machine tokens before workspace responses or response envelopes can drive panel/session routing
- the UI workspace contract verifies app-side reference import response `assetId`, `requestedAssetId`, and `highestPriorityAssetId` fields use the same safe project reference asset-id guard as project pointers before workspace responses or response envelopes can drive panel/session state
- the UI workspace contract verifies app-side reference import response `referenceSchema`, `referenceSchemaVersion`, and `referenceUnits` fields are filtered through the published canonical reference geometry schema name, schema version, and unit set before workspace responses or response envelopes can drive panel/session state
- the UI workspace contract verifies app-side reference import response `sourceTranslator` provenance is filtered to the built-in translator id, `external:<adapter-id>`, or safe adapter machine-token values, and `sourceTranslatorVersion` is filtered to a short path-free version token before translated-output or promoted-import workspace responses can drive panel/session state; the Reference Import panel exposes that already-filtered external-adapter provenance in translated-output/import metadata without showing built-in translator paths
- the UI workspace contract verifies app-side reference import next-action routing accepts only the published action-token whitelist for `safeNextAction`, `recommendedNextAction`, session `nextActionToken`, and failed-response fallbacks, and that the whitelist covers all translator/importer producer next-action tokens while still excluding execution modes and failure-kind labels; path-like, traversal, or unknown adapter/translator strings cannot drive panel workflow routing
- the UI workspace contract verifies the Reference Import panel can recover a pasted workflow run from UI-safe `responseEntries` when final response envelopes are absent, before falling back to full legacy response objects
- the UI workspace contract verifies the Reference Import panel filters pasted workflow-run summary routing metadata through known run-status, stop-reason, blocked-reason, workflow stage, response status, action-token, and input-descriptor whitelists before showing blocked/final workflow state
- the UI workspace contract verifies app-side adapter-preflight `stageDecision` data is filtered to readiness, requested-format/adapter routing, selected adapter keys, diagnostic counts/codes, fix-area routing, next actions, and fingerprints without carrying adapter config paths, resolved dependency paths, full adapter entries, diagnostic objects, stdout/stderr text, or canonical geometry payload fields into panel state; its dependency summary keeps only safe declared relative file/directory/command tokens and valid env names, rejecting absolute, drive-rooted, URL-like, query/fragment, or traversal dependency values
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referenceSourceSummary` data for source-discovery results, including source format family, requested-format aliases, import mode, adapter requirement, safe first execution mode, file-picker extension metadata, and discovery fingerprints while filtering source-discovery `stageDecision` data to source readiness and routing fields without carrying local input paths, source directories, file names/stems, source stat metadata, rich format catalog objects, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referencePlanSummary` data for plan-only results, including project-pointer readiness, source requested-format aliases, translation mode, adapter request capability, adapter config/preflight state, safe next execution mode, available next execution modes, and plan/preflight/registry fingerprints while filtering generic plan-only `stageDecision` routing data without carrying project, input, reference, output, or staging paths into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referenceAdapterRequestSummary` data for adapter-request results, including request/stage readiness, adapter key/output mode, source requested-format aliases, adapter preflight readiness, safe next action, and request/registry/preflight fingerprints while filtering generic adapter-request `stageDecision` routing data without carrying request paths, source/project/reference paths, stage/scratch/chunk/log paths, adapter request payloads, schema path maps, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referenceOutputSummary` data for dry-run/import translated canonical outputs, including format alias family, schema/units, bounds, object/layer/chunk/primitive counts, diagnostic counts, object-kind counts, and fingerprints while filtering dry-run `stageDecision` data to no-write validation/routing fields without carrying canonical `layers`/`objects`/`chunks`, vertices, faces, points, loaded chunk payloads, project/input/reference/output paths, staging paths, or absolute output paths into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referencePromotionSummary` data for confirmed import results, including project-pointer/write readiness, promoted manifest validation, chunk sidecar readiness, source requested-format aliases, adapter request fingerprint, promoted fingerprints, and audit routing while filtering import `stageDecision` data without carrying project, input, reference, output, adapter run, staging, log, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `referenceAuditSummary` data for check-references audit results, including overlay readiness, ready/needs-attention/error counts, aggregate primitive counts, fix-area/status routing, source/object kind count maps, and audit fingerprint while filtering check-references `stageDecision` data to audit routing fields without carrying `assets`, `referenceAssetEntries`, `referenceNeedsAttentionEntries`, `referenceAuditErrorEntries`, `errors`, paths, or geometry payload fields into panel state
- the UI workspace contract verifies app-side reference import workspace responses expose UI-safe `failureDecision` and `referenceFailureSummary` data for failed importer/adapter results, including failed stage, stable adapter/import error code, failure kind, fix-area routing, retry stage, boolean fix gates, safe fingerprints, source-format aliases, and error count without carrying raw `errors`, human messages, adapter stdout/stderr, local project/input/reference/output paths, stage/log paths, or geometry payload fields into panel state; adapter-preflight failures recovered from primary JSON errors preserve `adapter-preflight` retry routing with `fix-adapter-preflight` as the safe next action while keeping the specific adapter config/dependency recommendation
- reference geometry schema validation rejects point-cloud chunk sidecar paths that are absolute, Windows-drive based, backslash based, or contain `..` traversal segments
- local schema validation enforces tuple `items: false`, including project reference transform vectors, so extra coordinate components are rejected by schema-only checks
- local schema validation enforces `format: "date-time"` for canonical reference and adapter-request timestamp metadata
- reference geometry adapter config JSON validates against its local schema, including mutually exclusive adapter `format` / `formats` declarations before adapter discovery or preflight output can select a local converter
- the reference geometry translator smoke test emits canonical JSON from a DXF fixture and imports it into a temp project copy as a pointer-only asset
- the reference geometry translator smoke test verifies built-in DXF, STEP, and IFC outputs emit object-level bounds for generated canonical reference objects
- the reference geometry translator smoke test verifies STEP and IFC presentation-layer mesh and point assignments are promoted to source-derived canonical mesh/point layers instead of requiring app-runtime source layer inference
- the reference geometry translator smoke test verifies STEP and IFC mapped mesh source representation layer assignments survive `REPRESENTATION_MAP` / `MAPPED_ITEM` and `IFCREPRESENTATIONMAP` / `IFCMAPPEDITEM` transforms as source-derived canonical layers
- the reference geometry translator smoke test verifies STEP and IFC presentation-layer assignments directly on `MAPPED_ITEM` / `IFCMAPPEDITEM` mesh, curve, and point instances are promoted to source-derived canonical layers without duplicating generic output
- the reference geometry translator smoke test verifies STEP and IFC presentation-layer assignments directly on `REPRESENTATION_MAP` / `IFCREPRESENTATIONMAP` propagate to mapped mesh, curve, and point source items as source-derived canonical layers
- the reference geometry translator smoke test verifies IFC `IFCPRESENTATIONLAYERASSIGNMENT` on an `IFCSHAPEREPRESENTATION` propagates to contained curve linework as a source-derived canonical layer instead of requiring app-runtime IFC layer inference
- the reference geometry importer smoke test verifies requested units, visibility, snap policy, display overrides, and project transform are preserved on pointer-only imports
- the reference geometry importer smoke test verifies project reference asset pointer keys use the same safe canonical id shape as canonical reference manifest ids
- the reference geometry importer smoke test verifies project schema rejects canonical reference payload fields such as `layers`, `objects`, `chunks`, `vertices`, `faces`, and `points` inside `referenceGeometry.assets`, keeping project JSON pointer-only even after a canonical manifest is translated
- the reference geometry importer smoke test verifies successful built-in, external-adapter DWG/STEP/IFC mesh, external-adapter E57/e57pointcloud point-cloud, canonical JSON, pretranslated canonical, and replace-existing imports preserve the existing project `model` and `objectIndex` unchanged while adding or refreshing only pointer metadata under `referenceGeometry.assets`
- repository architecture contracts verify app runtime modules under `bobercad/app` do not import reference geometry translator, importer, adapter wrapper, or adapter contract tooling from `tools/reference-geometry`, keeping canonical JSON manifests as the app boundary
- the reference geometry importer smoke test verifies `--plan-only` validates new imports and replace-existing reimports without running translators/adapters or writing project JSON or target reference manifests, reports registered external-only formats as `external-adapter`, preserves source requested-format family metadata from source discovery in the top-level summary and compact `referenceImportPlanDecision`, reports `referenceImportContractVersion`, `referenceImportExecutionMode`, `referenceImportSideEffectPlan`, planned import name, explicit units override, point-cloud chunk size override, safe next-mode/recommended-action routing, and `referenceImportPlanFingerprint`, validates supplied adapter config/format/adapter selections through the translator preflight path, reports stable aggregate and selected-adapter preflight and registry fingerprints plus `adapterPreflightDecision`, and fails mismatched adapter selections as machine-readable JSON with matching plan/error `adapterPreflightDecision` before writing files
- the reference geometry importer smoke test verifies `--plan-only` resolves selected adapter `requiredFiles` and `requiredDirectories` through `{adapterConfigDir}` without launching the external adapter or writing project/reference files
- the reference geometry importer smoke test verifies `--dry-run` validates new imports, external-adapter outputs, and replace-existing reimports with translator output validation, reports translated manifest/artifact fingerprints and source requested-format family metadata from the temporary output, but does not write project JSON or target reference manifests
- the reference geometry importer smoke test verifies `--json` prints machine-readable import, plan-only, adapter-request, built-in dry-run, and external-adapter dry-run results with `referenceImportContractVersion`, `referenceImportExecutionMode`, `referenceImportSideEffectPlan`, input path, translation mode, planned import options, `referenceImportPlanDecision`, adapter-request `referenceImportAdapterRequestDecision` metadata for request readiness/stage paths/adapter preflight readiness/side effects/wrapper next actions, dry-run `referenceImportDryRunDecision` metadata for validated-output/no-write/import-next-action routing including source requested-format family, import `referenceImportPromotionDecision` metadata for project-pointer/manifest/chunk promotion including source requested-format family, promoted-vs-translated fingerprint matching, and audit next-action routing, `referenceImportPlanFingerprint`, project reference visibility/snap/display/transform pointer metadata, adapter config file metadata including stat fingerprint, plan-only and request-only adapter preflight summaries including `adapterPreflightDecision`, cheap source file metadata and stat fingerprint for plan-only, null translated manifest/artifact fingerprints before translation modes, translated manifest/artifact fingerprints for import and dry-run modes, canonical reference schema/version, canonical reference units, asset bounds, source coordinate system, canonical reference manifest and point-cloud chunk sidecar file metadata when promoted, total manifest chunk point count, total line segment count, total mesh face count, total point-cloud point count across inline and chunked storage, null promoted reference and chunk file metadata for plan-only and dry-run, source format, source file extension, requested source format token and alias family, source file stat metadata and stat fingerprint, selected external adapter key, built-in source checksum when verified, external source translator provenance with unverified adapter checksums omitted, diagnostic counts, prefixed reference object/layer/chunk counts, and prefixed reference object kind counts for automation without changing import semantics
- the reference geometry importer smoke test verifies project reference pointers reject embedded canonical geometry payload fields and import-runtime fields such as adapter request, stage, scratch, chunk, log, source, output, and target-manifest paths inside `referenceGeometry.assets`, and that importer discovery exposes those forbidden field families beside the pointer-only storage policy
- the reference geometry translator/importer smoke tests verify successful external-adapter `--json` results report launch correlation and command/cwd metadata (`adapterRequestFingerprint`, `adapterRunId`, `adapterCommand`, `adapterCommandFound`, `adapterResolvedCommand`, `adapterCwd`, and `adapterCwdExists`) while redacting the exact adapter run id and avoiding launch-metadata fields in canonical manifests
- the reference geometry importer smoke test verifies importing an already-translated canonical JSON manifest reports `translationMode: "canonical-json"` while preserving the manifest's original `sourceFormat` provenance such as `dxf`
- the reference geometry importer smoke test verifies `--check-references` rejects stale canonical `object.bounds`, stale inline or chunked point-cloud bounds, unverifiable chunked point-cloud bounds, and stale or unverifiable canonical `asset.bounds` as `invalid-reference`, routes the audit decision to `likelyFixArea: "canonical-reference"` and `recommendedNextAction: "fix-canonical-reference-output"`, keeps the reference overlay out of the ready path before UI/render use, and reports the manifest-level bounds failure through a path-free project-asset message instead of a local manifest path or raw domain text
- the reference geometry importer smoke test verifies `--check-references` reports project asset key versus manifest `asset.id` mismatches as `asset-id-mismatch`, routes the audit decision to `likelyFixArea: "project-pointer"` and `recommendedNextAction: "fix-project-reference-pointer"`, keeps that pointer repair path separate from canonical translator-output repair, and reduces public mismatch errors to path-free project-asset messages instead of local manifest paths or raw domain text
- the reference geometry importer smoke test verifies `--check-references` reports project reference pointers outside the active references directory as `outside-references-dir`, routes the audit decision to `likelyFixArea: "project-pointer"` and `recommendedNextAction: "fix-project-reference-pointer"`, and avoids reading or summarizing those external files
- the reference geometry importer smoke test verifies `--check-references` reports contained but missing project reference manifests as `missing-manifest`, routes the audit decision to `likelyFixArea: "project-pointer"` and `recommendedNextAction: "fix-project-reference-pointer"`, and avoids exposing canonical manifest metadata for files that do not exist
- the reference geometry importer smoke test verifies `--check-references` reports project reference assets without a usable `path` as `missing-path`, routes the audit decision to `likelyFixArea: "project-pointer"` and `recommendedNextAction: "fix-project-reference-pointer"`, and avoids canonical manifest reads when there is no safe manifest location
- the reference geometry importer smoke test verifies targeted `--check-references --asset-id` audits report absent selected reference assets as `missing-asset`, route the decision to `likelyFixArea: "project-pointer"` and `recommendedNextAction: "fix-project-reference-pointer"`, and repeat the requested asset id as the highest-priority audit target
- the reference geometry importer smoke test verifies contained malformed reference manifest JSON is reported as `read-error`, routes the audit decision to `likelyFixArea: "canonical-reference"` and `recommendedNextAction: "fix-canonical-reference-output"`, and avoids exposing canonical manifest metadata when the file cannot be parsed
- the reference geometry importer smoke test verifies contained readable JSON with an unsupported schema is reported as `unsupported-schema`, routes the audit decision to `likelyFixArea: "canonical-reference"` and `recommendedNextAction: "fix-canonical-reference-output"`, and avoids summarizing that file as reference overlay geometry
- the reference geometry importer smoke test verifies unsafe or missing point-cloud chunk sidecars are reported as `missing-chunks` with path-free top-level chunk path/missing-file class messages, existing but invalid point-cloud sidecars with malformed JSON, schema-check failures, schema violations, identity, ownership, point-count, point-attribute, manifest/sidecar bounds, or sidecar/payload bounds mismatches are reported through `referenceChunkFileInvalidCount` / bounded invalid entries while keeping the asset in `invalid-reference`, route the audit decision to `likelyFixArea: "canonical-reference"` and `recommendedNextAction: "fix-canonical-reference-output"`, reduce top-level sidecar errors to path-free reason-class messages, and keep bounded missing/invalid chunk entries for adapter repair
- the reference geometry importer smoke test verifies audits with both project-pointer failures and canonical-reference failures route to `likelyFixArea: "mixed"` and `recommendedNextAction: "fix-reference-audit-findings"` while preserving highest-priority needs-attention ordering, and that `--summary-only` preserves the same mixed routing, needs-attention ordering, bounded errors, and audit fingerprint without full per-asset summaries
- the reference geometry importer smoke test verifies failed `--json` imports exit nonzero with `ok=false` JSON on stdout, expose `referenceImportContractVersion`, expose planned import context, execution mode, side-effect plan, and `referenceImportPlanFingerprint` when the project reference candidate can be built before the failure, propagate translator rollback recovery metadata, and do not rewrite project JSON
- the reference geometry importer smoke test verifies checked-in bridge raw-config errors such as invalid CAD/OBJ vertex-color-layout settings fail before adapter request fingerprint creation or adapter process exit metadata, propagate adapter preflight diagnostics and stable adapter preflight fingerprints into the JSON error, and do not rewrite project JSON or promote a target reference manifest
- the reference geometry translator/importer smoke tests verify failed external adapter processes, including importer external-adapter dry-runs, adapter commands that fail to start, timed-out translator/importer CLI runs, and adapters that exit successfully without writing their staged manifest, expose structured JSON error metadata with adapter name, adapter config file metadata including stat fingerprint, expected adapter output path, scratch directory, scratch file count/size/latest modified-time metadata, bounded scratch file entries, omitted-count metadata, start error code, exit code/stdout/stderr excerpts for process failures, or timeout metadata for hung adapters, without promoting target manifests or rewriting project JSON
- the reference geometry importer smoke test verifies `--keep-stage-on-error --json` preserves failed external adapter staging files and reports the staged adapter request path and scratch directory for inspection while still refusing to rewrite project JSON or promote invalid reference manifests
- the reference geometry translator/importer smoke tests verify `--keep-stage --json` preserves successful external adapter staging files and reports `adapterRequestFingerprint`, `adapterRunId`, exact scratch/request/output/log/stdout/stderr paths, and scratch file metadata, including bounded scratch file entries and omitted counts, for inspection while still respecting normal target promotion and importer dry-run no-write semantics
- the reference geometry translator/importer smoke tests verify invalid staged external adapter manifests keep adapter identity, adapter config file metadata including stat fingerprint, adapter request path, adapter log path, and adapter output path in JSON errors, clean temporary adapter staging unless `--keep-stage-on-error` is requested, preserve keep-stage debug files including adapter logs when requested, and refuse target promotion
- the reference geometry translator/importer smoke tests verify real missing adapter dependencies are reported in `--json` errors as structured `adapterMissingRequiredFiles` / `adapterMissingRequiredDirectories` / `adapterMissingRequiredCommands` / `adapterMissingRequiredEnv` metadata with selected-adapter preflight diagnostics/fingerprints, without exposing environment variable values, and target manifests/project JSON remain untouched
- the reference geometry translator/importer smoke tests verify external adapter JSON errors include stable `adapterErrorCode` values for missing adapter configs, adapter process failures, missing dependencies, invalid staged output manifests, and stale canonical bounds (`adapter-output-bounds-invalid`) plus bounded `adapterOutputValidationMessage`, `adapterOutputValidationPath`, and `adapterOutputValidationKind` detail, so automation can branch and display useful wrapper failures without parsing human-readable messages
- the viewer runtime smoke test verifies browser-side reference geometry URL guards accept normal `../references/<manifest>.json` project pointers and manifest-local chunk sidecars, while rejecting project pointer paths with nested manifest directories, missing or uppercase `.json` extensions, percent-encoded filename characters, absolute URLs, backslashes, query/fragment paths, and plain or encoded traversal segments before any fetch is attempted; it also verifies project reference asset ids, pointer paths, visibility/snap flags, display overrides, and transform metadata must use safe canonical id tokens, pointer-only project fields, safe direct `../references/<manifest>.json` paths, valid types, colors, opacity, point sizes, no unsupported display override fields, finite origins, positive scales, finite non-zero axes, non-degenerate bases, and no unknown transform fields before manifest fetch, and fetched reference manifests, manifest layers, manifest objects, manifest point-cloud chunk entries, and fetched point-cloud chunk sidecars must use non-empty `$schema` refs, supported canonical schema/version values with matching safe asset ids, required asset/layer names, valid source format provenance with matching requested-format alias family, valid optional source provenance fields without unknown keys, RFC3339 source `modifiedTime` values matching the schema `date-time` boundary, canonical top-level `layers`/`objects` maps and `chunks`/`diagnostics` arrays, canonical diagnostic severity/code/message entries with safe in-manifest object refs and no unknown fields, manifest-prechecked layer/object ids and refs, valid layer/object display metadata, finite object payloads, valid line/mesh indices, exclusive point-cloud storage modes, kind-specific object payload fields, and complete verifiable `asset.bounds` before the manifest is accepted for rendering, runtime-retained diagnostics that still match the canonical diagnostic shape, runtime-retained diagnostic object refs that still match accepted runtime objects, unique safe manifest chunk ids before runtime chunk indexing, direct runtime chunk indexing that ignores unsafe chunk ids before lookup, directly built runtime data that rejects point-cloud objects referencing duplicate manifest chunk ids, directly built scene data that rejects manifest identity, schema, source metadata, bounds, stale or unverifiable payload bounds, diagnostics, chunks, unit, container, layer/object metadata including invalid canonical display values, layer/object map-key/id, and invalid asset coordinate system or project transform mismatches instead of accepting fallback metadata, direct asset-bounds verification that treats duplicated referenced chunk ids, invalid object payloads, and invalid referenced runtime layers as unverifiable, manifest chunks owned by point-cloud objects that list the chunk id, runtime-retained chunks that remain reachable from accepted runtime point-cloud objects, supported asset units, finite non-degenerate asset coordinate systems with no unknown fields, finite non-inverted asset/object/chunk bounds with no unknown fields, referenced chunk entries that pass the same point-cloud chunk kind/path/pointCount/bounds guards before chunked objects with declared bounds are retained, safe chunk paths, positive integer point counts, required finite sidecar bounds aligned with manifest and payload bounds, complete manifest chunk bounds for declared chunked object or asset bounds, and supported point attribute lengths before chunks or geometry are loaded
- the viewer runtime and left-dock contract smoke tests verify the Files panel and command palette expose safe `project.referenceGeometry.assets` as pointer-only Reference Geometry file rows without requiring manifest reads or storing references in `model` / `objectIndex`, while filtering unsafe ids, URLs, traversal paths, backslashes, and encoded separators before creating source links
- the viewer runtime smoke test verifies the Files panel source metadata can merge runtime reference load status and bounded diagnostic code summaries into safe pointer rows without returning canonical geometry payloads
- the reference geometry translator/importer smoke tests verify real imports fail before adapter launch when the configured adapter `cwd` is missing, reporting `adapter-cwd-missing`, `adapterCwd`, and `adapterCwdExists: false` without promoting target manifests or rewriting project JSON
- the reference geometry translator/importer smoke tests verify real imports fail before adapter launch when the main adapter command cannot be resolved, reporting `adapter-command-missing`, `adapterCommand`, and `adapterCommandFound: false` without writing staged output or project JSON
- the reference geometry translator/importer smoke tests verify adapter process, dependency, timeout, missing-output, and invalid staged-output failures carry source file metadata plus `adapterRequestFingerprint` when a request was staged, `adapterRunId`, `stageDir`, `scratchDir`, scratch file metadata including bounded entries and omitted counts, `adapterRequestPath`, `adapterLogPath`, `adapterStdoutPath`, `adapterStderrPath`, `adapterCommand`, `adapterCommandFound`, `adapterResolvedCommand`, and `adapterCwdExists` metadata so wrapper crashes and nonzero exits can be debugged without reading local adapter config files or preserved stages
- the reference geometry importer smoke test verifies `--replace-existing` refreshes an existing canonical reference manifest without duplicating asset ids or losing project pointer metadata
- the reference geometry importer smoke test verifies invalid `--replace-existing` project metadata fails before the existing project JSON or canonical reference manifest is rewritten
- the reference geometry importer smoke test verifies invalid project reference transforms with zero or degenerate axes fail before the existing project JSON or canonical reference manifest is rewritten
- the reference geometry importer smoke test verifies timed-out external adapters fail before project JSON or target reference manifests are rewritten, including machine-readable `--json` timeout metadata for automation
- the reference geometry importer smoke test verifies `--check-references` audits already-attached project reference assets, reports audited project file stat metadata, reports `referenceAuditContractVersion` and `referenceAuditLimits` and includes both in the audit fingerprint, reports project reference visibility/snap/display/transform pointer metadata plus stable per-asset `referenceAuditStatus` / `referenceReady` values with `referenceAuditSeverity` and numeric `referenceAuditStatusRank`, exposes top-level `referenceAuditStatusDefinitions` for rank/severity discovery, supports `--summary-only` output that omits the full `assets` object and local `projectPath`/`referencesDir`/`resolvedPath` fields while preserving the project file stat metadata, bounded audit index/needs-attention index/aggregate/error summary/fingerprint/errors and `referenceAuditDecision` readiness/fix-area/next-action routing and is rejected outside check-only mode, reports top-level ready/needs-attention/status/severity counts, bounded top-level `referenceAssetEntries` with pointer display/transform metadata, bounded top-level `referenceNeedsAttentionEntries` for non-ready reference assets sorted by descending audit rank then asset id, top-level `referenceAuditErrorCount`, bounded `referenceAuditErrorEntries`, top-level `referenceAggregate` totals/counts and `referenceAuditFingerprint` for selected assets without local resolved paths, proves identical missing-manifest summary-only audits keep the same fingerprint across different local workspace roots, emits path-free missing-manifest project-pointer errors before building audit fingerprints, bounded object/layer/chunk id samples, bounded object/layer summary entries, bounded diagnostic entries, and canonical reference schema/version/source coordinate system/manifest file stat/content fingerprint/point-cloud chunk sidecar stat fingerprint/bounded present chunk sidecar entries/complete artifact fingerprint/source file extension/source requested format/source requested-format family/source file stat/source stat fingerprint/source adapter/source translator/verified checksum when present/diagnostic/reference object/layer/chunk summaries only for contained supported reference manifests with safe chunk sidecar paths, supports single-asset filtering and custom `--references-dir` audits, reports manifest id mismatches as machine-readable JSON without translating new sources or writing project files, refuses to read or summarize existing pointer targets outside the active references directory, refuses to summarize contained non-reference JSON as canonical reference geometry, refuses to stat noncanonical chunk sidecar paths, and reports bounded missing and invalid chunk sidecar entries with chunk id, manifest path, and reason for unsafe, missing, or invalid sidecars
- the reference geometry importer smoke test verifies `--check-references --asset-id <missing>` reports a path-free missing-asset project-pointer error and that matching `--summary-only` missing-asset audits keep identical fingerprints across different local workspace roots without publishing local project/reference path fields
- the reference geometry importer smoke test verifies `--check-references` treats canonical `asset.source.format` / `asset.source.requestedFormat` as provenance only by auditing equivalent manifests as ready across DXF, DWG, STEP/P21, IFC/IFCZIP, E57/E57 point-cloud, and canonical JSON tokens
- the reference geometry translator/importer smoke tests verify `--list-formats` and `--list-format-groups` print machine-readable per-token and canonical-group format registry JSON without requiring source input, output paths, project paths, or project input paths; importer discovery also exposes the import contract version, allowed execution modes, side-effect plans by mode, token-specific execution/side-effect plans for mixed groups such as built-in `ifc` versus external-adapter `ifczip`, and a stable `referenceImportDiscoveryFingerprint`; importer `--list-import-discovery` exposes the combined per-token and grouped discovery catalog with supported schema versions including adapter config, absolute local schema paths, canonical output contract metadata and fingerprint, standalone adapter output validation contract metadata and fingerprint, shared source-description contract metadata and fingerprint, import command contract metadata with mode flags, required inputs by mode, UI-safe import policy gates, translation-mode side-effect variants, project pointer fields, output fingerprint availability, JSON success/error fields, and fingerprint, project pointer contract metadata with pointer-only no-geometry storage policy, path containment policy, display/transform fields, manifest alignment, runtime/audit validation fields, and fingerprint, audit contract metadata with `--check-references` flags, required/optional inputs, no-write/no-translator/no-adapter side-effect guarantees, status definitions, output limits, full/summary field sets, audit entry fields, and fingerprint, importer result JSON contract metadata, workflow status field, fingerprint fields, schema identity field lists, JSON error envelope fields, optional failed-plan context fields including workflow status, primary error metadata fields, workflow contract metadata with staged order, command flags, required inputs, public decision fields, fingerprint fields, decision/fingerprint policy, side-effect boundaries, no-project-or-target-write stages, promoted-write stages, workflow status fields, failure status policy, and fingerprint, stage-level `referenceImportWorkflowStatus` output on source discovery, plan-only, adapter-request, dry-run, import, and check-references success results plus incomplete-stage workflow status on JSON failure contexts that can build a safe import plan, adapter request schema/version/path, required request fields, optional adapter-context fields, source identity fields, stage path fields, schema contract fields, placeholder/default/stage metadata, adapter config schema/field/placeholder/output/default/format-token metadata, adapter preflight contract metadata with `--check-adapters` flags, no-launch/no-write side effects, dependency check fields, diagnostic codes, and fingerprint fields, result/workflow/adapter-output-validation/source-description/import-command/project-pointer/audit/adapter request/adapter config/adapter preflight contract fingerprints, a compact decision summary for file-picker/built-in/external-adapter/canonical-json/mixed-mode/CLI-only classification including one compact `filePickerGroups` row per canonical source family, and an aggregate fingerprint
- the reference geometry translator/importer smoke tests verify app-side `referenceGeometryImportFilePickerDescriptor()`, `referenceGeometryImportInputDescriptors()`, `referenceGeometryImportCliBlueprints()`, `referenceGeometryImportAdapterPreflightDescriptors()`, `referenceGeometryImportResultDescriptors()`, `referenceGeometryImportWorkflowDescriptor()`, `referenceGeometryImportSourceDecision()`, `referenceGeometryImportReadinessPreview()`, `referenceGeometryImportActionPreview()`, `referenceGeometryImportCommandPlanDescriptor()`, `referenceGeometryImportCommandPlan()`, `referenceGeometryImportWorkspaceRequestDescriptor()`, `referenceGeometryImportWorkspaceRequest()`, `referenceGeometryImportWorkspaceResponseDescriptor()`, `referenceGeometryImportWorkspaceResponse()`, `referenceGeometryImportSessionDescriptor()`, and `referenceGeometryImportSessionState()` metadata mirrors importer discovery file-picker groups, accept strings, CLI-only aliases, source requested-format family shortcuts, adapter/canonical JSON classifications, UI-safe gate order, workflow stages, command flags, required inputs, input descriptor ids, adapter-request artifact descriptor ids, required/optional CLI flag blueprints, adapter preflight no-launch/no-write side effects, dependency diagnostic codes, registry/preflight decision fields, JSON success/error envelope fields, failure decision fields, primary error fields, per-stage fingerprint availability, decision/fingerprint fields, side-effect boundaries, source-extension routing, safe next actions, stage availability states, panel action states/tokens, primary action stage routing, action runtime boundaries, argv-only command plan descriptors, concrete argv/template generation for plan-only, adapter-preflight, adapter-request, dry-run, and check-references stages, missing input reporting, invalid optional import value blocking for adapter timeout, point-cloud chunk-size, display opacity/color/point-size, and transform vector/scale/basis inputs, no shell-string generation, workspace request envelopes, host result-routing fields, command-host no-shell/no-human-output parse boundary, missing-input blocking, promoted-write confirmation blocking, parsed-JSON-only workspace response normalization, success/failure/missing-JSON/host-error/request-blocked response statuses, stage/failure decision routing, fingerprint summaries, explicit stdout/stderr text ignoring, app-side session stage selection including source-discovery to adapter-preflight routing for external-adapter sources, failed check-references audit session review routing with resubmittable no-write summary-only requests, next workspace request generation, dry-run-before-import blocking, write-confirmation state, and safe/promoted write classifications without importing translator or importer tooling into app runtime, launching adapters, or parsing human-readable importer output; UI workspace contracts also verify `model.referenceGeometry.import` is an available References command that opens the Data Dock `Import` tab, the runtime mounts `reference-import-panel`, and the command derives its import/input/CLI-blueprint/adapter-preflight/result/command-plan/workspace-request/workspace-response/session/action-preview metadata from those app-side descriptors while leaving importer execution with the workspace command host
- the UI workspace contracts verify the Reference Import panel can collect adapter key, adapter timeout, point-cloud chunk size, source unit override, references-dir, asset display name, replace-existing state, visibility/snap overrides, display overrides, transform placement metadata, and summary-only audit options, expose a project-free/source-free `Preflight` stage for adapter dependency checks, copy raw workspace request JSON, copy a one-shot `--request-json-base64` host-runner command, copy a full no-browser workflow command with `--options-json-base64`, block workflow/request copy and show invalid option fields when browser-entered timeout, point-cloud chunk-size, display, or transform values fail app-side shape checks, allow workflow command copy for read-only `check-references` from a project path without requiring source input, paste parsed host-result JSON or a full `referenceGeometryImportWorkflowRun`, prefer host `responseEnvelope` / workflow `finalWorkspaceResponseEnvelope` for UI-safe pasteback, preserve a UI-safe workflow run summary including blocked stage and missing input ids, display a UI-safe source-discovery summary for source format and adapter gating, display a UI-safe adapter-preflight summary for missing local command/file/directory/env dependencies, display a UI-safe plan summary for project-pointer readiness and next execution mode, display a UI-safe adapter-request summary for request readiness and adapter identity, display a UI-safe translated reference output summary for dry-run/import counts and source format, display a UI-safe reference audit summary for overlay readiness and ready/needs-attention counts, display a UI-safe failure summary for importer/adapter failure routing, normalize workspace responses through app-side metadata, keep response actions live while editing, include its stylesheet in the viewer bundle, and stay browser-only without translator/importer imports, CLI execution, adapter launches, source-file reads, or human-output parsing
- the reference geometry importer smoke tests verify `scripts/run_reference_import_workspace_request.mjs` executes whitelisted `referenceGeometryImportWorkspaceRequest` envelopes through argv-only, no-shell child processes from either request files or base64 request JSON, returns paste-safe `referenceGeometryImportWorkspaceResponse` JSON plus UI-safe `responseEnvelope` for source-discovery without raw `resultJson`, stdout/stderr text, or debug paths by default, keeps raw full host response fields behind explicit debug opt-in, refuses blocked requests before launch, repeats the promoted-write confirmation fuse by blocking manually edited import/write requests without `writeConfirmed`, classifies promoted import argv independently from tampered safe-looking request metadata, and rejects tampered non-importer argv entrypoints as `host-error` without starting a child command
- the reference geometry importer smoke tests verify `scripts/run_reference_import_workflow.mjs` accepts UI-style `--options-json-base64`, forwards unit override, point-cloud chunk-size, asset name, visibility/snap, display override, and transform import options into generated workspace-host argv, drives source-discovery, plan-only, and dry-run through app-side session state plus the trusted workspace command host, routes external-adapter source-discovery through no-launch `adapter-preflight` before project-backed `plan-only`, stages an external adapter request when the target is `adapter-request`, stops external-adapter sources such as DWG, IFC ZIP, and E57 point cloud with `adapter-config-required` after source discovery when no adapter config is supplied, reports blocked stage/input summaries for that stop, preserves source requested-format family metadata and UI-safe source/preflight/plan/adapter-request/output summaries in compact workflow `responseEntries` for source-aware stages, can start directly at project-free/source-free `adapter-preflight` and read-only `check-references`, preserves summary-only mixed audit routing and needs-attention ordering through failed host responses, keeps failed audit workflow `safeNextAction`, `recommendedNextAction`, and `referenceAuditFingerprint` available in response entries, summarizes every request/response without browser execution, exposes UI-safe `responseEnvelopes` and `finalWorkspaceResponseEnvelope` for panel pasteback without `resultJson`, host-runner logs, stdout/stderr text, raw `responses`, raw `finalWorkspaceResponse`, full request argv, or canonical geometry payload fields by default, exposes only allow-listed host `hostRunner.argvFlags` and workflow request `argvFlags` option names in default compact entries, rejects mismatches between a workspace request `stageId` and the actual importer argv stage before launching a child command, drops flag-like data values from `argvFlags`, keeps raw full host responses and full request argv behind explicit debug opt-in, performs no project/reference writes in the default dry-run, adapter-preflight, or audit-only targets, and refuses to submit the promoted import stage unless `--confirm-import` is provided
- the reference geometry importer smoke tests verify `scripts/check_reference_import_corpus.mjs` exposes a side-effect-free `--list-contract` for private corpus CI, including supported workflow stages, config case fields, safe tag pattern, workflow-option fields, expected assertion fields, required-format coverage fields, source-file stat fields including `statFingerprint`, adapter config file/support fields including path-free `statFingerprint`, performance fields, selection fields, config-preflight fields, global acceptance gates, acceptance-decision fields, bounded report fields, execution boundary, and write-safety policy; the same smoke tests verify `--print-example-config` emits a private corpus template with required DXF/DWG/STEP/IFC/E57 coverage, large-file tags, adapter placeholders for external DWG/E57 cases, and size/time acceptance gates without requiring a private corpus file; the same smoke tests verify `--check-config-only` validates fixture corpus source-file evidence, required adapter config files, adapter config JSON/schema identity and stat fingerprint, selected adapter existence and format-family support for DWG/E57 cases, per-case and global source-size expectations, coverage, tag selection, dry-run/default write safety, and optional promoted import-write requirements without launching workflow cases, translators, or external adapters, leaves `acceptance.accepted` false, reports only bounded adapter config metadata without adapter commands/args/env values, and reports missing private source files, missing adapter configs, invalid adapter configs, unsupported selected adapters, source-size failures, dry-run cases selected for promoted-import acceptance, and blocked import-write gates through bounded `configPreflight` fields; the same smoke tests verify the harness can run fixture-backed DXF, STEP, and IFC dry-run corpus cases through `scripts/run_reference_import_workflow.mjs`, supports `--tag` / `--exclude-tag` filtering before coverage and workflow execution, publishes selected/skipped case counts and skipped ids in `selection`, publishes a bounded `referenceImportCorpusCheck` report with per-case process/workflow status, source format family, source file size/extension/stat-fingerprint metadata without local paths, duration and performance duration fingerprints, translation mode, primitive counts, workflow source adapter config/registry/preflight fingerprints, path-free top-level config/runner metadata, coverage summary, performance summary, acceptance summary, global `--min-source-file-size-bytes` assertions, optional `--require-promoted-import-writes` assertions, and assertion results, writes the same report through `--output`, supports `minSourceFileSizeBytes`, `maxDurationMs`, and promoted-import acceptance gates, marks accepted reports only when required format coverage is satisfied, at least one case ran, all cases passed, and source/performance evidence is present, keeps raw workflow envelopes and host responses out of the corpus summary, defaults to no promoted writes, advertises the explicit import-write gate for private real-file corpus acceptance runs, runs a confirmed promoted-import corpus case against a disposable project copy to prove pointer-only project writes plus promoted canonical manifest writes, and fails before launching workflow cases when required source families such as DWG or E57 are missing or when promoted import writes are required but selected cases are still dry-run; the same smoke tests verify `scripts/verify_reference_import_corpus_report.mjs` exposes a side-effect-free saved-report contract, accepts bounded accepted dry-run, promoted-import, and synthetic DWG/E57 external-adapter plus E57 point-cloud evidence reports, and rejects saved reports that leak absolute top-level corpus paths, carry inconsistent or unsafe coverage metadata, carry inconsistent defaults metadata, carry inconsistent or unsafe top-level runtime/write-safety metadata, carry inconsistent or unsafe top-level case id/count/selection metadata, carry accepted-case workflow runtime-boundary evidence that allows shell or browser-side writes or carries extra local/runtime fields, carry accepted-case workflow response summaries that did not end in a succeeded final response with aligned response counts, carry accepted-case workflow summaries with extra local/debug fields, carry accepted-case workflow stage evidence that did not reach the declared target stage, carry request/workflow start-stage mismatches, carry extra accepted-case request-summary fields, carry extra workflow source-summary fields, carry invalid source format token or alias summaries, carry invalid reference-count summaries, carry invalid final fingerprint summaries, carry invalid source extension or modified-time evidence, carry accepted-case errors/stderr/stdout-parse payloads, carry invalid accepted-case process signal/timeout/error summaries or extra process-summary fields, carry invalid accepted-case request summaries or raw-response debug requests, are missing required source families, whose per-case workflow/source-file/coverage source families disagree, whose performance case-duration evidence does not match accepted case ids/source stat fingerprints, below a requested production source-size threshold, lack nonempty reference geometry, lack E57 point-cloud evidence, lack promoted-import evidence, or lack required external-adapter adapter id/config/preflight fingerprint evidence for selected source families without rereading private source files or launching workflows/adapters
- the reference import workflow-runner smoke test verifies compact workflow JSON exposes sanitized top-level `finalFailedWorkflowStage` and `finalRetryWorkflowStage` fields derived from the app-side response envelope, and the Reference Import panel whitelist-displays those fields in workflow summaries without parsing raw host output
- the reference import workflow-runner smoke test verifies UI-copied `startStage` can begin a host-owned workflow directly at `adapter-preflight` for retry/preflight recovery, continue to `plan-only`, expose compact request/response entries, and avoid source-discovery, raw argv payloads, or browser-side importer execution
- the reference geometry importer smoke tests verify project-facing `--check-adapters` runs without `--project` or `--input`, preserves adapter preflight fingerprints, `adapterPreflightDecision`, and source-format alias normalization, requires `--adapter-config`, and rejects accidental project/import/display/transform combinations with machine-readable failure decisions before any project/reference writes
- the reference geometry importer UI-session smoke tests verify failed parsed host responses can preserve the failed workflow stage for diagnostics while routing the next workspace request to a sanitized, explicitly exposed `retryWorkflowStage`, and unsafe/path-like retry-stage values fall back to the failed stage so external-adapter dependency recovery stays in the workspace-command boundary instead of running translator code in the browser
- the Reference Import panel contract verifies failed host-response sessions surface the sanitized retry/fallback stage as a browser-only `Retry` row in the workspace request summary without executing translator/importer code in the app runtime
- the reference geometry importer smoke tests verify project-facing `--list-adapters` runs without `--project` or `--input`, preserves sanitized adapter registry fingerprints and `adapterRegistryDecision` without dependency-preflight fields, requires `--adapter-config`, and rejects accidental project/import/preflight/display/transform combinations with machine-readable failure decisions before any project/reference writes
- the reference geometry translator smoke test verifies `--list-translation-discovery` prints a side-effect-free translator discovery catalog with translator contract version, schema contract versions/paths, canonical output contract metadata and fingerprint, standalone adapter output validation contract metadata and fingerprint, source-description contract metadata including side-effect guarantees and fingerprint field name, translation command contract metadata with mode flags, required inputs by mode, translation-mode side-effect variants, target output fields, output fingerprint availability, JSON success/error fields, and fingerprint, result JSON contract metadata including workflow status field, plan decision field, plan fingerprint, per-mode output fingerprint availability, JSON error envelope fields, classifiable error execution-context fields, primary error metadata fields, translator workflow contract metadata with staged source-discovery/plan-only/adapter-request/external-wrapper/translate/validate-only side-effect boundaries, plan-only decision routing through `referenceTranslationPlanDecision`, and fingerprint, adapter request schema/version/path, required request fields, optional adapter-context fields, source identity fields, stage path fields, schema contract fields, adapter config contract metadata, adapter preflight contract metadata with `--check-adapters` flags, no-launch/no-write side effects, dependency check fields, diagnostic codes, and fingerprint fields, per-token formats, grouped formats, contract fingerprints, and an aggregate discovery fingerprint without requiring source input, output paths, adapter configs, adapter launches, or file writes
- the reference geometry translator smoke test verifies `--describe-source`, `--plan-only --json`, `--write-adapter-request --json`, `--validate-only`, and successful translate `--json` outputs carry `referenceTranslationWorkflowStatus` with the current workflow stage, completed/next stages, no-project/no-target-write flags, promoted write flags, adapter launch possibility, fingerprint fields, and side-effect boundary
- the reference geometry translator smoke test verifies `--plan-only --json` reports `referenceTranslationContractVersion`, `referenceTranslationExecutionMode`, `referenceTranslationSideEffectPlan`, planned source/output/option metadata including source requested-format family metadata, null output fingerprints, `referenceTranslationPlanDecision`, `referenceTranslationWorkflowStatus`, and `referenceTranslationPlanFingerprint`, preflights selected external adapters without launching them, reports stable aggregate and selected-adapter preflight and registry fingerprints, routes built-in, external-adapter, canonical JSON, and failed-preflight plans with stable safe-next-mode/recommended-action tokens, returns failed adapter preflight plan context with correlated error-level plan fingerprint, workflow status marked incomplete, registry fingerprints, and diagnostics, rejects adapter-stage preservation options, and creates no target manifest, request, scratch, chunk, or stage directory
- the reference geometry translator/importer smoke tests verify `--describe-source` prints machine-readable source format, alias, file stat, adapter requirement, accept-filter, stable `referenceSourceDescriptionFingerprint`, translator-side `referenceTranslationSourceDecision`, and project-facing import metadata without requiring output paths, project paths, adapter launches, or project/reference writes; the same field set and no-side-effect guarantees are discoverable through `referenceSourceDescriptionContract` in translation/import discovery catalogs; when an adapter config is supplied, source descriptions expose adapter config stat metadata plus aggregate/per-adapter/source-format registry fingerprints without adapter preflight fields, and missing adapter configs fail as machine-readable JSON even without `--json`; translator source descriptions include compact `referenceTranslationSourceDecision` metadata for source-file readiness, canonical JSON passthrough, adapter requirements, available translator modes, safe first mode, and recommended next action; importer source descriptions include the same import contract version, execution modes, side-effect plan discovery, compact `referenceImportSourceDecision` metadata for selected-file readiness, adapter requirements, safe first mode, and recommended next action, plus a `referenceImportDiscoveryFingerprint` that includes the source-description fingerprint and source decision
- the reference geometry translator smoke test verifies common file extension aliases such as `p21`, `stpnc`, `ifcxml`, `ifczip`, and `e57pc` are listed, normalized for adapter preflight, and auto-detected from input paths without requiring `--format`
- the reference geometry translator smoke test verifies `--list-adapters` validates adapter config and prints a machine-readable adapter registry with supported placeholder keys plus stable aggregate and per-adapter registry fingerprints without requiring source input, output paths, or adapter process launches
- the reference geometry translator smoke test verifies adapter discovery/preflight exposes effective `streamMaxBufferBytes`, and a noisy external adapter that exceeds its configured process stream buffer fails with structured `adapter-stream-buffer-exceeded` JSON metadata before output promotion
- the reference geometry translator smoke test verifies adapter discovery, preflight, external translator `--json`, and external importer results report the adapter config file path, size, and modified time for adapter automation audits
- the reference geometry translator smoke test verifies schema-valid adapter configs with unsupported placeholder names in args or env fail before adapter discovery output or process launch
- the reference geometry translator smoke test verifies the checked-in example adapter config declares STEP `p21`/`stpnc`, IFC `ifcxml`/`ifczip`, and E57 `e57pc` aliases so copied local adapter configs accept the same source extensions as format auto-detection
- the reference geometry translator smoke test verifies the checked-in example adapter config exposes adapter key, adapter run id, adapter config file metadata, adapter registry fingerprints, stage directory, scratch directory, chunk directory, chunk path prefix, source directory, source file, source stem, source extension, source stat fingerprint, and schema-version env keys through `--list-adapters` while still hiding env placeholder values
- the reference geometry translator smoke test verifies `--check-adapters` validates adapter config, reports supported placeholder keys plus templated-env versus raw-config-env wrapper classifications, preserves stable aggregate and per-adapter registry fingerprints beside stable aggregate and per-adapter preflight fingerprints, exposes `adapterPreflightDecision` readiness/fix-area/next-action routing for ready, dependency-missing, selection-mismatch, argument-template default/shadowing warning cases, unchecked local converter command warning cases, local converter cwd paths outside stage, local converter output paths outside scratch, unsupported local wrapper placeholders including raw shell, stream-buffer, and E57 point-text parser config placeholders, invalid local wrapper args JSON, invalid local wrapper args strings, invalid local wrapper shell env booleans, invalid local wrapper stream-buffer env byte counts, invalid E57 point column env configs including empty configured columns and partial configured RGB/normal point-attribute groups, invalid E57 point delimiter env configs, and invalid E57 normalized-RGB env booleans, carries that decision through translator/importer plan-only success and failure JSON without changing plan fingerprints, checks local adapter command availability, and supports format/named-adapter preflight without requiring source input, output paths, or adapter process launches
- the reference geometry translator smoke test verifies checked-in bridge raw-config booleans accept `on`/`off` tokens consistently in preflight and runtime adapter launch paths
- the reference geometry translator smoke test verifies adapter `requiredFiles`, `requiredDirectories`, `requiredCommands`, and `requiredEnv` are resolved and checked during preflight and again before launch, resolved main adapter commands and existing required commands report executable file size, modified-time, and stat fingerprint metadata, existing required files report file size, modified-time, and stat fingerprint metadata, existing required directories report immediate entry/file/subdirectory counts, file-byte totals, latest modified time, and stat fingerprint metadata, with missing files reported as `adapter-required-file-missing`, missing directories reported as `adapter-required-directory-missing`, missing commands reported as `adapter-required-command-missing`, and missing environment variables reported as `adapter-required-env-missing` without promoting output or exposing environment values
- the reference geometry translator smoke test verifies adapter `requiredFiles` and `requiredDirectories` can resolve through `{adapterConfigDir}` while the adapter process runs from its isolated staging output directory
- the reference geometry translator smoke test verifies `--write-adapter-request` generates schema-valid adapter request JSON with `adapterRequestFingerprint`, `adapterRunId`, adapter registry fingerprints for the sanitized config/selected adapter, canonical `format`, original `requestedFormat`, normalized output directory/stage directory/scratch directory/file name/file stem and chunk directory, chunk path prefix, staged adapter log path, source directory, source file metadata, source file stem, source file extension, source stat fingerprint, selected adapter key and adapter config path/directory/file metadata when an adapter config is supplied, schema contract versions, adapter/reference/chunk schema paths, effective adapter timeout, `referenceTranslationExecutionMode: "adapter-request"`, request-only side-effect plan metadata, source requested-format family metadata in the command summary and adapter-request decision, `referenceTranslationWorkflowStatus`, null target output fingerprints, adapter preflight fingerprints, `adapterPreflightDecision`, and `referenceTranslationAdapterRequestDecision` in the command summary but not the schema-validated request payload, and a `referenceTranslationPlanFingerprint` that matches an equivalent plan-only request, without launching an adapter or writing the target reference manifest, and schema-invalid request attempts or named-adapter format mismatches do not replace an existing request file; it also verifies the standalone adapter request validation CLI accepts staged requests, reports request fingerprint/run/adapter-registry/source/staged path/schema/import parameter metadata, exposes its own `--list-contract` output matching translator discovery including validator success/error fields, and rejects unknown options, missing option values, missing requests, unreadable request files, and malformed request JSON with stable `adapterRequestErrorCode` and `adapterRequestValidationKind` metadata
- the reference geometry translator smoke test verifies the standalone adapter request validation CLI preserves request fingerprint/run/adapter-registry/config/source/staged path/schema/import parameter metadata in `adapter-request-invalid` JSON errors when the staged request JSON can be read and parsed, so wrapper CI can correlate invalid request edits without adapter launch
- the reference geometry translator smoke test verifies the checked-in DWG, E57, and CAD/OBJ bridge wrappers reject unsupported adapter request `schemaVersion`, `schemaVersions`, and mismatched `adapterRequestFingerprint` contracts before reading adapter-owned path fields, resolving local converter settings, or launching converter processes; the wrappers also reject malformed or placeholder local converter stream-buffer env values before launching converter processes; the E57/XYZ wrapper also rejects explicit point-column configs that include neither x/y/z nor sphericalRange/sphericalAzimuth/sphericalElevation, reject placeholders, unknown point delimiters, and malformed normalized-RGB booleans before launching the local exporter, bounds verbose converter stdout/stderr copied into adapter logs while preserving full stdout payloads used as converted point text, reports a clear local converter stream-buffer failure when configured too low, skips PTS-style leading point-count rows even when point text columns are configured explicitly, and skips PTX-style column/row count plus scanner transform header rows at each scan block boundary before canonical chunking; the checked-in example adapter config also preflights the bridge wrapper script, request/output contract helpers, standalone request/output validator CLIs, and canonical translator script as required files
- the reference geometry translator smoke test verifies real external adapter staging writes the schema-validated `{request}` JSON atomically before launch and rewrites adapter-emitted canonical manifests through the same temp-then-rename path before staged validation/promotion
- the reference geometry translator smoke test verifies adapter `env` keys are reported without exposing values, wrapper external-tool argument-template mode/source/shadowing metadata is reported without exposing env values, and external adapter process environments receive placeholder-expanded values including selected adapter key, adapter run id, canonical format, requested format token, adapter config path/directory/file metadata, adapter registry fingerprints, stage directory, scratch directory, chunk directory, chunk path prefix, adapter log path, source directory, source file stem, source file extension, normalized source file stat metadata, source stat fingerprint, and schema contract versions
- the reference geometry translator smoke test verifies built-in and external-adapter canonical manifests carry normalized source alias/stat metadata in `asset.source.fileExtension`, `asset.source.requestedFormat`, `asset.source.fileSizeBytes`, `asset.source.modifiedTime`, and `asset.source.statFingerprint`, and external-adapter manifests carry the selected adapter key in `asset.source.adapterKey`
- the reference geometry translator smoke test verifies external adapter output cannot spoof cheap source identity metadata; file name, extension, requested format, source size, source modified time, and source stat fingerprint are normalized from the real input before promotion
- the reference geometry translator smoke test verifies the checked-in DWG-to-DXF bridge adapter can run a local converter command through the dedicated scratch directory, accept numbered, JSON-array, or no-shell `BOBERCAD_DWG_TO_DXF_ARGS` converter argument templates, translate the staged DXF through the built-in DXF translator, and still promote canonical output with original DWG source provenance and no temporary-DXF checksum leakage; it also verifies successful converters that emit malformed staged DXF fail through canonical DXF validation before output promotion, and that DXF passthrough requests through the same wrapper ignore DWG converter command/output settings and translate the input DXF directly
- the reference geometry translator smoke test verifies the checked-in DWG-to-DXF, E57-to-XYZ, and CAD-to-OBJ bridge wrappers treat a configured output value of `-` the same as `stdout`, capturing converter stdout into staged DXF, point-text, or OBJ scratch payloads before canonical translation/chunking
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge adapter can run a local exporter command through the dedicated scratch directory, accept numbered, JSON-array, or no-shell `BOBERCAD_E57_TO_XYZ_ARGS` exporter argument templates, parse unique configured point columns separated by commas, semicolons, pipes, or whitespace including pipe-separated and quoted whitespace `BOBERCAD_E57_XYZ_COLUMNS` and the `whitespace` delimiter alias, auto-detect semicolon/pipe point-text delimiters even when values contain decimal commas, auto-detect whitespace-delimited numeric rows whose values use decimal commas, parse `D`/`d` scientific exponent point-text numbers, strip repeated unit/range/type suffixes from point-text headers before column alias matching, use non-empty unique comment-prefixed point-text headers including bare and `Columns:` / `Fields:` labeled column lists with leading point id/index/scan/row/column/time/return/scan-angle metadata columns before ambiguous headerless row-width inference, reject empty configured or detected point-text header columns, reject partial point attribute groups such as `r,g` without `b` or `nx,ny` without `nz` from configured/detected columns and structured PCD `FIELDS` / PLY vertex property headers, derive unique columns from ASCII PCD `FIELDS` headers while skipping PCD metadata until explicit `DATA ascii`, decode PCD/PCL float32-bit packed `rgb` fields and PCD packed `rgba` fields into canonical colors while ignoring alpha, reject binary/non-ASCII PCD, duplicate recognized PCD headers such as `VERSION`, `FIELDS`, `COUNT`, `SIZE`, `TYPE`, `VIEWPOINT`, `POINTS`, `WIDTH`, and `HEIGHT` before `DATA ascii`, duplicate normalized PCD `FIELDS` columns, duplicate normalized configured or detected point-text header columns, point payload rows whose field count does not match the active columns, zero-vector point normals, PCD headers with missing `DATA` modes or extra `DATA` mode values, PCD payload rows before `DATA ascii`, PCD header rows after `DATA ascii`, and PCD `DATA ascii` without `POINTS` or complete `WIDTH * HEIGHT` payload bounds before canonical output promotion, skip known non-finite cartesian coordinate rows such as `nan` and `inf` before canonical chunk writing, skip PTS-style point-count rows after such headers, skip PTX-style column/row counts plus scanner position/axis/matrix header rows before parsing point payloads for initial and later scan blocks, infer headerless PTX point payload columns such as `x,y,z,intensity,r,g,b` after the scanner header, parse numeric, long, and short `T/F` validity flags before skipping cartesian/spherical invalid rows, convert `sphericalRange` / `sphericalAzimuth` / `sphericalElevation` point rows into canonical Cartesian point chunks, scale configured 0..1 normalized RGB exports including integer-looking `1 0 0` channels, write canonical point-cloud chunk sidecars with aligned color/intensity/classification/normal attributes, chunk larger converted point-text payloads into multiple sidecars without inline points or object-level attributes in the canonical manifest, and still promote canonical output with original E57 source provenance and no temporary point-text checksum leakage
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge requires a single explicit ASCII PLY `format ascii 1.0` header before `element` / `property` rows with exactly one mode and the supported `1.0` version value, derives columns from a single ASCII PLY `element vertex` / unique scalar `property` header section, requires vertex and non-vertex PLY `element` headers to contain exactly an element name and row count, requires each PLY `property` row to follow a preceding `element` row, requires an `element vertex` row-count declaration, requires `element vertex` row counts to be positive base-10 integer tokens instead of decimal or exponent-form numbers, requires vertex scalar `property` headers to contain exactly a supported scalar type and name, requires non-vertex scalar and list `property` headers to have their exact scalar/list token counts with supported scalar/list item types and integer list count types, requires `end_header` to contain no extra values, skips known PLY metadata headers such as `comment` and `obj_info`, rejects unknown PLY header rows, rejects missing or extra scalar property tokens, unsupported scalar property types, unsupported list count types, and vertex list properties, skips non-vertex list properties, parses only the declared vertex point payload row count after `end_header`, ignores later non-vertex payload rows such as face index lists, and rejects missing, late, malformed, binary, non-ASCII, property-before-element, duplicate format, duplicate vertex-element, or duplicate normalized vertex-property PLY headers before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge respects ASCII PCD `POINTS` payload counts after explicit `DATA ascii`, rejects payload rows before `DATA ascii`, rejects PCD header rows after `DATA ascii`, requires `POINTS` or complete `WIDTH * HEIGHT` metadata before `DATA ascii`, requires `POINTS` / `WIDTH` / `HEIGHT` headers to contain exactly one value, requires those values to be positive base-10 integer tokens with smoke coverage for decimal and exponent-form values on all three headers, derives the payload count from `WIDTH * HEIGHT` when `POINTS` is absent, ignores non-point footer rows after the declared payload, rejects extra point-like payload rows beyond either explicit `POINTS` or derived `WIDTH * HEIGHT` counts, and still preserves aligned point attributes
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge rejects ASCII PCD files whose `POINTS` count conflicts with `WIDTH * HEIGHT` before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge rejects ASCII PCD files whose payload ends before the declared point count before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge rejects ASCII PCD files with zero `POINTS` counts or zero `WIDTH`/`HEIGHT` dimensions before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge expands ASCII PCD `COUNT` headers before parsing rows, requires `FIELDS` headers to contain at least one field, requires `COUNT`/`SIZE`/`TYPE` headers to follow a preceding `FIELDS` header, requires `COUNT` and `SIZE` values to be positive base-10 integer tokens with zero, decimal, and exponent-form rejection coverage, requires `TYPE` values to be PCD scalar type tokens `F`, `I`, or `U`, rejects malformed, non-finite, or zero-quaternion `VIEWPOINT` rows, rejects malformed or unsupported `VERSION` rows outside supported PCD `0.7` text while accepting the `.7` exporter shorthand, rejects `COUNT`/`SIZE`/`TYPE` headers whose entry count does not match `FIELDS`, ignores unknown multi-count fields as adapter-local placeholders, rejects non-vertex PLY elements before `element vertex`, rejects extra non-empty PLY rows after the declared vertex plus non-vertex payload counts while still ignoring declared non-vertex PLY payload rows such as faces, and preserves later canonical color/intensity/classification/normal attributes
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge rejects ASCII PLY files whose vertex payload ends before the declared `element vertex` count before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge rejects ASCII PLY files whose `element vertex` count is zero before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge skips rows with known non-finite optional point-attribute tokens such as `nan` and `inf` before canonical chunk writing, records `metadata.skippedAttributeInvalidPointCount`, and still writes aligned color/intensity/classification arrays for surviving points
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge treats Windows/MSVCRT-style non-finite point-text tokens such as `1.#QNAN` and `-1.#IND` as invalid point rows before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes ASCII PLY `diffuse_red` / `diffuse_green` / `diffuse_blue` vertex properties to canonical RGB point attributes while keeping unsupported properties such as `alpha` out of chunk `pointAttributes`
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes ASCII PLY `scalar_Intensity` and `scalar_Classification` vertex properties to canonical intensity and classification point attributes
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge parses `on`/`off` point-text validity flags before skipping cartesian invalid rows
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge parses configured short and E57-style color channel aliases such as `color.r`, `color.g`, `color.b`, and `cartesianRed` / `cartesianGreen` / `cartesianBlue` into canonical RGB point attributes before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge unpacks single-column decimal `rgb`, all-digit bare `rgbHex`/`rgbaHex`, and prefixed hex `rgba` point-text colors into canonical RGB point attributes while ignoring alpha before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge accepts comment-prefixed LAS-style metadata headers such as `PointSourceID`, `UserData`, `ScanDirectionFlag`, and `EdgeOfFlightLine` before coordinate columns, preserving coordinates and canonical point attributes while treating those source columns as adapter-local metadata
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge parses quoted comma-delimited CSV point text through a literal `,` delimiter setting, quoted whitespace-delimited point text through the `space` delimiter setting, and tab-delimited point text through the `tsv` and `\t` delimiter aliases while preserving aligned point attributes before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes reversed coordinate-label aliases such as configured `XCoord` / `YCoord` / `ZCoord` columns and quoted CSV `X Coordinate` / `Y Coordinate` / `Z Coordinate` or `Cartesian X Coordinate` / `Cartesian Y Coordinate` / `Cartesian Z Coordinate` headers before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes position-label aliases such as configured `PosX` / `PosY` / `PosZ` columns and quoted CSV `Position X` / `Position Y` / `Position Z` headers before canonical chunk writing
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes survey-style `Easting`, `Northing`, and contextual `Elevation` point-text columns through both adapter preflight and runtime parsing before writing canonical Cartesian point-cloud chunks
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes E57-style dotted/prefixed point-text column headers, including `cartesianNormalX` / `cartesianNormalY` / `cartesianNormalZ` normal-vector aliases, and skips rows flagged by `cartesianInvalidState` / `cartesianInvalid` or inverse `cartesianValidState` / `cartesianValid` values before writing canonical point-cloud chunk sidecars
- the reference geometry translator smoke test verifies the checked-in E57-to-XYZ point-cloud bridge normalizes scalar/classification point-text aliases such as `intensityValue`, `cartesianIntensity`, `reflectance`, `amplitude`, `classId`, and `classificationValue` into canonical intensity and classification chunk attributes before writing sidecars
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ mesh bridge adapter can run a local STEP/IFC tessellator command through the dedicated scratch directory, accept numbered, JSON-array, or no-shell `BOBERCAD_CAD_TO_OBJ_ARGS` converter argument templates, parse staged OBJ vertices/faces/lines/point elements, including inline comments, backslash-continued face/line records, `v/vt/vn`-style references, relative negative vertex indices, OBJ face records closed by repeating the first vertex, decimal-comma and `D`/`d` exponent OBJ coordinates and MTL `Kd`/`d`/`Tr` material values, optional normalized, 8-bit, or 16-bit vertex RGB for point elements, optional homogeneous vertex weights with non-1 coordinate normalization both with and without following RGB channels, uniform per-face/per-line/per-point-element vertex RGB promoted to canonical `display.color`, `usemtl off` / `usemtl none` clearing material state without writing fake `objMaterial` metadata, quoted `mtllib` filenames with spaces, literal `#`, or backslash-escaped `#` characters, multiple `mtllib` entries on one line with mixed quoted and bare-escaped filenames, bare backslash-escaped OBJ/MTL object/group/material names and `mtllib` filenames with spaces or `#` characters, and quoted OBJ object/group/material plus MTL material names containing spaces, `#` characters, backslash-escaped quotes, and doubled single quotes with MTL material display metadata with inline comments and `d -halo` opacity records, into canonical mesh, line-set, and inline point-cloud objects with aligned point colors, rejects degenerate OBJ face records with fewer than three distinct resolved vertex references, face records with consecutive duplicate vertex references, face records with repeated non-closing vertex references, line records with consecutive duplicate vertex references, point-element records with duplicate vertex references, point-cloud groups that mix colored and uncolored point vertex references, duplicate MTL material definitions, repeated or invalid MTL `Kd` color values, repeated or invalid MTL `d` / `Tr` opacity values, real `usemtl` names without any staged `mtllib`, missing `mtllib` files, and `mtllib` paths outside the staged OBJ directory before canonical output promotion, and still promote canonical output with original STEP/IFC source provenance and no temporary-OBJ checksum leakage
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects unterminated OBJ line continuations, including a trailing empty backslash continuation, before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects `usemtl` names that do not match any material defined by declared staged MTL libraries before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects empty OBJ `o`, `g`, and `usemtl` statements and empty MTL `newmtl` material definitions before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects empty OBJ `mtllib` statements and empty quoted `mtllib` entries before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects MTL `Kd` / `d` / `Tr` material property records and neutral ignored material metadata such as `illum` when they appear before the first named `newmtl` definition
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects MTL `Kd` records with extra color channels and MTL `d` / `Tr` records with extra opacity tokens before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects unsupported MTL texture-map records such as `map_Kd` before canonical output promotion so texture data must be baked by the CAD-to-OBJ converter instead of silently dropped
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge explicitly ignores syntactically valid common neutral MTL metadata such as `Ka`, `Ks`, `Ke`, `Ns`, `Ni`, `Tf`, `sharpness`, and `illum` while rejecting malformed, negative, overrange color/scalar, unknown, or out-of-range `illum` ignored MTL material records before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects OBJ `v` records with unsupported or extra vertex fields before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects decimal, exponent, empty, or otherwise non-integer OBJ vertex index tokens in `f`, `l`, and `p` records before canonical output promotion
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects malformed `v/vt/vn` reference tokens, malformed staged `vt`/`vn` records, zero-vector `vn` normals, and texture/normal indices that do not resolve to staged `vt`/`vn` records instead of ignoring invalid texture/normal index fields
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge rejects unsupported OBJ curve/surface records and controls such as `curv`, `cstype`, `parm`, `con`, and `surf` instead of silently dropping non-`v/f/l/p` tessellator geometry
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge explicitly ignores syntactically valid neutral OBJ smoothing group records such as `s 1` / `s off`, rejects malformed smoothing records, and rejects unknown OBJ records before canonical output promotion instead of silently dropping adapter output
- the reference geometry translator smoke test verifies the checked-in CAD-to-OBJ bridge accepts configured `BOBERCAD_CAD_OBJ_VERTEX_COLOR_LAYOUT=rgba`, treats `v x y z r g b a` point vertex colors as RGB with ignored alpha, and rejects unknown layout values during adapter preflight before launching a tessellator
- the reference geometry translator smoke test verifies the adapter-side output contract helper accepts canonical manifests produced for the staged adapter request and rejects mismatched `asset.id`, `asset.name`, `asset.units`, `asset.source.format`, `asset.source.requestedFormat`, request-declared `asset.source.fileName`/`fileExtension`/`fileSizeBytes`/`modifiedTime`/`statFingerprint`/`adapterKey` values, missing selected `asset.source.adapterKey`, adapter-emitted `asset.source.checksum` values for selected external adapters, unsafe/path-like selected-external-adapter `asset.source.translator` or `asset.source.translatorVersion` values, or point-cloud chunk sidecars outside the request's `chunkDir`; it also verifies the standalone adapter request validator reports adapter config path/stat metadata and `adapterConfigStatFingerprint` for wrapper CI correlation, and that the standalone adapter output validation CLI reports machine-readable request/output/asset/object metadata, adapter request fingerprint/run/registry/source/config correlation fields, adapter request/reference/chunk schema version/path correlation fields, manifest/chunk/artifact fingerprints, manifest file stats, and point-cloud sidecar entries for valid output, exposes its own `--list-contract` JSON contract without requiring a request/output manifest, advertises request `schemaVersions`/`schemas`, adapter-request schema version/path alignment, staged output path policy, canonical chunk path schema pattern, external-adapter checksum omission, and external-adapter source-provenance safety in that contract including safe translator/version token patterns, reserved translator tokens, the 64-character version-token limit, and the stable identity-mismatch failure code, and rejects stale canonical bounds, asset-name/asset-units/source-format/source-provenance/adapter-key/checksum/translator-provenance mismatches, unknown options, missing option values, missing requests, unreadable or malformed request JSON, and output paths that do not match the staged request with stable `adapterOutputErrorCode`, `adapterOutputValidationKind`, output path metadata, and request correlation fields where the request was already schema-checked
- the reference geometry translator smoke test verifies `--validate-only` validates canonical manifests and point-cloud chunk sidecars without requiring an output path or writing translated assets, and reports `referenceTranslationExecutionMode: "validate-only"` plus side-effect and workflow status metadata in successful and failed JSON output
- the reference geometry translator smoke test verifies `--json` prints machine-readable translation, validation, `referenceTranslationContractVersion`, `referenceTranslationExecutionMode`, `referenceTranslationSideEffectPlan`, built-in/external translation mode, canonical reference schema/version, canonical reference units, asset bounds, source coordinate system, canonical reference manifest and point-cloud chunk sidecar file metadata, total manifest chunk point count, total line segment count, total mesh face count, total point-cloud point count across inline and chunked storage, prefixed reference object/layer/chunk counts, prefixed reference object kind counts, selected external adapter key, safe source translator provenance, verified source checksum when present, omission of unverified external-adapter checksums, diagnostic counts, and failed-validation results for adapter automation without changing default stderr error behavior
- the reference geometry translator smoke test verifies binary or malformed DXF inputs and malformed STEP/IFC Part 21 inputs fail before writing a canonical manifest, preserve any existing target manifest, and report machine-readable `--json` failure details with translation contract, execution mode, side-effect plan, and classifiable translation mode metadata
- the reference geometry translator smoke test verifies DXF truecolor `420` and transparency `440` layer/entity appearance values are promoted to canonical `display.color` / `display.opacity`, including same-layer geometry split by differing appearance before app runtime consumption
- the reference geometry translator smoke test verifies DXF `LWPOLYLINE` bulge values are sampled as curved reference linework instead of flattened into a single chord
- the reference geometry translator smoke test verifies classic DXF `POLYLINE` vertex bulge values are sampled as curved reference linework instead of flattened into a single chord
- the reference geometry translator smoke test verifies malformed DXF `LWPOLYLINE` and classic `POLYLINE` bulge values emit explicit `dxf-polyline-invalid-bulge-skipped` diagnostics instead of silently flattening source arcs into straight fallback linework
- the reference geometry translator smoke test verifies DXF `3DLINE` entities translate to bounded canonical line-set reference geometry instead of being reported as unsupported
- the reference geometry translator smoke test verifies zero-length DXF `LINE` records emit an explicit `dxf-degenerate-line-skipped` diagnostic while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `LINE`/`3DLINE` records with missing or invalid endpoint coordinates emit explicit `dxf-line-invalid-skipped` diagnostics instead of defaulting malformed endpoints to the origin
- the reference geometry translator smoke test verifies DXF `LWPOLYLINE` and classic `POLYLINE` records without supported line or face segments emit explicit `dxf-polyline-empty-skipped` diagnostics while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `LWPOLYLINE` and classic `POLYLINE` records with missing or invalid required vertex coordinates emit explicit `dxf-polyline-invalid-vertex-skipped` diagnostics instead of generating fake linework through malformed vertices
- the reference geometry translator smoke test verifies DXF `LWPOLYLINE` and classic `POLYLINE` records with malformed explicit extrusion normals emit explicit `dxf-polyline-invalid-extrusion-skipped` diagnostics instead of generating fake default-plane linework
- the reference geometry translator smoke test verifies DXF `CIRCLE`/`ARC` extrusion normals rotate sampled OCS vertices into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies DXF `LWPOLYLINE` extrusion normals rotate sampled OCS vertices into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies classic DXF `POLYLINE` extrusion normals rotate sampled OCS vertices into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies DXF `HATCH` extrusion normals rotate boundary OCS vertices into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies DXF `ELLIPSE` full and partial curves are sampled as canonical reference linework
- the reference geometry translator smoke test verifies DXF `ELLIPSE` WCS center/major-axis coordinates use the extrusion normal to place the curve plane without treating ellipse coordinates as OCS vertices
- the reference geometry translator smoke test verifies DXF `SPLINE` knot/control and fit-point curves are sampled as canonical reference linework
- the reference geometry translator smoke test verifies degenerate DXF `CIRCLE`, `ARC`, `ELLIPSE`, and `SPLINE` records that produce no supported curve segments emit explicit `dxf-curve-empty-skipped` diagnostics while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `CIRCLE`, `ARC`, `ELLIPSE`, and `SPLINE` records with missing or invalid required curve parameters, including malformed curve extrusion normals where applicable, emit explicit `dxf-curve-invalid-parameter-skipped` diagnostics instead of generating canonical linework from fake origin/default values
- the reference geometry translator smoke test verifies DXF `LEADER` vertex paths are translated as canonical reference linework
- the reference geometry translator smoke test verifies DXF `MLINE` vertices are translated as centerline fallback linework with an explicit diagnostic
- the reference geometry translator smoke test verifies DXF `LEADER` and `MLINE` records without supported path or centerline segments emit explicit `dxf-leader-empty-skipped` / `dxf-mline-empty-skipped` diagnostics while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `LEADER` and `MLINE` records with missing or invalid required path vertex coordinates emit explicit `dxf-leader-invalid-vertex-skipped` / `dxf-mline-invalid-vertex-skipped` diagnostics instead of generating fake path linework
- the reference geometry translator smoke test verifies DXF `TEXT`, `MTEXT`, `MLEADER`, `MULTILEADER`, `TOLERANCE`, `ATTDEF`, and `ATTRIB` annotations are skipped with explicit diagnostics instead of being imported as misleading reference geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies DXF `HELIX` curve entities are skipped with explicit adapter-owned tessellation diagnostics instead of being imported as misleading reference geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies DXF `XLINE` and `RAY` infinite construction geometry are skipped with explicit diagnostics instead of being clipped into arbitrary finite reference geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies DXF `IMAGE`, `WIPEOUT`, `PDFUNDERLAY`, `DGNUNDERLAY`, and `DWFUNDERLAY` raster or underlay references are skipped with explicit diagnostics instead of being imported as misleading reference geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies opaque DXF ACIS/BREP entities such as `3DSOLID`, `BODY`, `REGION`, and surface records are skipped with explicit diagnostics instead of being imported as fake mesh geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies DXF `ACAD_PROXY_ENTITY` and `ACAD_PROXY_OBJECT` payloads are skipped with explicit diagnostics instead of being imported as misleading geometry or reported as generic unsupported entities
- the reference geometry translator smoke test verifies common DXF table/object metadata records such as `LTYPE`, `STYLE`, `APPID`, `DIMSTYLE`, `LAYOUT`, `XRECORD`, and layout `VIEWPORT` records are ignored without generic unsupported diagnostics
- the reference geometry translator smoke test verifies ASCII DXF group-code `999` comment pairs are ignored before canonical linework output
- the reference geometry translator smoke test verifies DXF `POINT` entities translate to canonical point-cloud reference points and DXF `3DFACE` entities translate to canonical mesh faces, including repeated fourth vertices collapsed to triangles
- the reference geometry translator smoke test verifies DXF `POINT` records with missing or invalid required coordinates emit explicit `dxf-point-invalid-skipped` diagnostics instead of defaulting malformed coordinates to the origin
- the reference geometry translator smoke test verifies degenerate DXF `3DFACE`, `SOLID`, and `TRACE` records with fewer than three distinct points emit explicit `dxf-degenerate-face-skipped` diagnostics while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `3DFACE`, `SOLID`, and `TRACE` records with missing or invalid required vertex coordinates emit explicit `dxf-face-invalid-vertex-skipped` diagnostics instead of generating canonical mesh faces from fake origin/default coordinates
- the reference geometry translator smoke test verifies DXF `SOLID` and `TRACE` records with malformed explicit extrusion normals emit explicit `dxf-face-invalid-extrusion-skipped` diagnostics instead of generating fake default-plane mesh faces
- the reference geometry translator smoke test verifies DXF group-value numbers using `D`/`d` scientific exponents are parsed before canonical LINE, POINT, and 3DFACE output
- the reference geometry translator smoke test verifies DXF `SOLID` and `TRACE` records are translated as canonical quadrilateral mesh faces
- the reference geometry translator smoke test verifies DXF `SOLID`/`TRACE` extrusion normals rotate flat OCS mesh faces into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies DXF `MESH` face-list records are translated as canonical mesh faces rather than reported as unsupported
- the reference geometry translator smoke test verifies DXF `MESH` records without supported face-list records emit an explicit `dxf-mesh-empty-skipped` diagnostic while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `MESH` records with missing or invalid required vertex coordinates emit an explicit `dxf-mesh-invalid-vertex-skipped` diagnostic instead of generating canonical mesh faces from fake or reindexed vertices
- the reference geometry translator smoke test verifies DXF `POLYLINE` polyface meshes are translated as canonical mesh faces rather than linework
- the reference geometry translator smoke test verifies DXF `POLYLINE` polygon mesh grids are translated as canonical quadrilateral mesh faces rather than linework
- the reference geometry translator smoke test verifies DXF `POLYLINE` polygon mesh grid vertices honor header extrusion-normal OCS placement before canonical mesh output
- the reference geometry translator smoke test verifies DXF `HATCH` polyline, line, arc, ellipse, and spline boundary records are translated as canonical reference linework, including clockwise arc edges
- the reference geometry translator smoke test verifies DXF `HATCH` records without supported boundary segments emit an explicit `dxf-hatch-empty-skipped` diagnostic while preserving other supported geometry in the file
- the reference geometry translator smoke test verifies DXF `HATCH` records with missing or invalid required boundary parameters, including malformed explicit extrusion normals, emit explicit `dxf-hatch-invalid-boundary-skipped` diagnostics instead of generating canonical linework from fake hatch boundary vertices
- the reference geometry translator smoke test verifies colliding sanitized DXF layer-derived object ids are suffixed instead of overwriting translated linework
- the reference geometry translator smoke test verifies DXF `BLOCK`/`INSERT` expansion with block base point, translation, scale, rotation, layer inheritance, and by-block color/transparency propagation into canonical line, point, and mesh objects
- the reference geometry translator smoke test verifies DXF `BLOCK` definitions with missing or invalid base point coordinates emit explicit `dxf-block-invalid-base-skipped` diagnostics instead of expanding inserts from fake block-local base points
- the reference geometry translator smoke test verifies DXF `INSERT` records with missing or invalid required transform or array parameters emit explicit `dxf-insert-invalid-transform-skipped` diagnostics instead of generating canonical geometry from fake insertion, scale, rotation, extrusion, or array values
- the reference geometry translator smoke test verifies nested DXF `BLOCK`/`INSERT` references compose child and parent transforms without importing uninstanced block-local geometry
- the reference geometry translator smoke test verifies DXF `INSERT` row/column arrays expand into repeated canonical linework without importing uninstanced block-local geometry
- the reference geometry translator smoke test verifies DXF `INSERT` extrusion normals rotate insertion points and inserted block geometry into WCS instead of leaving them in the default XY plane
- the reference geometry translator smoke test verifies DXF `DIMENSION` picture blocks expand into canonical reference linework with by-block picture linework color/transparency inherited from the dimension entity, without interpreting dimension semantics
- the reference geometry translator smoke test verifies DXF `DIMENSION` picture blocks with missing or invalid base point coordinates emit source-level `dxf-block-invalid-base-skipped` diagnostics instead of downstream missing-dimension-block diagnostics or fake picture linework
- the reference geometry translator smoke test verifies DXF `$INSUNITS`, STEP `LENGTH_UNIT` / conversion-based inch units, and IFC `IFCSIUNIT` / conversion-based foot units are detected by label or supported conversion factor while explicit `--units` overrides are preserved
- the reference geometry translator smoke test verifies STEP `POLYGONAL_FACE_SET`/`INDEXED_POLYGONAL_FACE` entities are translated as canonical reference meshes
- the reference geometry translator smoke test verifies malformed STEP `CARTESIAN_POINT_LIST_3D` coordinate tuples emit `step-tessellated-points-invalid-skipped` for dependent tessellated face sets and do not synthesize mesh coordinates from partially parsed numeric values
- the reference geometry translator smoke test verifies STEP `EDGE_LOOP`/`ORIENTED_EDGE`/`EDGE_CURVE`/`VERTEX_POINT` topology is translated as canonical reference mesh faces
- the reference geometry translator smoke test verifies standalone STEP `VERTEX_POINT` entities are translated as canonical reference point-cloud points instead of being reported as unsupported geometry, and mapped point-only STEP `MAPPED_ITEM` / `REPRESENTATION_MAP` instances receive target transforms without leaking source-local point coordinates into canonical output
- the reference geometry translator smoke test verifies point-only STEP `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with malformed origin `CARTESIAN_POINT` tuples emit `step-mapped-item-invalid-transform-skipped` and do not emit source-local or fake transformed point-cloud coordinates
- the reference geometry translator smoke test verifies standalone STEP `VERTEX_POINT` records that reference malformed `CARTESIAN_POINT` coordinate tuples emit `step-vertex-point-invalid-skipped` diagnostics instead of generating fake point-cloud coordinates from partially parsed numeric tuples
- the reference geometry translator smoke test verifies STEP Part 21 block comments are stripped before entity scanning so commented-out `VERTEX_POINT` records are not imported as canonical point-cloud geometry
- the reference geometry translator smoke test verifies STEP Part 21 `#id`-like text inside quoted strings is ignored when resolving entity references, so labels cannot redirect `AXIS2_PLACEMENT_3D` geometry
- the reference geometry translator smoke test verifies STEP Part 21 numbers using `D`/`d` scientific exponents are parsed before canonical `VERTEX_POINT` point-cloud and `CIRCLE` linework output
- the reference geometry translator smoke test verifies STEP `POLYLINE`/`GEOMETRIC_CURVE_SET` entities are translated as canonical reference linework
- the reference geometry translator smoke test verifies STEP `PRESENTATION_LAYER_ASSIGNMENT` on `GEOMETRIC_CURVE_SET` propagates to contained curve linework as a source-derived canonical layer instead of requiring app-runtime layer inference
- the reference geometry translator smoke test verifies mapped STEP `POLYLINE`/`GEOMETRIC_CURVE_SET` linework receives `MAPPED_ITEM` transforms without importing source curves at origin
- the reference geometry translator smoke test verifies mapped STEP curve `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with malformed origin `CARTESIAN_POINT` tuples emit `step-mapped-item-invalid-transform-skipped` and do not emit source-local or fake transformed curve vertices
- the reference geometry translator smoke test verifies mapped STEP curve `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with malformed or zero-length target-axis `DIRECTION` tuples emit `step-mapped-item-invalid-transform-skipped` and do not emit source-local or fake transformed curve vertices
- the reference geometry translator smoke test verifies mapped STEP curve `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with parallel target axes emit `step-mapped-item-invalid-transform-skipped` for a degenerate transform basis and do not emit source-local or fake transformed curve vertices
- the reference geometry translator smoke test verifies mapped STEP curve `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with malformed or zero explicit target scale values emit `step-mapped-item-invalid-transform-skipped` and do not emit source-local, default-scale, or collapsed fake curve vertices
- the reference geometry translator smoke test verifies mapped STEP `POLYLINE`/`GEOMETRIC_CURVE_SET` linework receives non-uniform X/Y/Z `MAPPED_ITEM` target transforms without importing source curves at origin
- the reference geometry translator smoke test verifies mapped STEP `TRIMMED_CURVE` linework receives `MAPPED_ITEM` transforms without importing source curves at origin
- the reference geometry translator smoke test verifies mapped STEP `TRIMMED_CURVE` B-spline linework receives `MAPPED_ITEM` transforms without importing source curves or full base B-spline endpoints
- the reference geometry translator smoke test verifies STEP `CIRCLE`/`GEOMETRIC_CURVE_SET` entities are sampled as canonical reference linework with `AXIS2_PLACEMENT` transforms
- the reference geometry translator smoke test verifies STEP `CIRCLE` entities that reference `AXIS2_PLACEMENT_3D` records with malformed origin `CARTESIAN_POINT` tuples emit `step-curve-invalid-placement-skipped` and do not synthesize default-origin circle linework
- the reference geometry translator smoke test verifies STEP `CIRCLE` entities that reference `AXIS2_PLACEMENT_3D` records with zero-length target axes or parallel placement axes emit `step-curve-invalid-placement-skipped` and do not synthesize fake circle linework
- the reference geometry translator smoke test verifies STEP `ELLIPSE`/`GEOMETRIC_CURVE_SET` entities are sampled as canonical reference linework with `AXIS2_PLACEMENT` transforms
- the reference geometry translator smoke test verifies STEP non-rational `B_SPLINE_CURVE_WITH_KNOTS` entities are sampled as canonical reference linework without flattening to endpoint chord geometry
- the reference geometry translator smoke test verifies STEP complex-entity rational B-spline curves with explicit knots apply weight data when sampled as canonical reference linework
- the reference geometry translator smoke test verifies STEP `COMPOSITE_CURVE` entities compose supported child curves, honor `SameSense = FALSE`, and avoid duplicate child curve output
- the reference geometry translator smoke test verifies STEP `TRIMMED_CURVE` parameter ranges over `CIRCLE`/`ELLIPSE` base curves are sampled as canonical reference linework without importing the full base curves
- the reference geometry translator smoke test verifies STEP `TRIMMED_CURVE` Cartesian point trims over `CIRCLE`/`ELLIPSE` base curves are resolved to canonical reference linework without importing the full base curves
- the reference geometry translator smoke test verifies STEP `TRIMMED_CURVE` parameter ranges over `LINE` base curves use `VECTOR` magnitude to emit finite canonical line segments
- the reference geometry translator smoke test verifies STEP `VECTOR` records that reference malformed `DIRECTION` coordinate tuples emit `step-vector-invalid-direction-skipped` and do not synthesize trimmed-line coordinates from partially parsed numeric values
- the reference geometry translator smoke test verifies STEP `TRIMMED_CURVE` parameter ranges over `B_SPLINE_CURVE_WITH_KNOTS` base curves sample only the trimmed B-spline span without importing full base curve endpoints
- the reference geometry translator smoke test verifies STEP `FACE_BOUND` inner loops are preserved as canonical reference linework while outer faces remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP faces without explicit `FACE_OUTER_BOUND` use the first `FACE_BOUND` as canonical mesh geometry and preserve remaining bounds as inner linework
- the reference geometry translator smoke test verifies STEP `INDEXED_POLYGONAL_FACE_WITH_VOIDS` inner loops are preserved as canonical reference linework while outer polygon faces remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP mapped faceted instances receive basic `MAPPED_ITEM` target transforms without importing source representations at origin
- the reference geometry translator smoke test verifies STEP mapped faceted instances receive non-uniform X/Y/Z `MAPPED_ITEM` target transforms without importing source representations at origin
- the reference geometry translator smoke test verifies STEP mapped `MANIFOLD_SOLID_BREP`/shell wrapper items resolve through `REPRESENTATION_MAP` and do not import source faces at origin
- the reference geometry translator smoke test verifies basic STEP `EXTRUDED_AREA_SOLID` rectangle profiles are translated to canonical meshes with swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP `EXTRUDED_AREA_SOLID` records with zero-length or malformed extrusion `DIRECTION` records emit `step-extruded-solid-invalid-direction-skipped` and do not synthesize fake swept mesh vertices from fallback axes
- the reference geometry translator smoke test verifies basic STEP `REVOLVED_AREA_SOLID` rectangle profiles are sampled to canonical meshes with swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP `REVOLVED_AREA_SOLID` records with zero-length or malformed `AXIS1_PLACEMENT` axis `DIRECTION` records emit `step-revolved-solid-invalid-axis-skipped` and do not synthesize fake revolved mesh vertices from fallback axes
- the reference geometry translator smoke test verifies STEP `SWEPT_DISK_SOLID` directrix curves are sampled into canonical tube meshes without also emitting the directrix as standalone curve linework
- the reference geometry translator smoke test verifies STEP `SWEPT_DISK_SOLID` start/end path-distance parameters trim the directrix before tube sampling
- the reference geometry translator smoke test verifies STEP `SWEPT_DISK_SOLID` records with malformed explicit start/end path-distance parameters emit `step-swept-disk-invalid-parameter-skipped` and do not synthesize full or partially trimmed tube mesh vertices
- the reference geometry translator smoke test verifies STEP `SWEPT_DISK_SOLID` records with malformed outer radius or inner radius parameters emit `step-swept-disk-invalid-parameter-skipped` and do not synthesize tube mesh vertices from neighboring numeric arguments or ignored pipe-wall parameters
- the reference geometry translator smoke test verifies STEP `SWEPT_DISK_SOLID` records with start/end path-distance parameters outside the directrix range or in reverse order emit `step-swept-disk-invalid-parameter-skipped` and do not synthesize tube mesh vertices from clamped or swapped trim ranges
- the reference geometry translator smoke test verifies STEP revolved hollow profiles preserve sampled inner profile rings as canonical reference linework while outer revolved profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies compatible STEP `EXTRUDED_AREA_SOLID_TAPERED` start/end profiles are translated to canonical tapered meshes with swept-solid placement transforms
- the reference geometry translator smoke test verifies compatible STEP tapered hollow profiles preserve start/end profile void loops as canonical reference linework while outer tapered profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP mapped tapered hollow profiles apply `MAPPED_ITEM` transforms to both outer tapered meshes and start/end profile void linework
- the reference geometry translator smoke test verifies STEP mapped revolved hollow profiles apply `MAPPED_ITEM` transforms to both sampled outer revolved meshes and sampled inner profile ring linework
- the reference geometry translator smoke test verifies STEP swept profile `AXIS2_PLACEMENT_2D` positioning composes with swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP swept profile or swept-solid `AXIS2_PLACEMENT` records with zero-length axes or degenerate/parallel placement bases emit `step-profile-invalid-placement-skipped` or `step-extruded-solid-invalid-placement-skipped` and do not synthesize fake swept mesh vertices
- the reference geometry translator smoke test verifies STEP `DERIVED_PROFILE_DEF` parent-profile transforms with `CARTESIAN_TRANSFORMATION_OPERATOR_2D` and `CARTESIAN_TRANSFORMATION_OPERATOR_2D_NON_UNIFORM` compose with swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP `MIRRORED_PROFILE_DEF` mirrors positioned parent profiles about the parent y-axis before swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP `TRAPEZIUM_PROFILE_DEF` swept profiles are translated as canonical reference meshes with swept-solid placement transforms
- the reference geometry translator smoke test verifies STEP `I_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `ASYMMETRIC_I_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `L_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `C_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `U_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `T_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `Z_SHAPE_PROFILE_DEF` swept profiles are translated as sharp-corner canonical reference meshes
- the reference geometry translator smoke test verifies STEP `ELLIPSE_PROFILE_DEF` swept profiles are translated as sampled canonical reference meshes
- the reference geometry translator smoke test verifies STEP `ROUNDED_RECTANGLE_PROFILE_DEF` swept profiles are translated as sampled canonical reference meshes
- the reference geometry translator smoke test verifies STEP `RECTANGLE_HOLLOW_PROFILE_DEF` swept profiles preserve inner hollow loops as canonical reference linework while outer profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP `RECTANGLE_HOLLOW_PROFILE_DEF` inner and outer fillet radii are sampled into canonical outer mesh geometry and inner-loop linework
- the reference geometry translator smoke test verifies STEP `CIRCLE_HOLLOW_PROFILE_DEF` swept profiles preserve sampled inner circular loops as canonical reference linework while outer profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP `ARBITRARY_PROFILE_DEF_WITH_VOIDS` inner curves on `EXTRUDED_AREA_SOLID` profiles are preserved as canonical reference linework while outer swept profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP mapped swept solids preserve transformed profile void linework while mapped outer swept profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies STEP `DRAUGHTING_PRE_DEFINED_COLOUR` styled items resolve named drafting colors into canonical mesh `display.color`
- the reference geometry translator smoke test verifies STEP `SHAPE_REPRESENTATION` `STYLED_ITEM` appearance propagates to contained mesh, line-set, and point-cloud canonical objects without app-runtime style inference
- the reference geometry translator smoke test verifies STEP mapped mesh `MAPPED_ITEM` instance `STYLED_ITEM` appearance overrides the styled source mesh item before canonical mesh emission
- the reference geometry translator smoke test verifies mapped STEP mesh `MAPPED_ITEM` records that reference `CARTESIAN_TRANSFORMATION_OPERATOR` records with malformed origin `CARTESIAN_POINT` tuples emit `step-mapped-item-invalid-transform-skipped` and do not emit source-local or fake transformed mesh vertices
- the reference geometry translator smoke test verifies STEP `CURVE_STYLE` styled curve items resolve presentation colors into canonical line-set `display.color`
- the reference geometry translator smoke test verifies STEP mapped curve `MAPPED_ITEM` instance `STYLED_ITEM` appearance overrides the styled source curve item before canonical line-set emission
- the reference geometry translator smoke test verifies STEP `STYLED_ITEM` appearance on standalone `VERTEX_POINT` entities splits canonical point-cloud objects by display color and preserves nonzero transparency as point-cloud `display.opacity`
- the reference geometry translator smoke test verifies STEP point-only `MAPPED_ITEM` instance `STYLED_ITEM` appearance overrides the styled source `VERTEX_POINT` representation item before canonical point-cloud emission
- the reference geometry translator smoke test verifies IFC faceted BREP product meshes receive basic `IFCLOCALPLACEMENT` translation and axis rotation
- the reference geometry translator smoke test verifies IFC products whose `IFCLOCALPLACEMENT` references an invalid relative `IFCAXIS2PLACEMENT` emit `ifc-local-placement-invalid-skipped` and do not leak source-local or fake product-placed mesh vertices while unrelated standalone BREP geometry still imports
- the reference geometry translator smoke test verifies IFC products whose `IFCLOCALPLACEMENT` parent chain is cyclic emit `ifc-local-placement-invalid-skipped` and do not leak source-local or fake product-placed mesh vertices while unrelated standalone BREP geometry still imports
- the reference geometry translator smoke test verifies IFC `IFCEDGELOOP`/`IFCORIENTEDEDGE`/`IFCEDGE`/`IFCEDGECURVE`/`IFCVERTEXPOINT` topology is translated as canonical reference mesh faces
- the reference geometry translator smoke test verifies standalone IFC `IFCVERTEXPOINT` entities are translated as canonical reference point-cloud points instead of being reported as unsupported geometry, product-owned `IFCVERTEXPOINT` points receive `IFCLOCALPLACEMENT` transforms without leaking local point coordinates into canonical output, and point-only `IFCMAPPEDITEM` / `IFCREPRESENTATIONMAP` instances compose mapped-item transforms with product placement without leaking source-local or pre-product point coordinates
- the reference geometry translator smoke test verifies point-only IFC `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with malformed origin `IFCCARTESIANPOINT` tuples emit `ifc-mapped-item-invalid-transform-skipped` and do not emit source-local, pre-product, or fake product-placed point-cloud coordinates
- the reference geometry translator smoke test verifies standalone IFC `IFCVERTEXPOINT` records that reference malformed `IFCCARTESIANPOINT` coordinate tuples emit `ifc-vertex-point-invalid-skipped` and do not synthesize point-cloud coordinates from partially parsed numeric values
- the reference geometry translator smoke test verifies IFC Part 21 block comments are stripped before entity scanning so commented-out `IFCVERTEXPOINT` records are not imported as canonical point-cloud geometry
- the reference geometry translator smoke test verifies IFC Part 21 `#id`-like text inside quoted strings is ignored when resolving entity references, so product names cannot redirect `IFCLOCALPLACEMENT` geometry
- the reference geometry translator smoke test verifies IFC Part 21 numbers using `D`/`d` scientific exponents are parsed before canonical `IFCVERTEXPOINT` point-cloud and `IFCCIRCLE` linework output
- the reference geometry translator smoke test verifies malformed `IFCCARTESIANPOINTLIST2D` / `IFCCARTESIANPOINTLIST3D` coordinate tuples emit `ifc-indexed-polycurve-points-invalid-skipped` for dependent `IFCINDEXEDPOLYCURVE` linework and do not synthesize indexed coordinates from partially parsed numeric values
- the reference geometry translator smoke test verifies IFC `IFCSTYLEDITEM` appearance on standalone `IFCVERTEXPOINT` entities splits canonical point-cloud objects by display color and preserves nonzero transparency as point-cloud `display.opacity`
- the reference geometry translator smoke test verifies IFC point-only `IFCMAPPEDITEM` instance `IFCSTYLEDITEM` appearance overrides the styled source `IFCVERTEXPOINT` representation item before canonical point-cloud emission
- the reference geometry translator smoke test verifies IFC `IFCTRIANGULATEDFACESET` and `IFCPOLYGONALFACESET` tessellated geometry translates to canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCINDEXEDPOLYGONALFACEWITHVOIDS` inner loops are preserved as canonical reference linework while outer polygon faces remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC `IFCINDEXEDPOLYCURVE` line indices are translated as canonical reference linework with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCINDEXEDPOLYCURVE` arc indices are sampled as canonical reference linework through the middle arc point
- the reference geometry translator smoke test verifies IFC `IFCCIRCLE` and `IFCELLIPSE` curve entities are sampled as canonical reference linework with `IFCAXIS2PLACEMENT` and product `IFCLOCALPLACEMENT` transforms
- the reference geometry translator smoke test verifies IFC `IFCCIRCLE` entities that reference `IFCAXIS2PLACEMENT2D` records with malformed origin `IFCCARTESIANPOINT` tuples emit `ifc-curve-invalid-placement-skipped` and do not synthesize product-placed default-origin circle linework
- the reference geometry translator smoke test verifies IFC `IFCCIRCLE` entities that reference `IFCAXIS2PLACEMENT` records with zero-length target axes or parallel placement axes emit `ifc-curve-invalid-placement-skipped` and do not synthesize fake product-placed circle linework
- the reference geometry translator smoke test verifies IFC non-rational `IFCBSPLINECURVEWITHKNOTS` entities are sampled as canonical reference linework with product `IFCLOCALPLACEMENT` transforms and without flattening to endpoint chord geometry
- the reference geometry translator smoke test verifies IFC `IFCRATIONALBSPLINECURVEWITHKNOTS` entities apply weight data when sampled as canonical reference linework with product `IFCLOCALPLACEMENT` transforms
- the reference geometry translator smoke test verifies IFC `IFCPOLYLINE` and `IFCCOMPOSITECURVE` entities compose canonical reference linework with product `IFCLOCALPLACEMENT`, honor `SameSense = FALSE`, and avoid duplicate child curve output
- the reference geometry translator smoke test verifies IFC `IFCTRIMMEDCURVE` parameter ranges over `IFCCIRCLE`/`IFCELLIPSE` base curves are sampled as canonical reference linework without importing the full base curves
- the reference geometry translator smoke test verifies IFC `IFCTRIMMEDCURVE` Cartesian point trims over `IFCCIRCLE`/`IFCELLIPSE` base curves are resolved to canonical reference linework without importing the full base curves
- the reference geometry translator smoke test verifies IFC `IFCTRIMMEDCURVE` parameter ranges over `IFCLINE` base curves use `IFCVECTOR` magnitude and product `IFCLOCALPLACEMENT` to emit finite canonical line segments
- the reference geometry translator smoke test verifies IFC `IFCVECTOR` records that reference malformed `IFCDIRECTION` coordinate tuples emit `ifc-vector-invalid-direction-skipped` and do not synthesize trimmed-line coordinates from partially parsed numeric values
- the reference geometry translator smoke test verifies IFC `IFCTRIMMEDCURVE` parameter ranges over `IFCBSPLINECURVEWITHKNOTS` base curves sample only the trimmed B-spline span with product `IFCLOCALPLACEMENT` and without importing full base curve endpoints
- the reference geometry translator smoke test verifies IFC `IFCFACEBOUND` inner loops are preserved as canonical reference linework while outer faces remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC faces without explicit `IFCFACEOUTERBOUND` use the first `IFCFACEBOUND` as canonical mesh geometry and preserve remaining bounds as inner linework
- the reference geometry translator smoke test verifies IFC mapped faceted BREP instances receive basic `IFCMAPPEDITEM` target transforms and product placement
- the reference geometry translator smoke test verifies IFC mapped faceted BREP instances receive non-uniform X/Y/Z `IFCMAPPEDITEM` target transforms composed with product placement
- the reference geometry translator smoke test verifies mapped IFC mesh `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with malformed origin `IFCCARTESIANPOINT` tuples emit `ifc-mapped-item-invalid-transform-skipped` and do not emit source-local, pre-product, or fake product-placed mesh vertices
- the reference geometry translator smoke test verifies IFC mapped curve linework receives `IFCMAPPEDITEM` target transforms and product placement without importing source curves at origin
- the reference geometry translator smoke test verifies mapped IFC curve `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with malformed origin `IFCCARTESIANPOINT` tuples emit `ifc-mapped-item-invalid-transform-skipped` and do not emit source-local, pre-product, or fake product-placed curve vertices
- the reference geometry translator smoke test verifies mapped IFC curve `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with malformed or zero-length target-axis `IFCDIRECTION` tuples emit `ifc-mapped-item-invalid-transform-skipped` and do not emit source-local, pre-product, or fake product-placed curve vertices
- the reference geometry translator smoke test verifies mapped IFC curve `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with parallel target axes emit `ifc-mapped-item-invalid-transform-skipped` for a degenerate transform basis and do not emit source-local, pre-product, or fake product-placed curve vertices
- the reference geometry translator smoke test verifies mapped IFC curve `IFCMAPPEDITEM` records that reference `IFCCARTESIANTRANSFORMATIONOPERATOR` records with malformed or zero explicit target scale values emit `ifc-mapped-item-invalid-transform-skipped` and do not emit source-local, default-scale, collapsed, or fake product-placed curve vertices
- the reference geometry translator smoke test verifies IFC mapped `IFCPOLYLINE` linework receives non-uniform X/Y/Z `IFCMAPPEDITEM` target transforms composed with product placement without importing source curves at origin
- the reference geometry translator smoke test verifies IFC mapped `IFCTRIMMEDCURVE` B-spline linework composes `IFCMAPPEDITEM` target transforms with product placement without importing source curves or full base B-spline endpoints
- the reference geometry translator smoke test verifies IFC mapped `IFCCLOSEDSHELL` representation items resolve through `IFCREPRESENTATIONMAP` and do not import source faces at origin
- the reference geometry translator smoke test verifies IFC `IFCDRAUGHTINGPREDEFINEDCOLOUR` styled items resolve named drafting colors into canonical mesh `display.color`
- the reference geometry translator smoke test verifies IFC `IFCSHAPEREPRESENTATION` `IFCSTYLEDITEM` appearance propagates to contained mesh, line-set, and point-cloud canonical objects without app-runtime style inference
- the reference geometry translator smoke test verifies IFC mapped mesh `IFCMAPPEDITEM` instance `IFCSTYLEDITEM` appearance overrides source `IFCINDEXEDCOLOURMAP` face colors before canonical mesh emission
- the reference geometry translator smoke test verifies IFC `IFCCURVESTYLE` styled curve items resolve presentation colors into canonical line-set `display.color`
- the reference geometry translator smoke test verifies IFC mapped curve `IFCMAPPEDITEM` instance `IFCSTYLEDITEM` appearance overrides the styled source curve item before canonical line-set emission
- the reference geometry translator smoke test verifies basic IFC `IFCEXTRUDEDAREASOLID` rectangle profiles are translated to canonical meshes with swept-solid and product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCEXTRUDEDAREASOLID` records with zero-length or malformed extrusion `IFCDIRECTION` records emit `ifc-extruded-solid-invalid-direction-skipped` and do not synthesize fake product-placed swept mesh vertices from fallback axes
- the reference geometry translator smoke test verifies basic IFC `IFCREVOLVEDAREASOLID` rectangle profiles are sampled to canonical meshes with swept-solid and product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCREVOLVEDAREASOLID` records with zero-length or malformed `IFCAXIS1PLACEMENT` axis `IFCDIRECTION` records emit `ifc-revolved-solid-invalid-axis-skipped` and do not synthesize fake product-placed revolved mesh vertices from fallback axes
- the reference geometry translator smoke test verifies IFC `IFCSWEPTDISKSOLID` directrix curves are sampled into canonical tube meshes with product placement transforms and without also emitting the directrix as standalone curve linework
- the reference geometry translator smoke test verifies IFC `IFCSWEPTDISKSOLID` start/end path-distance parameters trim the directrix before tube sampling and product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCSWEPTDISKSOLID` records with malformed explicit start/end path-distance parameters emit `ifc-swept-disk-invalid-parameter-skipped` and do not synthesize full, partially trimmed, or fake product-placed tube mesh vertices
- the reference geometry translator smoke test verifies IFC `IFCSWEPTDISKSOLID` records with malformed outer radius or inner radius parameters emit `ifc-swept-disk-invalid-parameter-skipped` and do not synthesize pre-product or product-placed tube mesh vertices from invalid pipe parameters
- the reference geometry translator smoke test verifies IFC `IFCSWEPTDISKSOLID` records with start/end path-distance parameters outside the directrix range or in reverse order emit `ifc-swept-disk-invalid-parameter-skipped` and do not synthesize pre-product or product-placed tube mesh vertices from clamped or swapped trim ranges
- the reference geometry translator smoke test verifies IFC revolved hollow profiles preserve sampled inner profile rings as canonical reference linework while outer revolved profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies compatible IFC `IFCEXTRUDEDAREASOLIDTAPERED` start/end profiles are translated to canonical tapered meshes with swept-solid and product placement transforms
- the reference geometry translator smoke test verifies compatible IFC tapered hollow profiles preserve start/end profile void loops as canonical reference linework while outer tapered profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC mapped tapered hollow profiles compose `IFCMAPPEDITEM` and product placement transforms for both outer tapered meshes and start/end profile void linework
- the reference geometry translator smoke test verifies IFC mapped revolved hollow profiles compose `IFCMAPPEDITEM` and product placement transforms for both sampled outer revolved meshes and sampled inner profile ring linework
- the reference geometry translator smoke test verifies IFC swept profile `IFCAXIS2PLACEMENT2D` positioning composes with swept-solid and product placement transforms
- the reference geometry translator smoke test verifies IFC swept profile or swept-solid `IFCAXIS2PLACEMENT` records with zero-length axes or degenerate/parallel placement bases emit `ifc-profile-invalid-placement-skipped` or `ifc-extruded-solid-invalid-placement-skipped` and do not synthesize fake product-placed swept mesh vertices
- the reference geometry translator smoke test verifies Tekla-style IFC `IFCDISCRETEACCESSORY`/`IFCMECHANICALFASTENER` products, common structural `IFCFOOTING`/`IFCPILE` products, `IFCFASTENER` products, reinforcement `IFCREINFORCINGBAR`/`IFCREINFORCINGMESH` products, and common distribution/MEP products such as duct segments, pipe fittings, and air terminals receive product placement transforms instead of disappearing or falling back to unplaced source geometry in the reference overlay
- the reference geometry translator smoke test verifies IFC `IFCBOOLEANRESULT` and `IFCBOOLEANCLIPPINGRESULT` items render their first supported operand with explicit fallback diagnostics while exact subtractive CSG remains adapter-owned
- the reference geometry translator smoke test verifies IFC 2D profile coordinates using scientific notation are parsed instead of causing valid swept solids to be skipped
- the reference geometry translator smoke test verifies IFC `IFCSTYLEDITEM` surface colors through `IFCSURFACESTYLE`/`IFCCOLOURRGB` split canonical mesh objects by display color while preserving all imported faces
- the reference geometry translator smoke test verifies IFC `IFCDERIVEDPROFILEDEF` parent-profile transforms compose `IFCCARTESIANTRANSFORMATIONOPERATOR2DNONUNIFORM`, swept-solid placement, and product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCMIRROREDPROFILEDEF` mirrors positioned parent profiles about the parent y-axis before swept-solid and product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCTRAPEZIUMPROFILEDEF` swept profiles are translated as canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCISHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCASYMMETRICISHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCLSHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCCSHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCUSHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCTSHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCZSHAPEPROFILEDEF` swept profiles are translated as sharp-corner canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCELLIPSEPROFILEDEF` swept profiles are translated as sampled canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCROUNDEDRECTANGLEPROFILEDEF` swept profiles are translated as sampled canonical reference meshes with product placement transforms
- the reference geometry translator smoke test verifies IFC `IFCRECTANGLEHOLLOWPROFILEDEF` swept profiles preserve inner hollow loops as canonical reference linework while outer profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC `IFCRECTANGLEHOLLOWPROFILEDEF` inner and outer fillet radii are sampled into canonical outer mesh geometry and inner-loop linework
- the reference geometry translator smoke test verifies IFC `IFCCIRCLEHOLLOWPROFILEDEF` swept profiles preserve sampled inner circular loops as canonical reference linework while outer profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC `IFCARBITRARYPROFILEDEFWITHVOIDS` inner curves on `IFCEXTRUDEDAREASOLID` profiles are preserved as canonical reference linework while outer swept profiles remain canonical mesh geometry
- the reference geometry translator smoke test verifies IFC mapped swept solids compose `IFCMAPPEDITEM` and `IFCLOCALPLACEMENT` transforms for both outer swept profile meshes and profile void linework
- the reference geometry translator smoke test validates canonical JSON passthrough import, chunk sidecar copying, validation-before-rename for atomic manifest/chunk promotion, manifest/chunk backup cleanup on replace-existing, restore-through-backup for manifest/chunk rollback, machine-readable rollback recovery metadata in JSON errors, chunk rollback when final manifest replacement fails, and cleanup of atomic promotion temp files
- the reference geometry translator smoke test rejects canonical reference objects whose layer references are missing, whose layer ids do not match their map keys, whose bounds are inverted or stale relative to payload bounds, whose primitive payloads are empty or degenerate, whose diagnostics point to missing reference objects, whose line/mesh indices point outside their vertex arrays, or whose asset coordinate-system axes are degenerate
- the reference geometry translator smoke test runs mock external adapters for DWG, STEP, IFC, and E57 and validates their canonical output
- the reference geometry translator smoke test verifies mock external adapters receive configured environment variables without leaking their values through adapter discovery or preflight output
- standalone adapter-request validator JSON failures keep structured local CI correlation fields such as `requestPath` and staged path metadata, but sanitize request-contract `errors[].message` values so invalid hand-written adapter requests cannot echo absolute request paths or private request file names into machine-readable proof logs
- standalone adapter-output validator JSON failures keep structured local CI correlation fields such as `requestPath` and `outputPath`, but sanitize `errors[].message` for manifest/sidecar failures so a missing or invalid adapter output cannot echo absolute paths or private output file names into machine-readable proof logs
- the reference geometry translator smoke test verifies successful external adapter stdout/stderr are preserved as bounded informational canonical diagnostics instead of being dropped
- the reference geometry translator smoke test verifies external adapters can receive a schema-validated staged `{request}` JSON file with canonical `format`, original `requestedFormat`, normalized source identity, original source file extension, selected adapter key, adapter config path/directory, stage directory, scratch directory, chunk path prefix, schema contract versions, and import parameters
- the reference geometry translator smoke test verifies external adapter output cannot spoof the final canonical `asset.id`
- the reference geometry translator smoke test verifies external adapter output does not force the translator to hash the source input and that adapter-emitted `asset.source.checksum` values are discarded unless a trusted translator step verifies them
- the reference geometry translator smoke test verifies a named external adapter must declare support for the requested source format before launch
- the reference geometry translator smoke test verifies schema-invalid external adapter configs fail before the adapter process is launched or output is promoted
- the reference geometry translator smoke test verifies `--keep-stage-on-error` preserves failed external adapter staging files for inspection while still refusing to promote invalid output
- the reference geometry translator smoke test verifies timed-out external adapters fail with timeout detail, including machine-readable `--json` timeout metadata, and do not promote partial staged output
- the reference geometry translator smoke test verifies oversized inline point-cloud payloads and aligned point attributes from an external adapter are promoted into atomically written and validated chunk sidecars
- the reference geometry translator smoke test verifies inline point-cloud chunking recomputes object bounds from real points and keeps generated chunk manifest bounds aligned with sidecar bounds
- the reference geometry translator smoke test rejects point-cloud objects that mix inline `points` with chunked `chunkIds` or repeat a chunk id in `chunkIds`
- the reference geometry translator smoke test rejects point-cloud chunks whose owner is not a point-cloud object or whose id is not listed by the owning object
- the reference geometry translator smoke test rejects external adapter point-cloud chunk paths that escape the staged manifest directory before any target manifest or target sidecar can be promoted
- the reference geometry translator smoke test rejects mismatched point-cloud attribute arrays and zero-vector point normals before adapter output is promoted
- the reference geometry translator smoke test rejects inline point-cloud object bounds that do not match their point payload when the payload stays inline
- the reference geometry translator smoke test rejects chunked point-cloud object bounds that do not match the complete union of referenced chunk bounds, including declared object bounds whose referenced manifest chunks do not all carry bounds
- the reference geometry translator smoke test rejects point-cloud chunk manifests whose `pointCount` disagrees with the sidecar payload before adapter output is promoted
- the reference geometry translator smoke test rejects point-cloud chunk sidecars whose bounds do not match their point payload before adapter output is promoted
- the reference geometry importer check-references smoke test verifies unsafe, missing, malformed-JSON, schema-invalid, identity-mismatched, count/bounds/attribute-invalid, and schema-check-failed point-cloud chunk sidecars report path-free class messages while preserving manifest-relative sidecar paths in bounded missing/invalid chunk entries
- the viewer runtime smoke test verifies inline and chunked reference point-cloud previews use per-point intensity colors from canonical `pointAttributes`, and verifies direct reference point-cloud preview rejects out-of-range or misaligned RGB attributes before falling back to aligned intensity colors while rejecting mixed inline/chunked storage, duplicate or unsafe object chunk ids, chunked object-level attributes, and missing/unsafe/mismatched/duplicated manifest chunk entries before primitives or preview-stat entries are emitted
- the viewer runtime smoke test verifies reference point-cloud previews honor `render.referenceGeometry.pointPreviewLimit` before expanding preview glyphs, limit selected point-cloud sidecars with `render.referenceGeometry.pointPreviewChunkLimit`, report only budget-selected sidecars as preview-loaded in pointer-only QA runtime status, distinguish missing selected chunks from budget-omitted chunks, and report rendered/omitted point counts in runtime-only `referenceGeometryPreviewStats`
- the viewer runtime smoke test verifies reference line-set and mesh previews honor `render.referenceGeometry.lineSegmentPreviewLimit` and `render.referenceGeometry.meshFacePreviewLimit`, report rendered/omitted segment/face counts in runtime-only `referenceGeometryPreviewStats`, reject unsafe direct reference asset/object ids, unsafe/missing/mismatched direct reference layer ids, and degenerate direct line-set/mesh payloads before scene primitives or preview-stat entries are emitted, and sanitize invalid direct reference display colors/opacity to the next valid source or viewer defaults before primitives are emitted
- the viewer runtime smoke test verifies the QA bridge exposes a defensive copy of current-scene `referenceGeometryPreviewStats` through both `window.__boberCadQa.referenceGeometryPreviewStats()` and the existing `bobercad:qa-request` DOM request bridge
- the viewer runtime smoke test verifies the QA bridge exposes pointer-only reference geometry runtime load status through both `window.__boberCadQa.referenceGeometryRuntimeStatus()` and the existing `bobercad:qa-request` DOM request bridge, including loaded, not-loaded, source-format, requested-format family, snap, preview chunk count metadata, diagnostic code counts, and bounded sanitized load diagnostic entries with stage/severity/code/message fields without returning canonical geometry payloads, unsafe project reference pointers, or runtime manifest URLs; it also verifies Files/left-dock source descriptors carry runtime status tokens, structured requested-format family fields/search keywords for alias tokens such as IFC ZIP and E57 point cloud, normalizes the E57 point-cloud alias family to `e57` in QA status and Project Files runtime rows even when only alias `asset.source.format` provenance is present, preserves Project Files dataset search metadata, and reports diagnostic summaries as metadata only
- the viewer runtime smoke test rejects unsupported top-level manifest, asset, `asset.source`, layer, object, diagnostic, manifest chunk, and point-cloud chunk sidecar metadata fields before reference geometry can render or load point-cloud chunk sidecars, rejects malformed canonical source checksums outside the 64-character lowercase SHA-256 shape and path-like translator/version provenance, while accepting canonical object and sidecar `metadata` objects
- the viewer runtime smoke test verifies the same canonical reference manifest renders identical reference geometry primitives and preview stats when only valid `asset.source.format` / `asset.source.requestedFormat` provenance changes across DXF, DWG, STEP, IFC, E57, E57 point-cloud, and canonical JSON tokens, while cross-family requested-format provenance is rejected before rendering
- the viewer runtime smoke test verifies reference asset `units` and `coordinateSystem` are applied before the project reference asset transform
- the viewer runtime smoke test verifies reference geometry snap candidates are gated by `snapEnabled` and exclude point-cloud preview glyphs
- the viewer runtime and left-dock contract smoke tests verify reference geometry Files/search rows expose pointer-only visibility, snap, display override, and transform status without reading canonical manifests
- the reference geometry translator smoke test verifies invalid staged built-in and adapter output does not replace an existing target manifest or chunk sidecar
- domain validation checks project reference asset paths and transforms, rejects reference asset ids stored in `objectIndex`, model collections, top-level `model` fields, or `model.addonData`, and checks canonical bounds, non-empty and non-degenerate reference geometry payloads, diagnostic object references, chunk paths, local schemas, point-cloud storage mode, point-cloud chunk reference uniqueness, point-cloud chunk ownership/reachability, point-cloud attribute lengths, line/mesh object payload bounds, asset payload bounds including unverifiable declared asset bounds, inline point payload bounds, chunked point-cloud object bounds including missing referenced chunk bounds, chunk payload bounds, and object id collisions
- domain validation checks project reference asset ids against their canonical manifest `asset.id`
- Smart Component register and component `config.json` files validate against their schemas
- important docs exist
- `AGENTS.md` points to current paths
- sample project `objectIndex` entries point to existing model objects
- sample projects do not use old `model.patterns` or `patternRef` names

Use:

```powershell
node .\scripts\validate_json_schema.js .\bobercad\data\projects\sample_seed_connection_structure.json
```

This validates a JSON file against the schema referenced by its `$schema` field. With no arguments it validates sample projects, reference geometry files, point-cloud chunks, the Smart Component register, and Smart Component `config.json` files. It has no third-party package requirement.

- the reference geometry importer smoke tests verify JSON failure summaries expose `referenceImportFailureDecision` with the failed workflow stage when available, stable `adapterErrorCode`, compact `failureKind`/`likelyFixArea`, recommended next action, retry stage, and boolean fix gates; covered routing includes missing adapter config, plan/import adapter-preflight config or dependency failures, adapter process failure, invalid canonical adapter output, unsupported source-format import options, and CLI option conflicts without parsing human-readable messages

## Validation Philosophy

- Schema validation checks shape.
- Domain validation checks cross-file references, project object references, reference geometry pointers, numbering, fabrication rules, and geometry constraints that JSON Schema cannot express cleanly.
- Do not mix domain validation into `scripts/validate_json_schema.js`; that script is schema-only.
- Keep domain checks in `scripts/validate_domain_model.js` or another focused contract script.
